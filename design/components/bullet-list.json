{
  "$schema": "../schemas/component.schema.json",
  "$comment": "箇条書き。bullets の本文として使う（DR-0010）。",
  "name": "bullet-list",
  "role": "並列関係にある複数の項目を、見出しの下にまとめて示す。",
  "allowedIn": ["bullets"],
  "usage": [
    "使ってよい個数は allowedIn の各 layout の slots が定める",
    "項目のあいだに時系列や手順の意味を持たせない。順序を示したいときはこの部品を使わない",
    "各項目は 1 文の短い主張にする。項目の中で改行や入れ子構造を作らない"
  ],
  "props": {
    "items": {
      "type": "string[]",
      "required": true,
      "description": "箇条書きの各項目。1 項目 1 文。"
    }
  }
}
