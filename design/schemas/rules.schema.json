{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/nashiusagi/slide-design-system/design/schemas/rules.schema.json",
  "title": "design/rules.json",
  "$comment": "検査ルールの契約を検証するスキーマ（DR-0009 / DR-0011）。rules 配列がルールIDと method（lint / measure / review）の対応の正本になる。閾値（contrast / minFontSize / noRawScale）は各ルールの実装がここを読みに来る値で、DR 本文には複製しない（DR-0008 / DR-0012）。",
  "type": "object",
  "required": ["rules", "contrast", "minFontSize", "noRawScale", "noOverflow"],
  "additionalProperties": false,
  "properties": {
    "$schema": { "type": "string" },
    "$comment": { "type": "string" },
    "rules": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["id", "method", "severity", "description"],
        "allOf": [
          {
            "$comment": "review は人が判断し、自動判定を持たない（DR-0011）。機械で実行する bypass フィクスチャを要求しても走らせる入口が無いため、宣言そのものを求めない（DR-0044）。制約の本体は下の properties にある。",
            "if": {
              "required": ["method"],
              "properties": { "method": { "not": { "const": "review" } } }
            },
            "then": {
              "required": ["bypassAxes", "scopeExclusions"],
              "properties": { "bypassAxes": {}, "scopeExclusions": {} }
            }
          }
        ],
        "properties": {
          "id": { "type": "string", "pattern": "^[a-z]+(-[a-z]+)*$" },
          "method": {
            "type": "string",
            "enum": ["lint", "measure", "review"],
            "description": "検査の実行手段。lint は ESLint、measure は Playwright での実測、review は人（DR-0011）。"
          },
          "severity": {
            "type": "string",
            "enum": ["error", "warn"],
            "description": "method が review のときは自動合否を持たないため常に無視される（README の検査表）。"
          },
          "description": { "type": "string", "minLength": 1 },
          "bypassAxes": {
            "type": "array",
            "minItems": 1,
            "uniqueItems": true,
            "description": "このルールが塞いだことを示すべき『書き方の軸』（DR-0044）。軸ごとに bypass フィクスチャの事例を1つ以上持つことを scripts/validate-design.mjs が検査する。",
            "items": {
              "type": "string",
              "enum": ["alternate-notation", "value-composition", "boundary", "enumeration-tail"]
            }
          },
          "scopeExclusions": {
            "type": "array",
            "uniqueItems": true,
            "description": "このルールが意図的に見ない領域（DR-0044）。ここに挙げた id ごとに、通ることを固定するフィクスチャの事例を1つ以上持つ。挙げていない見落としは抜け道であって除外ではない。",
            "items": {
              "type": "object",
              "additionalProperties": false,
              "required": ["id", "description"],
              "properties": {
                "id": { "type": "string", "pattern": "^[a-z]+(-[a-z]+)*$" },
                "description": { "type": "string", "minLength": 1 }
              }
            }
          }
        }
      }
    },
    "contrast": {
      "type": "object",
      "additionalProperties": false,
      "required": ["surfaces", "requirements"],
      "description": "contrast ルール（measure）の閾値。scripts/validate-design.mjs の算出値検査と、将来の measure 実装が読む（DR-0008 / DR-0011 / DR-0033）。",
      "properties": {
        "$comment": { "type": "string" },
        "surfaces": {
          "type": "array",
          "minItems": 1,
          "uniqueItems": true,
          "items": { "type": "string", "minLength": 1 }
        },
        "requirements": {
          "type": "array",
          "minItems": 1,
          "items": {
            "type": "object",
            "additionalProperties": false,
            "required": ["role", "foregrounds", "minimum"],
            "properties": {
              "role": { "type": "string", "minLength": 1 },
              "foregrounds": {
                "type": "array",
                "minItems": 1,
                "uniqueItems": true,
                "items": { "type": "string", "minLength": 1 }
              },
              "minimum": { "type": "number", "exclusiveMinimum": 1 }
            }
          }
        }
      }
    },
    "minFontSize": {
      "type": "object",
      "additionalProperties": false,
      "required": ["px"],
      "description": "min-font-size ルール（measure）の下限（DR-0012）。",
      "properties": {
        "$comment": { "type": "string" },
        "px": { "type": "integer", "exclusiveMinimum": 0 }
      }
    },
    "noOverflow": {
      "type": "object",
      "additionalProperties": false,
      "required": ["toleranceInPx"],
      "description": "no-overflow ルール（measure）の許容誤差（DR-0011 帰結）。",
      "properties": {
        "$comment": { "type": "string" },
        "toleranceInPx": { "type": "number", "minimum": 0 }
      }
    },
    "noRawScale": {
      "type": "object",
      "additionalProperties": false,
      "required": ["allowedLiterals"],
      "description": "no-raw-scale ルール（lint）が例外的に許す長さリテラル（DR-0011 帰結）。",
      "properties": {
        "$comment": { "type": "string" },
        "allowedLiterals": {
          "type": "array",
          "minItems": 1,
          "uniqueItems": true,
          "items": { "type": "string", "minLength": 1 }
        }
      }
    }
  }
}
