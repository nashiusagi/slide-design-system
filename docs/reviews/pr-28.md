# PR #28 レビュー

- **タイトル**: 公開データの sanitize と audit
- **レビュー日**: 2026-09-10
- **対象**: main...feature/11_public-data-sanitize-audit
- **変更ファイル数**: 15

## 要約

blocker 0件、should 7件、consider 2件。6観点すべてから指摘が出たが、いずれも成立し、同じPR内で修正した。最も重かったのはコード品質観点の指摘で、バイナリ判定が拡張子の大文字小文字を無視しており、大文字拡張子の画像等を sanitize が UTF-8 として読み書きして破損させうる状態だった。検査ルールの実効性観点からは、`docs/PUBLICATION_POLICY.md` 自身が名指しする脅威（一時ディレクトリのパス、単独のユーザー名）の一部を audit が検査していないという、ドキュメントと実装の不一致も見つかった。

## should

### code/binary-extension-check-case-sensitive — 拡張子の大文字小文字を無視しているため、大文字拡張子のバイナリが sanitize で破損しうる

- **場所**: `scripts/sanitize-run-artifacts.mjs`、`scripts/audit-public-data.mjs`（いずれも `BINARY_EXTENSIONS.has(extname(...))` を大文字小文字を区別したまま使っていた）
- **指摘した観点**: コード品質
- **累計**: 1回目（新規カテゴリ）
- **何が起きているか**: `extname()` は拡張子をそのまま返す（`extname('Screenshot.PNG')` は `'.PNG'`）。`BINARY_EXTENSIONS` は小文字のみの `Set` だったため、大文字拡張子のファイルは「バイナリではない」と誤判定されていた。レビュアーが実際に疑似バイナリ（不正バイトを含む25バイト）を大文字拡張子で保存し、`sanitizeDirectory` に通したところ、`writeFileSync` の UTF-8 再エンコードで内容が変わり、元のバイト列と一致しなくなることを確認した。
- **なぜ問題か**: 保存 Run を安全に公開用へ整形するという本PRの目的そのものに反する。画像を書き換えて壊してしまっては、audit が緑でも公開できるデータにならない。
- **対応**: `extname(path).toLowerCase()` で比較する `isBinaryPath` を `scripts/lib/fs-walk.mjs` へ切り出し、両スクリプトから使うようにした。大文字拡張子（`Screenshot.PNG`）で書き込まれないことを固定する回帰テストを追加した。

### inspection/rule-coverage-partial — audit が `PUBLICATION_POLICY.md` 自身の挙げる脅威の一部（一時ディレクトリのパス、単独のユーザー名）を検査していない

- **場所**: `scripts/audit-public-data.mjs`（`LEAK_PATTERNS` が `/home/` と `/Users/` の接頭辞、および秘密鍵形式しか持たない）
- **指摘した観点**: 検査ルールの実効性
- **累計**: 5回目
- **何が起きているか**:
  1. `docs/PUBLICATION_POLICY.md` は絶対パス漏洩の例として「隔離ワークスペースの絶対パス（`/home/<user>/...` **や一時ディレクトリのパス**）」を挙げているが、`prepare-workspace.mjs` の既定の出力先は `os.tmpdir()`（Linux では通常 `/tmp`）配下であるにもかかわらず、`LEAK_PATTERNS` には `/tmp/` を拾うパターンが無かった。実際に `/tmp/...` を含む文字列が検知されないことを確認した。
  2. 同ポリシーは「OS ユーザー名。...単独の文字列として混ざることがある」とも書くが、`LEAK_PATTERNS` にはパス形状・秘密鍵形状しか無く、単独文字列としてのユーザー名を拾う手段が無かった。sanitize 側は単語境界つきの置換をすでに実装しているのに、audit 側にそれと対になる検査が無い状態だった。
- **なぜ問題か**: `pnpm public:audit`（`pnpm check` 経由）が緑でも、ポリシー文書が明示する脅威の半分が素通りする。「audit が通る＝既知パターンは無い」という前提が、文書に書かれた既知パターンの全部をカバーしていなかった。
- **対応**: `tmp-path` / `macos-tmp-path` の静的パターンを追加した（一時ディレクトリの接頭辞は環境に依存しない一般的な形なので、sanitize 側と同様に固定パターンで拾える）。ユーザー名は `buildLeakPatterns({ username })` を新設し、既定で現在の OS ユーザー名を単語境界つきで検査対象へ加えた。ただし audit を実行しているマシンのユーザー名しか分からないため、別マシンで生成し sanitize しなかった Run のユーザー名までは検出できないという限界を `docs/PUBLICATION_POLICY.md` に明記した。

### decisions/undocumented-decision — audit の API キー/token 検査対象パターンの選定が DR に無い

- **場所**: `scripts/audit-public-data.mjs`（`LEAK_PATTERNS` に OpenAI/Anthropic/GitHub/AWS/Slack の5形式 + 汎用代入パターン）
- **指摘した観点**: 決定記録（DR）との整合
- **累計**: 9回目
- **何が起きているか**: DR-0023 の「決定」節は audit の役割を「置換漏れの文字列検査」とだけ定め、API キー・token には一言も触れていない。Issue #11 の完了条件がこれを要求してはいるが、どの形式を選ぶか・閾値をどう置くかという実装判断は DR に無いまま実装された。
- **なぜ問題か**: 「他の選択肢がありえて、選んだ理由を後から聞かれうる判断」（命名・閾値・対象範囲の選定）が記録されずに既成事実化していた。半年後に「なぜこの5形式か」を再検討する材料が無い。
- **対応**: [DR-0040](../decisions/0040-audit-secret-pattern-selection.md) を新設し、対象形式の選定理由・却下した代替（外部secret-scanningツール導入、汎用パターンのみに絞る案、有効性の外部確認）を記録した。DR-0023 の「決定」節からも DR-0040 へリンクした。

### contract/design-data-duplicated — `README.md` が `docs/PUBLICATION_POLICY.md` の内容を複製している

- **場所**: `README.md`「公開時の注意」節
- **指摘した観点**: 設計契約との整合
- **累計**: 6回目
- **何が起きているか**: 本PRは `README.md` の「正本の在り処」表に `docs/PUBLICATION_POLICY.md` を正本として追加する一方、同じ README の「公開時の注意」節に、保存 Run の内容・sanitize/audit の役割・「audit は公開承認ではない」「最終判断は人」といった、正本であるはずの内容をほぼそのまま書き写していた。
- **なぜ問題か**: 「設計データは正本にのみ置く。Skill やドキュメントへ複製しない」という README 自身の既存ルールに反する。将来 `PUBLICATION_POLICY.md` を改訂しても README 側は自動的に追随せず、どちらが最新かを読者が判断できなくなる。
- **対応**: 「公開時の注意」節を `docs/PUBLICATION_POLICY.md` への参照一文に絞り、具体的な基準文言は正本側だけに残した。

### writing/contradictory-instruction — 保存 Run に含まれるものの説明が DR-0023 と実際の保存形式で食い違う

- **場所**: `README.md`、`docs/PUBLICATION_POLICY.md` vs `docs/decisions/0023-public-repo-with-audit.md`（変更前）
- **指摘した観点**: 日本語ドキュメント
- **累計**: 3回目（ルール化候補入り）
- **何が起きているか**: 本PRの README/POLICY は「生成ソース、採点結果、実測結果」と書くが、両方が根拠として引く DR-0023 は変更前の時点で「生成ソース、`changes.diff`、`events.jsonl`（AI の行動ログ）、各種検証ログ、プロンプト全文」と書いており、列挙が一致していなかった（DR-0023 は #10 の実装確定前に書かれた参考元由来の想定を引きずっていた）。
- **なぜ問題か**: 同じ決定記録を権威として指す3文書が、この PR の主題（何を sanitize/audit で守るべきか）について異なる事実を述べており、読み手によって解釈が割れる。
- **対応**: DR-0023 の文脈節を、実際の保存形式（#10 で確定した `run.schema.json`）に合わせて修正し、当初の参考元由来の想定（`changes.diff` / `events.jsonl`）は実装しなかった旨を明記した。README/POLICY 側の記述はそのまま維持し、3文書の記述を揃えた。

### writing/inaccurate-rationale — sanitize の「バックアップを取らない理由」が、同じPRが定める運用手順と矛盾する

- **場所**: `scripts/sanitize-run-artifacts.mjs` の JSDoc、`docs/PUBLICATION_POLICY.md`「使い方」節
- **指摘した観点**: 日本語ドキュメント
- **累計**: 5回目
- **何が起きているか**: sanitize のコメントは「バックアップは取らない（git 管理下にあるため、必要なら差分を戻せる）」と書いていたが、同じPRの `PUBLICATION_POLICY.md` は「Run を保存したら、**コミットする前に** sanitize → audit の順で実行する」と定めている。この主要な使用場面では対象はまだ未追跡ファイルで、戻せる差分が存在しない。
- **なぜ問題か**: コメントを信じて「まずければ git で戻せる」と考えたまま実行すると、コミット前の Run データを失いうる。
- **対応**: コメントを実態に合わせ、「疑いが残るときは実行前に `git add -N` するか、コミット後に実行する」旨に書き直した。

## consider

### code/duplicate-binary-extensions-list — `BINARY_EXTENSIONS` が2ファイルへ同一内容で複製されていた

- **場所**: `scripts/sanitize-run-artifacts.mjs`、`scripts/audit-public-data.mjs`
- **指摘した観点**: コード品質
- **累計**: 1回目（新規カテゴリ）
- **対応**: 上記の大文字小文字バグの修正と合わせ、`scripts/lib/fs-walk.mjs` の `BINARY_EXTENSIONS` / `isBinaryPath` へ統合した。

### writing/test-title-ambiguous — テストタイトルが固定する不変条件を示していなかった

- **場所**: `scripts/sanitize-run-artifacts.test.mjs`（「既定の identifiers（現在の OS の homedir/username）でも動く」）
- **指摘した観点**: 日本語ドキュメント
- **累計**: 2回目
- **対応**: 「identifiers を省略すると、例外を投げずファイルをそのまま残す（既定値として現在の OS の homedir/username を使う）」に改めた。

## 落とした指摘

なし。9件すべて検証の上で成立を確認し、修正した。

## ルール化候補

- `inspection/rule-coverage-partial` — 通算5回。
- `decisions/undocumented-decision` — 通算9回。
- `writing/contradictory-instruction` — 通算3回。ドキュメント間で同じ決定記録を引用しているとき、その決定記録が記述する内容と実際の記述が一致しているかを突き合わせる仕組み（`checkSkillNoDesignDataDuplication` に似た、文書間の事実整合性チェック）を検討する余地がある

## 2周目（2026-09-10）

前周の指摘9件すべての修正を、前周のコミット（9ed8bd5）からの増分に対して6観点で再レビューした。フェーズスコープ・検査ルールの実効性のうち検査対象追加自体は問題なしだったが、検査ルールの実効性観点から重大な誤検知リスクが新たに見つかった。合計8件の指摘が出て、いずれも検証の上で成立し、同じPR内で修正した。

### should

#### inspection/rule-coverage-partial — `current-username` パターンが汎用アカウント名に一致し、コンテナ/CI環境で保存Runのほぼ全件を誤検知する

- **場所**: `scripts/audit-public-data.mjs`（`buildLeakPatterns` の `current-username` パターン）
- **何が起きているか**: 前周で追加した「実行環境のOSユーザー名を単独文字列として検査する」パターンが、`root` のような汎用アカウント名にも無条件で適用されていた。`root` は React/Vite の scaffold が持つ `id="root"` というボイラープレートと一致するため、audit を root 権限のコンテナ（Docker ベースのCI等でよくある）で実行すると、保存 Run のほぼ全件が「ユーザー名の漏洩」として誤検知されることを実際に確認した。
- **対応**: `root` / `admin` / `node` / `runner` 等の汎用アカウント名の除外リスト（`GENERIC_USERNAMES`）を追加し、該当する場合は `current-username` パターンを生成しないようにした。回帰テストを追加した。

#### code/audit-test-env-username-coupling — `current-username` パターンに依存するテストが実行環境のOSユーザー名次第で不決定になる

- **場所**: `scripts/audit-public-data.test.mjs`（`auditDirectory` の厳密な `toEqual([])` を使う2テスト）
- **累計**: 1回目（新規カテゴリ）
- **何が起きているか**: `LEAK_PATTERNS` が既定でOSユーザー名を含むようになった一方、`auditDirectory` を呼ぶ一部テストは `patterns` を明示せずデフォルトのまま `toEqual([])` を主張しており、実行環境のユーザー名がテストフィクスチャの語（`dist` 等）と偶然一致すると無関係な理由で失敗しうる状態だった。
- **対応**: 該当テストを `buildLeakPatterns({ username: '' })` で明示的に静的パターンのみに絞る形へ揃えた。

#### decisions/citation-overclaims-source-scope（新規） — DR-0023 が採点結果・実測結果の正本を誤って引用していた

- **場所**: `docs/decisions/0023-public-repo-with-audit.md`
- **累計**: 1回目（新規カテゴリ）
- **何が起きているか**: 前周の修正で「保存Runには生成ソース・採点結果・実測結果が含まれ、run.schema.jsonを正本とする」と書いたが、run.schema.json 自身の `$comment` は採点結果をスキーマの対象外と明記しており、引用が実態と食い違っていた。
- **対応**: メタデータは `run.schema.json`、Run ディレクトリ全体の構成は `scripts/README.md` を正本とする、と正確に分けて引用し直した。

#### contract/design-data-duplicated — README修正の過程で、同じ複製がDR-0023の文脈節とJSDocへ移っただけだった

- **場所**: `docs/decisions/0023-public-repo-with-audit.md`（文脈節）、`scripts/audit-public-data.mjs`（`buildLeakPatterns` のJSDoc）
- **何が起きているか**: 前周「READMEがPUBLICATION_POLICY.mdを複製していた」指摘への対応で、README側は参照一文へ絞ったが、同じ増分の中でDR-0023の文脈節とJSDocコメントに、PUBLICATION_POLICY.mdの具体的な記述（漏洩経路の列挙、ユーザー名検出の限界説明）がほぼそのまま書き写されていた。
- **対応**: 両箇所とも`docs/PUBLICATION_POLICY.md`への参照一文に絞り、具体的な列挙・説明は正本側だけに残した。

#### writing/inaccurate-rationale — sanitizeのコメントが助言する `git add -N` が実際には機能しない

- **場所**: `scripts/sanitize-run-artifacts.mjs`
- **何が起きているか**: 前周追加した「疑いが残るときは `git add -N` して差分を取れるようにする」という助言を実際に検証したところ、`git add -N` はintent-to-addであり、sanitize後に`git diff`しても「新規追加」としてしか見えず、`git checkout --`で復元しようとするとファイルが空になることを確認した（前周指摘「バックアップが無い」を別の形で再発させていた）。
- **対応**: `git add -N` ではなく `git add`（フルステージ）を使う助言に修正した。

#### writing/contradictory-instruction — DR-0023書き直し後の文で、内容の列挙とその後の言及が食い違っていた

- **場所**: `docs/decisions/0023-public-repo-with-audit.md`
- **何が起きているか**: 前周の書き直しで保存Runの内容を「生成ソース・採点結果・実測結果」の3種に絞ったにもかかわらず、直後の文で「実測結果やログ、生成ソースのコメント」と、3種に含まれない「ログ」への言及が残っていた。
- **対応**: 文脈節全体を参照一文へ絞る書き直し（上記の複製解消と同じ修正）で、この矛盾も解消した。

### consider

#### writing/subjectless-predicate（新規） — README圧縮後の一文に主語が無かった

- **場所**: `README.md`
- **対応**: 「実行コマンドは `pnpm public:sanitize` / `pnpm public:audit`」と体言止めに変え、主語問題を解消した。

## 落とした指摘

なし。8件すべて検証の上で成立を確認し、修正した。

## ルール化候補

今周で新たに3回以上に達したカテゴリは無い。

## 3周目（2026-09-10、打ち切り）

前周の指摘8件すべての修正を、前周のコミット（ce47364）からの増分に対して6観点で再レビューした。フェーズ・コード品質は問題なし。残り4観点から計4件の指摘が出て、いずれも検証の上で成立し、同じPR内で修正した。うち3件（下記「複数観点から指摘された」の項目）は、独立した3観点（設計契約・決定記録・日本語）が同じ根本原因（DR-0023の引用先の誤り）を別々に見つけたもの。

### should

#### decisions/citation-points-to-wrong-file（新規、複数観点から指摘された: 設計契約・決定記録・日本語）— DR-0023 が「Run ディレクトリ全体の構成の正本」として誤ったファイルを引用していた

- **場所**: `docs/decisions/0023-public-repo-with-audit.md`
- **何が起きているか**: 前周の修正で「Run ディレクトリ全体の構成は `scripts/README.md` を正本とする」と書いたが、`scripts/README.md` にはスクリプト一覧の役割表しかなく、Run ディレクトリの構成（`run.json`/`source/`/`dist/`/`scoring.json`）についての記述が無い。実際にそれを記述しているのは `experiments/harness-intro/runs/README.md` だった。
- **なぜ問題か**: 前々周に見つかった「run.schema.jsonへの過剰な引用」（decisions/citation-overclaims-source-scope）を直す過程で、分割した引用先の一方が誤ったファイルを指すようになっていた。同じ一文が3周連続で問題を持ち続けている。
- **対応**: 引用先を `experiments/harness-intro/runs/README.md` に訂正した。

#### inspection/rule-coverage-partial（再発）— `GENERIC_USERNAMES` の除外リストが不完全で、同種の誤検知が別の名前で再発する

- **場所**: `scripts/audit-public-data.mjs`
- **何が起きているか**: 前周追加した除外リストに `root`/`admin`/`node`/`runner` 等はあったが、同じ基準（OS/コンテナの既定アカウント名、短い識別子）に当てはまる `pi`（Raspberry Pi OS既定ユーザー）・`core`（Fedora CoreOS既定ユーザー）・`default`（Kubernetes既定サービスアカウント）・`app`（一部PaaSの既定ユーザー）が抜けており、実際に `pi` はminify後のコードの短い識別子と、`default` は `export default` と衝突して誤検知することを確認した。
- **なぜ問題か**: 手作業の列挙だけに頼る限り、同じ種類の誤検知が名前を変えて何度でも再発する。
- **対応**: `core`/`default` を除外リストへ追加し、根本対応として「4文字未満のユーザー名は対象にしない」（`MIN_USERNAME_LENGTH`）という長さベースの除外を追加した。これにより `pi`/`app` も長さで自動的に除外される。リストが網羅を保証しないことを明記した。

#### contract/design-data-duplicated（再発）— `GENERIC_USERNAMES` の除外理由が JSDoc と `PUBLICATION_POLICY.md` の両方にほぼ同一内容で書かれていた

- **場所**: `scripts/audit-public-data.mjs`、`docs/PUBLICATION_POLICY.md`
- **何が起きているか**: 前周「READMEがPUBLICATION_POLICY.mdを複製していた」問題を直した際、同じ複製が今度は `GENERIC_USERNAMES` のJSDocとPUBLICATION_POLICY.mdの間で新たに発生していた。
- **対応**: JSDocを短縮し、詳細説明はPUBLICATION_POLICY.md側だけに残した。

#### writing/dangling-quote-reference — JSDocが参照する「DR-0023 の帰結」に該当する内容が無い

- **場所**: `scripts/audit-public-data.mjs`
- **累計**: 2回目
- **何が起きているか**: `GENERIC_USERNAMES` のJSDocが「DR-0023 の帰結を参照」と書いていたが、DR-0023の帰結節にはこの誤検知対策についての記述が無かった。
- **対応**: 上記の複製解消に合わせてJSDocを短縮する過程で、この宙ぶらりんな参照も削除した。

## 落とした指摘

なし。4件すべて検証の上で成立を確認し、修正した。

## ルール化候補

今周で新たに3回以上に達したカテゴリは無い。

## 打ち切りの判断

3周連続でレビューを実施し、毎周見つかった指摘はすべて同じPR内で修正した（1周目9件・2周目8件・3周目4件、blocker 0件）。しかし3周目も指摘ゼロでは終わらなかったため、`issue-workflow` の基準（3周で収束しないなら打ち切り）に従い、ここで打ち切る。

**見立て**: 未対応のまま残っている指摘は無い。問題は「直しきれなかったこと」ではなく、「1つの修正が、形を変えて同種の問題（正本の複製、DRの引用不正確）を別の場所に作り直す」というパターンが3周連続で発生したこと。特に次の2つの根本原因が疑われる。

1. **`docs/decisions/0023-public-repo-with-audit.md` の文脈節は、このPRを通じて4回書き直された。** 保存Runの内容確定（#10）に対してDR-0023が追随しておらず、その追随作業のたびに新しい不正確さ（過剰な範囲の主張、誤ったファイルの引用）を生んでいた。DR-0023自体が古くなっていたことが根本原因で、今回の書き直しで実態に追いついたはずである。
2. **`current-username` のような「実行環境に依存するヒューリスティックな検査」は、性質上、列挙による対症療法では追いつかない。** 今回 `MIN_USERNAME_LENGTH` という長さベースの根本対応を追加したが、それでも将来別の4文字以上の汎用アカウント名（例: `test1`、`user1`）で同種の誤検知が起きる可能性は残る。

いずれも今回の修正で実務上のリスクは大きく下がったと判断するが、「本当に打ち切ってよいか」（4周目のレビューをもう1周走らせるか、現状で十分と判断するか）は人が決めてほしい。

レビュー全文: `docs/reviews/pr-28.md`
