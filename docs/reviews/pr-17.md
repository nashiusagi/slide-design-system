# PR #17 レビュー

- **タイトル**: 開発の一連の流れを担う workflow skill を作る
- **レビュー日**: 2026-09-06
- **対象**: main...feature/16_dev-workflow-skill
- **変更ファイル数**: 4

## 要約

blocker 0件、should 6件、consider 4件。差分は `.claude/skills/issue-workflow/` の新規4ファイルのみで、設計データ（色・余白・キャンバス寸法・layout / component 一覧）の複製も、検査ルールへの変更も無い。

指摘は2系統に割れた。ひとつは**手順書としての穴** — `<N>` が Issue 番号と PR 番号の両方を指す、打ち切り時に完了コメントを出すのか出さないのかが SKILL.md と reference で食い違う、`should` の「原則直す」に例外条件が無い。もうひとつは**このスキルが `pr-review` の記録を壊す経路** — 同一 PR へ最大3周レビューをかけると `docs/review_log.md` の「1 PR につき1回」という数え方が崩れ、レビュー成果物をコミットするかどうかも定義されていない。後者は、レビューをルールへ育てる経路そのものを侵食するため重い。

## blocker

なし。

## should

### `writing/notation-inconsistency` — `<N>` が Issue 番号と PR 番号の両方を指し、参照先のファイル名が確定しない

- **場所**: `.claude/skills/issue-workflow/SKILL.md`:21, :34, :37-39, :46（Issue 番号）／:68, :106、`references/pr-body.md`:27、`references/pending-comment.md`:19（PR 番号）
- **指摘した観点**: 日本語ドキュメント
- **累計**: 2回目
- **何が起きているか**: 同一文書内で `<N>` が二つの異なる番号を指す。PR 番号には `<PR番号>` という別表記も併用されている（:59, :91, :103, :111）。既存の `pr-review/SKILL.md` では `<N>` は一貫して PR 番号であり、そちらとも食い違う。
- **なぜ問題か**: `docs/reviews/pr-<N>.md` は PR 番号で命名される。手順 0〜1 で `<N>` = Issue 番号と定義された直後に同じ記号が現れるため、Issue 番号でパスを組み立てうる。
- **失敗シナリオ**: この PR 自身が実例になる。Issue #16 → PR #17。`<N>` を 16 と解釈すると `docs/reviews/pr-16.md` を読もうとして存在せず、PR 本文にも誤ったリンクが残る。
- **提案**: Issue 番号を `<Issue番号>`、PR 番号を `<PR番号>` に統一し、`<N>` を廃止する。あわせて数字と助数詞の空白（frontmatter「最大3周」／本文「3 周」）も、既存 `pr-review/SKILL.md` の表記（空白なし）に寄せる。

### `writing/contradictory-instruction` — 打ち切り時に完了コメントを出すのか出さないのかが、SKILL.md と `pending-comment.md` で食い違う

- **場所**: `.claude/skills/issue-workflow/SKILL.md`:81-97（手順5）, :109（手順7）／`references/pending-comment.md`:28
- **指摘した観点**: 日本語ドキュメント
- **累計**: 1回目（新規）
- **何が起きているか**: SKILL.md の手順は 5 →（6）→ 7 と一本道で、手順 7 に条件が書かれていない。一方 `pending-comment.md` は「残指摘があるまま完了コメントを出さない」と明確に禁じている。
- **なぜ問題か**: このスキルの唯一の終端出力が「PR にどのコメントが残るか」であり、そこが二通りに読める。
- **失敗シナリオ**: 3周しても blocker が1件残った PR で、残指摘コメントの直後に「実装が完了した。」が投稿される。通知だけを見た人は完了扱いでマージ判断に入る。
- **提案**: 手順 7 に条件を書く。「残指摘ゼロで抜けた場合のみ完了を報告する。打ち切りで抜けた場合は手順5の残指摘コメントが最終報告になる」。

### `writing/ambiguous-criterion` — Draft を外す基準と `should` の扱いが、どちらも分岐条件として機能しない

- **場所**: `.claude/skills/issue-workflow/SKILL.md`:56（「実装が一通り動く状態」）, :73（「`should` — 原則直す」）, :85（抜け条件）
- **指摘した観点**: 日本語ドキュメント
- **累計**: 2回目
- **何が起きているか**: (a) Ready 化は「レビューの開始線」という重い意味を与えられているのに、基準は「一通り動く」だけで、手順0の完了条件との関係が書かれていない。ドキュメントと Skill だけの差分では「動く」が何を指すかも決まらない。(b) `blocker` は「必ず直す」、`consider` は「判断する。直さないなら理由を残す」と条件があるのに、`should` だけ「原則」という留保が付き、例外がどういうときかは書かれていない。
- **なぜ問題か**: (b) は手順5の抜け条件（`should` ゼロ）と噛み合わない。「理由を残せば見送ってよい」と読むと、見送りを自己判定して「実質ゼロ」とみなし、打ち切り扱いを経ずに完了コメントへ進める。上の `writing/contradictory-instruction` と重なると、未収束の PR が完了報告付きで出る。
- **失敗シナリオ**: (a) `SKILL.md` は書いたが `references/*.md` が未着手の状態を「一通り動く」と判断して Ready 化 → レビューが「参照先が存在しない」で埋まり1周を空費する。(b) 2周目で `should` が2件残る → 「両方見送り、理由は PR 本文に記載」と判断 → 抜け条件を満たしたとみなして完了コメントを投稿。
- **提案**: (a) 「Issue の完了条件をすべて満たし、追加・変更したファイルがすべて差分に入っていること」と、実行可能物が無い差分にも当てはまる形で書く。(b) 「直す。直さないと決めたなら理由を残し、その PR は打ち切りとして扱う」と、`consider` と同じ粒度にする。

### `writing/term-inconsistency` — 「設計正本」という新しい呼び名が入り、外延が既存語と重ならない

- **場所**: `.claude/skills/issue-workflow/SKILL.md`:12, :54／`references/pending-comment.md`:22（こちらは「設計契約」）
- **指摘した観点**: 日本語ドキュメント
- **累計**: 2回目
- **何が起きているか**: 「設計正本」はリポジトリの既存文書に出てこない語。既存では「正本」が値・記述の出どころを指し、「設計契約」は DR-0009 の5層を指す（DR-0021 が「設計契約」に Agent Skill を含まないと明示している）。3語目を足したうえ、同じ PR 内の `pending-comment.md` では「設計契約」を使っており、1スキル内でも揺れている。
- **なぜ問題か**: 「設計正本 = DR-0009 の5層」なのか「`design/` 配下すべて（`rules.json` を含む）」なのかが確定しない。DR-0021 がわざわざ設けた区別が持ち込まれない。
- **失敗シナリオ**: 該当なし（設計上の問題）。ただし「設計正本に従う」を根拠に、`design/rules.json` の閾値を Skill 側へ写す判断を正当化しうる。
- **提案**: 記述の出どころを指すなら「正本（`DESIGN.md` / `design/`）」、DR-0009 の5層を指すなら「設計契約」に統一し、`pending-comment.md` と揃える。

### `code/review-loop-double-counts-log` — 同一 PR に対する3周ループが、`review_log.md` の「1 PR につき1回」という数え方を壊す

- **場所**: `.claude/skills/issue-workflow/SKILL.md`:64-97（手順3〜5）／突き合わせ先 `.claude/skills/pr-review/references/review-log-format.md`「数え方」節・「更新の手順」2
- **指摘した観点**: コード品質
- **累計**: 1回目（新規）
- **何が起きているか**: 同じ PR 番号で `pr-review` を最大3回呼ぶ。`pr-review` は毎回 `docs/review_log.md` の該当カテゴリを +1 し、レビュー履歴に行を足す。issue-workflow 側に「2周目以降はカウントしない」という指示は無く、`pr-review` 側にも再実行の検知は無い。
- **なぜ問題か**: `review-log-format.md` は「1つのカテゴリは、1つの PR につき1回だけ数える」と明記し、理由を「1回のレビューで閾値に到達してしまい、ルール化の判断材料にならない」と書いている。3周ループはまさにその状態を作る。このログは指摘を `design/rules.json` へ昇格させる唯一の経路なので、カウントが膨らむと判断材料そのものが嘘になる。
- **失敗シナリオ**: 1周目で `writing/ambiguous-criterion` が出る → 修正 → 2周目・3周目も同カテゴリが再指摘。この1 PR だけで累計が 1 → 3 になり、ルール化候補の表へ昇格する。レビュー履歴にも同じ PR の行が3本並ぶ。
- **提案**: 手順3に「2周目以降は再レビューであり、`docs/review_log.md` は更新しない（`docs/reviews/pr-<PR番号>.md` のみ更新する）」と明示する。

### `code/review-artifact-handling-undefined` — `pr-review` が書き出す2ファイルをコミットするかどうかが、手順のどこにも無い

- **場所**: `.claude/skills/issue-workflow/SKILL.md`:64-79（手順3・4）、`references/pr-body.md`:27、`references/pending-comment.md`:19
- **指摘した観点**: コード品質
- **累計**: 1回目（新規）
- **何が起きているか**: `pr-review` は `docs/reviews/pr-<N>.md` と `docs/review_log.md` を作業ツリーへ書く（`.gitignore` に該当パターンは無く追跡対象になる）。issue-workflow は「直したらコミットして push する」としか書いておらず、この2ファイルを PR に含めるのかが定義されていない。
- **なぜ問題か**: どちらに転んでも壊れる。含めれば次周のレビュー対象（`gh pr diff`）に前周のレビュー文書が入り、レビュアーがレビュー結果自体をレビューする。含めなければ、PR 本文と残指摘コメントが指す「レビュー全文」がリポジトリに残らず、未コミットの変更が次の Issue の手順1（`git checkout main && git pull`）へ持ち越される。
- **失敗シナリオ**: 1周目のレビュー後に `git add -A` でコミット → 2周目で日本語ドキュメント観点のレビュアーが `pr-17.md` の文面へ指摘を出す → レビュー文書を直すという、際限なく伸びる作業が発生する。
- **提案**: 手順4で「レビュー成果物はレビュー専用の別コミットにする」と決め、手順3で「差分のうち `docs/reviews/` と `docs/review_log.md` はレビュー対象外」と明示する。

### `decisions/undocumented-decision` — 開発運用の方針が SKILL.md に確定したが、対応する DR が無い

- **場所**: `.claude/skills/issue-workflow/SKILL.md`:30（ブランチ命名）, :48（Draft 先行）, :85（3周上限）, :118（マージしない）／`docs/decisions/` に対応 DR 無し
- **指摘した観点**: 決定記録
- **累計**: 2回目
- **何が起きているか**: Issue #16 が未決事項として挙げた4点（ブランチ命名規則 / Draft か Ready か / 打ち切り時の扱い / 実装方針をどこまで指示するか）を、この PR はすべて SKILL.md 内で確定させた。理由は本文に散在するが、`docs/decisions/` に DR は無く、索引にも追記が無い。
- **なぜ問題か**: `pr-review/agents/decisions.md` が判断とみなす目安の「命名規約」「閾値の設定」に、ブランチ命名と3周上限がそのまま該当する。いずれも他の選択肢が実在した（Issue #16 自身が `feature/<issue番号>-<slug>` を例示し、Draft か Ready かを二択で提示している）。`docs/reviews/pr-13.md` は既に同じ問題を指摘し、DR-0025「意思決定 → Issue → ブランチの順序と粒度」を提案したまま保留になっている。この PR はその保留領域に具体的な運用規則を書き込むもので、既存の欠落を悪化させる方向に働く。
- **失敗シナリオ**: 後日「ブランチをハイフン区切りに変えたい」「レビューは2周でよいのでは」という提案が出たとき、根拠は SKILL.md の断片的な地の文しかなく、Issue #16 で一度済んだ議論をやり直すことになる。索引にも無いので、そもそも決定として探されない。
- **提案**: DR-0025「Issue 起点の開発フローと、その各段階で残す痕跡」を起こし、4決定を「文脈 / 決定 / 理由 / 検討した他の選択肢と却下理由 / 帰結」で記録して索引へ追記する。pr-13 で保留された論点も同じ DR に含められる。

### `decisions/dr-recording-bypass` — SKILL.md が「DR に残すか、少なくとも PR 本文へ書く」を許容し、DR 記録の抜け道を公認している

- **場所**: `.claude/skills/issue-workflow/SKILL.md`:54
- **指摘した観点**: 決定記録、日本語ドキュメント（2観点から独立に挙がった）
- **累計**: 1回目（新規）
- **何が起きているか**: 実装フェーズの指示が、PR 本文への記載を DR の代替として明示的に認めている。二つの記録先が「か」で並び、どちらを選ぶかの基準も無い。
- **なぜ問題か**: `pr-review/agents/decisions.md` は「PR の説明に判断の理由が書かれている場合、それは DR に昇格すべき候補として扱う。PR の説明はマージ後に読まれないが、DR は残るからだ」と述べ、PR 本文への記載を到達点ではなく**指摘対象**としている。既知カテゴリ `decisions/undocumented-decision` の定義そのものが、この行が許容する状態を指す。新しい Skill が、同じリポジトリの `pr-review` が指摘する行為を正規の選択肢として指示している。このスキルは今後すべての Issue 実装の入口になるため、影響は1回の PR に留まらない。
- **失敗シナリオ**: 次の Issue で実装者が本行に従い、ライブラリ選定の理由を PR 本文にだけ書く。同じ PR を `pr-review` にかけると `decisions/undocumented-decision` が指摘され、直しても Skill の指示に戻るたび再発する。実体のない再発で `review_log.md` の累計だけが閾値へ近づく。
- **提案**: 分岐条件を書く。「後から別の実装が同じ判断を必要とするなら DR を起こす。この PR に閉じた選択なら PR 本文の『なぜこうしたか』に書く」。PR 本文を終着点ではなく DR 化待ちの一時置き場と位置づける。

## consider

### `contract/workflow-constant-duplicated` — `pr-review` が持つ取り決めが issue-workflow 側へ書き写されている

- **場所**: `.claude/skills/issue-workflow/SKILL.md`:3, :68, :85, :87／`references/pr-body.md`:27／`references/pending-comment.md`:3, 6, 11, 19, 26
- **指摘した観点**: 設計契約
- **累計**: 1回目（新規）
- **何が起きているか**: SKILL.md:66 は「記録先もあちらが持っている」と宣言しているのに、直後の :68 と2つの reference で記録先パス `docs/reviews/pr-<N>.md` を自前に書いている。ループ上限「3周」も SKILL.md と `pending-comment.md` に計7箇所ハードコードされている。
- **なぜ問題か**: 複製されたのは設計データではないが、構造は同じ「同じ情報が二箇所にあり、片方だけ更新される」。とくに `pending-comment.md` は「3周したが」という文面自体に数字が焼き込まれているため、上限を変えるとコメント文面が実態と食い違う。
- **失敗シナリオ**: `pr-review` の記録先を変更 → issue-workflow が生成した PR 本文・打ち切りコメントが存在しないパスを指す。上限を2周に変えた場合も、コメント文面だけ「3周した」と残る。
- **提案**: パスは SKILL.md の1箇所に集約し、テンプレートはプレースホルダにする。「3周」は issue-workflow が決めた値なので SKILL.md §5 を正本とし、`pending-comment.md` 側は数字を埋めない。文面としての具体性は読みやすさに効くので、直さない判断もあり得る。

### `phase/out-of-scope-addition` — 開発プロセス用の Skill が、README の正本地図にも DR にも接続しないまま3つ目として増えている

- **場所**: `.claude/skills/issue-workflow/`（4ファイル）
- **指摘した観点**: フェーズスコープ
- **累計**: 2回目
- **何が起きているか**: README の「正本の在り処」表に載る Skill は `skills/slide-harness/`（DR-0013 が根拠）だけで、`.claude/skills/` 配下の開発プロセス用 Skill は表にも24本の DR にも現れない。本 PR はその系列に1つ足すが、DR も README の追記も伴わない。
- **なぜ問題か**: 開発プロセスの Skill だけが正本地図の外で増え続けると、「このリポジトリで何が正本なのか」が README から読み取れなくなる。
- **失敗シナリオ**: 該当なし（設計上の問題）。後から入る人が README の正本表だけを読むと、`.claude/skills/` 配下のスキルの存在と役割分担に気づけない。
- **提案**: 「開発プロセスの Skill は `.claude/skills/` に置き、設計正本とは別系統として扱う」旨を DR で明示し、README にも一行足す。`pr-review`（PR #15）の時点で既に同じ状態なので、本 PR 単独の責任として差し戻す必要はない。

### `code/undefined-command-input` — 手順6の `gh pr edit --body-file <path>` の `<path>` をどこに用意するかが未定義

- **場所**: `.claude/skills/issue-workflow/SKILL.md`:100-107
- **指摘した観点**: コード品質
- **累計**: 1回目（新規）
- **何が起きているか**: 本文ファイルを作れという指示も、置き場所の指定も無い。他の手順のコマンド片はすべてそのまま実行できる形になっているので、ここだけ入力が欠けている。
- **なぜ問題か**: 置き場所が決まっていないため、リポジトリ直下に一時ファイルが作られ、そのまま `git add` されて PR に混入する経路が開いている。
- **失敗シナリオ**: 手順6でリポジトリ直下に本文ファイルを書く → 削除の指示が無い → 直後のコミットで一時ファイルが差分に入り、マージされて残る。
- **提案**: ヒアドキュメントで直接渡す形にするか、`--body-file` を使うならリポジトリ外の一時パスを明示し、使用後に削除する旨を書く。

### `decisions/consequence-not-followed` — push 手順が DR-0023 の「人が開いて確認する」帰結に触れていない

- **場所**: `.claude/skills/issue-workflow/SKILL.md`:52-56／`docs/decisions/0023-public-repo-with-audit.md`:41-43
- **指摘した観点**: 決定記録
- **累計**: 1回目（新規）
- **何が起きているか**: このスキルは Issue 実装の push を常に通る経路になるが、push 前の確認について何も述べていない。DR-0023 の帰結には「audit は文字列検査であり公開承認ではない。差分・ログ・画像は人が開いて確認する」とある。
- **なぜ問題か**: Run 成果物を伴う Issue をこの Skill で処理した場合、絶対パスや OS ユーザー名を含む差分がそのまま push される流れになる。`scripts/sanitize-run-artifacts.mjs` / `scripts/audit-public-data.mjs` は未実装のため、現時点で実害は生じない。
- **失敗シナリオ**: sanitize + audit 実装後、Run を追加する Issue をこの Skill で処理 → audit が実行されず `/home/<user>/...` を含む `events.jsonl` が public へ push される。
- **提案**: sanitize / audit が実装された時点で、手順2の push に一行足して DR-0023 へリンクする。あるいはこの Skill の適用範囲を「Run 成果物を含まない Issue」に限ると明記する。

## 落とした指摘

なし。全10件が成立した。

統合したもの: 日本語ドキュメント観点の consider「決定記録に残すか PR 本文へ書くかの選択基準が無い」は、決定記録観点の `decisions/dr-recording-bypass` と同一箇所（SKILL.md:54）・同一原因のため1件にまとめ、2観点から挙がったものとして記録した。同じく日本語ドキュメント観点の「Draft を外す基準」と「`should` の原則直す」は、いずれも `writing/ambiguous-criterion` に該当するため1件にまとめた（カテゴリの累計は1 PR につき1回）。

## ルール化候補

なし。累計3回に達したカテゴリは無い（最大は2回）。

---

## 2周目（2026-09-06）

- **対象**: `01bf6ad..cedb15a`（1周目の修正差分と DR-0025）
- **観点**: 日本語ドキュメント / 決定記録 / コード品質の3つに絞った。設計契約・フェーズは1周目の consider が DR-0025 で閉じ、検査は1周目が指摘ゼロで守備範囲に触れていないため省いた

should 6件、consider 3件。1周目の should 6件はすべて解消を確認したが、**修正が新しい穴を作った**ものが3件ある。とくに重いのは、1周目で入れた「再レビューでは指摘ログを触らせない」という指示に、呼ばれる側（`pr-review`）の受け口が無かったこと。呼ぶ側の願望だけが増え、`pr-review` を手順どおり実行すれば1周目と同じ結果になる状態だった。

### should

#### `code/review-loop-double-counts-log`（再掲・未解消だった） — 再レビューの指示に `pr-review` 側の受け口が無い

- **場所**: `.claude/skills/issue-workflow/SKILL.md` 手順3／`.claude/skills/pr-review/SKILL.md` 手順1・2・4・6
- **指摘した観点**: コード品質
- **何が起きているか**: issue-workflow は (a) 指摘ログを触らせない (b) 対象を増分に絞る (c) 観点を絞る (d) レビュー成果物を対象外にする、の4つを `pr-review` へ要求していた。しかし `pr-review` 側は対象を `gh pr diff <N>` に固定し、「1回の応答の中で6つすべてを spawn する」と全観点を必須にし、手順6で無条件に +1 する。再レビューかどうかを受け取る引数も分岐も存在しなかった。
- **対応**: `pr-review` 側に受け口を作った。手順1に再レビューの分岐、手順2に増分差分への差し替えと `docs/reviews/` の除外、手順4に「初回は6つすべて／再レビューは絞ってよい（ただし実装コードを含むなら絞らない）」、手順6に計上の条件を追加した。

#### `code/undefined-command-input` — 「前周の HEAD」を記録する指示が無い

- **場所**: `.claude/skills/issue-workflow/SKILL.md` 手順3
- **指摘した観点**: コード品質
- **何が起きているか**: 2周目の対象を `git diff <前周の HEAD>..HEAD` と定めたが、その SHA を控えろという指示がどこにも無かった。手順4で修正コミットとレビュー成果物コミットが積まれるため、2周目に入る時点で HEAD は動いている。起点を `git log` から推測すると、同じ周の修正コミットが差分から丸ごと落ちる経路があった。
- **対応**: 手順3の冒頭に「呼ぶ直前に `git rev-parse HEAD` を控える」を追加した。

#### `writing/contradictory-instruction` — 3周目で収束した PR が「収束」と「打ち切り」の両方に当てはまる

- **場所**: `.claude/skills/issue-workflow/SKILL.md` 手順5・手順7
- **指摘した観点**: 日本語ドキュメント
- **何が起きているか**: 抜け条件が「収束 — 未対応がゼロ」「打ち切り — 3周を終えた、または `should` を見送る」の二つで、排他になっていなかった。3周目で指摘ゼロになった場合は両方に当てはまり、完了コメントを出すか残指摘コメントを出すかが決まらない。1周目に同じカテゴリで直した箇所の、境界のケースが未定義のまま残っていた。
- **対応**: 「先に収束を判定する。未対応がゼロなら何周目であっても収束」と順序を固定し、打ち切りの条件を「3周を終えても**なお残っている**」に変えた。

#### `decisions/dr-restates-canonical-value` — DR-0025 が「値を再掲しない」と宣言しながら数字を4箇所に書いている

- **場所**: `docs/decisions/0025-issue-driven-development-flow.md`
- **指摘した観点**: 決定記録、日本語ドキュメント（2観点から独立に挙がった）
- **何が起きているか**: 帰結が「周回数の正本は `SKILL.md`。この DR は値を再掲しない」と書く一方、決定・理由・却下理由・帰結に「3 周」が入っていた。既存の値持ち DR（0007 / 0008 / 0012）はヘッダに `**正本**:` 行を置いて値を書かない形式なのに、DR-0025 にはその行が無かった。同じコミットで `SKILL.md` の description と `pending-comment.md` から数字を抜いておきながら、DR 側に写しを4つ増やしていた。
- **対応**: ヘッダに `**正本**:` 行を追加し、本文から数字を落とした（「上限を設ける」という決定として書き直した）。帰結も「上限を設けること」と「上限の値そのもの」を分け、値を変えるだけなら DR は置き換えないと明示した。

#### `decisions/undocumented-decision` — 1周目の修正で足した運用判断が DR-0025 に入っていない

- **場所**: `docs/decisions/0025-issue-driven-development-flow.md`／`.claude/skills/issue-workflow/SKILL.md` 手順3・4・5
- **指摘した観点**: 決定記録
- **何が起きているか**: DR-0025 の決定は抜け条件を二つとしていたが、`SKILL.md` には三つ目（`should` を見送ると判断した）が足されていた。再レビューの運用（ログの数え方、対象と観点の絞り込み）も DR に無い。DR の決定文が Skill の実際の手順と食い違っている状態そのものが、DR を「昔こう考えていた」の化石にする経路になる。
- **対応**: DR-0025 の決定に抜け条件の三つ目と再レビューの扱いを追加し、却下した選択肢に「毎周すべての観点・全差分を見る」を足した。帰結の列挙も揃えた。

#### `writing/ambiguous-criterion` — 絞り込みの例外の主語が落ちている

- **場所**: `.claude/skills/issue-workflow/SKILL.md` 手順3
- **指摘した観点**: 日本語ドキュメント
- **何が起きているか**: 「2周目以降は対象と観点の両方を絞る」の後に「ただし実装コードを含む差分では絞らない」と書いたが、この「絞らない」が観点だけを指すのか両方を指すのかが書かれていなかった。両方と読むと、その節が回避しようとしたフルスキャンに戻り、目的と例外が節の中で衝突する。
- **対応**: 「実装コードを含む差分では、**観点は**絞らない。対象を増分に絞るのは続けてよい」と主語を明示した。

### consider

#### `code/rereview-overwrites-prior-review` — 2周目のレビュー全文が1周目を上書きする

- **場所**: `.claude/skills/pr-review/references/review-file-format.md`
- **指摘した観点**: コード品質
- **何が起きているか**: `pr-review` は PR ごとに1ファイルを書く書式しか持たず、追記か上書きかの指定がどちらの側にも無かった。上書きされると、PR 本文が指す「レビュー全文」から最初の blocker が存在した事実が消える。
- **対応**: `review-file-format.md` に「再レビューでは `## N周目（YYYY-MM-DD）` を追記し、既存の節を消さない」を追加した。このファイル自身がその形式の最初の実例になっている。

#### `decisions/index-section-mismatch` — DR-0025 の索引登録が「メタ」節にある

- **場所**: `docs/decisions/README.md`
- **指摘した観点**: 決定記録
- **何が起きているか**: 「メタ」の既存住人は DR-0024（決定記録そのものの形式）だけで、リポジトリの回し方を決める DR は「実験と運用」に並んでいる。DR-0025 の帰結は「`.claude/skills/` 配下の Skill はこの DR の系統として扱う」と索引経由でしか辿れない扱いを定めているので、入口が想定と違う節にあると辿られにくい。
- **対応**: 「実験と運用」節へ移した。

#### `writing/notation-inconsistency` — `pending-comment.md` にだけ数字と助数詞の空白が残っていた

- **場所**: `.claude/skills/issue-workflow/references/pending-comment.md`
- **指摘した観点**: 日本語ドキュメント
- **対応**: `<n>周` に揃えた。なお `docs/decisions/` は既存 DR も空白ありで統一されているため、DR 側は変更していない。

### 落とした指摘

なし。9件すべてが成立した。

`decisions/consequence-not-followed`（1周目 consider、DR-0023 の sanitize / audit）は今回も未対応だが、`scripts/sanitize-run-artifacts.mjs` が未実装で実行できない指示になるため見送りを継続する。ただし DR-0025 の帰結に「対応するスクリプトが実装された時点で手順へ組み込む」と書き、判断を記録に残した。

### 2周目の指摘ログの扱い

1周目で計上済みのカテゴリ（`code/review-loop-double-counts-log`、`code/undefined-command-input`、`writing/contradictory-instruction`、`writing/ambiguous-criterion`、`decisions/undocumented-decision`、`writing/notation-inconsistency`）は、再び指摘されたが**加算しない**。同じ PR で同じカテゴリが浮上した回数は1回だからだ。

2周目で初めて出た2カテゴリ（`decisions/dr-restates-canonical-value`、`code/rereview-overwrites-prior-review`）と、1周目には無かった `decisions/index-section-mismatch` のみ、新規行として足す。

---

## 3周目（2026-09-06）

- **対象**: `cedb15a..d91b3e2`（2周目の修正と、`pr-review` への再レビュー受け口の追加）
- **観点**: 日本語ドキュメント / 決定記録 / コード品質 / 設計契約の4つ。2周目の修正が2つのスキルにまたがる取り決めを動かしたため、設計契約を戻した
- **レビュアーのモデル**: 4観点すべて Sonnet（1・2周目は Opus）。観点ごとの難度を測るため意図的に切り替えた

should 6件、consider 3件。2周目の指摘はすべて解消を確認したが、今回も**修正が新しい穴を作っている**。パターンは3周を通じて一貫していて、「ある文書に規則を足したが、その規則の正本である参照ファイルを直していない」ことによる食い違いが繰り返し出る。

### should

#### `code/review-loop-double-counts-log`（3回目・部分未解消） — 「まだ数えていない」の判定手続きが無い

- **場所**: `.claude/skills/pr-review/SKILL.md` 手順6
- **指摘した観点**: コード品質
- **何が起きているか**: 2周目で「その PR でまだ数えていないカテゴリだけ足す」という結果を指示したが、それをどう判定するかの手続きが無かった。呼ぶ側から渡るのは「前周のコミット」と「前周で指摘が出た観点」だけで、計上済みカテゴリの一覧は渡らない。判定できるのは会話の記憶だけという状態だった。
- **なぜ問題か**: 長い並列レビューを挟むと文脈は圧縮され、記憶は残らない。その状態で手順6に入ると、一般規則（既存カテゴリなら +1）に素直に従って再加算し、2周目に閉じたはずの二重計上が別経路から再発する。
- **対応**: 判定材料を `docs/review_log.md` の「最終指摘」列と明示した。既に今回の PR 番号になっていれば計上済み。記憶で判定するなと明記した。

#### `code/review-history-row-update-undefined` — レビュー履歴の件数の意味が、実際に書いた値と食い違う

- **場所**: `.claude/skills/pr-review/SKILL.md` 手順6／`references/review-log-format.md`「更新の手順」
- **指摘した観点**: コード品質、日本語ドキュメント（2観点から独立に挙がった）
- **何が起きているか**: 2周目で「レビュー履歴の行はその PR につき1行だけ持つ（件数は最新の周のものへ更新する）」と書いたが、実際に `review_log.md` へ書いた値は1周目と2周目の**合算**（should 12 / consider 7）だった。指示と実物が食い違っている。さらに、その書式の正本である `review-log-format.md`「更新の手順」4は「今回の行を足す」のまま変更しておらず、再レビューの例外に触れていない。
- **なぜ問題か**: 「最新の周の値で置き換える」を文字どおり実行すると、収束した周（指摘ゼロ）で行が 0/0/0 に書き換わり、`review-log-format.md` 自身が禁じている「累計を減らす」操作が起きる。
- **対応**: 手順6を「全周を合算した値へ更新する（置き換えない）」に直し、`review-log-format.md` の更新手順にも同一 PR の行を更新する分岐を足した。

#### `writing/ambiguous-criterion` — 追記節の見出しが「2周目」に固定されている

- **場所**: `.claude/skills/pr-review/references/review-file-format.md`
- **指摘した観点**: 日本語ドキュメント
- **何が起きているか**: 2周目で「再レビューでは `## 2周目（YYYY-MM-DD）` を追記する」と書いたが、対象は「2周目以降」なのに見出しの数字が固定文字列のままだった。この3周目の追記が、既存の「## 2周目」と衝突する。
- **対応**: `## <N>周目（YYYY-MM-DD）` に変え、`<N>` は通し番号だと添えた。この節がその最初の実例になっている。

#### `contract/workflow-constant-duplicated`（2回目・範囲が拡大） — 再レビューの判断ロジックが2つのスキルに書き写された

- **場所**: `.claude/skills/issue-workflow/SKILL.md` 手順3／`.claude/skills/pr-review/SKILL.md` 手順1・2・4・6
- **指摘した観点**: 設計契約
- **何が起きているか**: 2周目で `pr-review` に受け口を作った際、`issue-workflow` 側の対応する段落を消さなかった。結果、対象の絞り方・観点の絞り方とその例外・ログの計上条件が、ほぼ同じ理由付けの文章で両方に存在する状態になった。1周目の consider（パスと数値の複製）と同じ構造が、ロジック全体へ広がっている。
- **なぜ問題か**: `issue-workflow` 手順3の冒頭は「レビューの観点も記録先も、あちらが持っている」と宣言している。その直後で正本の判断ロジックを全文書き写しており、宣言と実体が食い違う。片方だけ変更されても検出できない。
- **対応**: `issue-workflow` 手順3から条件分岐を削り、「何を渡すか」という呼ぶ側の責務だけを残して `pr-review` の該当手順へのポインタに置き換えた。

#### `decisions/undocumented-decision`（3回目） — レビュー履歴を1行に集約する判断が DR に無い

- **場所**: `docs/decisions/0025-issue-driven-development-flow.md`
- **指摘した観点**: 決定記録
- **何が起きているか**: 「レビュー履歴の行は PR につき1行」という判断は、`review-log-format.md` の既存記述（毎回足す）と対立する選択だったが、DR-0025 に記録されていなかった。
- **対応**: DR-0025 の決定へ一文足した。あわせて、この段落自体が条件分岐を書きすぎていたため（設計契約観点の指摘と同根）、正本を `pr-review/SKILL.md` と名指しして要約に縮めた。

#### `decisions/undocumented-decision` — DR-0025 の「次の4点」が実際は5つ

- **場所**: `docs/decisions/0025-issue-driven-development-flow.md`
- **指摘した観点**: 決定記録
- **何が起きているか**: 2周目で「再レビューは初回と別扱いにする」を決定として足したのに、冒頭の点数を4のまま放置していた。決定の数が数えられないと、それが正式な決定なのか本文の補足なのか読み手が迷う。
- **対応**: 5点に直した。

### consider

#### `contract/source-of-truth-ambiguous` — 再レビューのロジックの正本が DR・2つのスキルの3箇所に散っている

- **指摘した観点**: 設計契約
- **対応**: DR-0025 のヘッダの `**正本**:` 行に「再レビュー時の絞り込み条件と計上判定は `pr-review/SKILL.md`」を追加し、DR の決定節からは条件分岐を落とした。3箇所のうち DR と `issue-workflow` を参照側に寄せ、正本を `pr-review` 1つにした。

#### `writing/notation-inconsistency` — 「前周のコミット」と「前周の HEAD」が同一手順内に混在

- **指摘した観点**: 日本語ドキュメント
- **対応**: 「前周のコミット」に統一した。

#### `writing/term-inconsistency` — 「呼び出し側」と「呼ぶ側」の混在

- **指摘した観点**: 日本語ドキュメント
- **対応**: 「呼ぶ側」に統一した。

### 統合時に自分で見つけたもの

レビュアーからの指摘ではないが、3周目の修正を入れる過程で**「収束」の定義が甘い**ことに気づいた。手順5は「未対応の `blocker` と `should` がゼロなら収束」と書いていたが、これだと最終周の指摘を直した直後に自分で収束を宣言できる。その修正は誰にも見られていないのにだ。実際この PR がその状態にある。

定義を「**指摘ゼロのレビューを1周通ったこと**」に締めた。DR-0025 の決定文も揃えた。この定義では、この PR は収束していない。

### 落とした指摘

なし。9件すべてが成立した。

### レビュアーのモデルについて

4観点すべて Sonnet で回した結果、1・2周目（Opus）と比べて質の低下は見られなかった。とくにコード品質は、こちらが仕掛けた「判定手続きが存在するか」という問いに答えたうえで、**指示（最新の周の値へ更新）と実物（合算値 12/7）の食い違い**という、指示されていない観察まで出している。日本語ドキュメントは、`## 2周目` の固定見出しが「まさに実行中のこの3周目」と衝突することを指摘した。いずれも照合ではなく、手順を頭の中で実行して破綻を探す種類の発見だ。

当初の見立て（照合系は Sonnet、不在の発見は Opus）は、この1回の観測に限れば支持されなかった。ただし差分が小さく（4ファイル・ドキュメントのみ）、過去2周のレビュー全文という強い足場を渡している。実装コードを含む大きな差分でも同じかは、この結果からは言えない。
