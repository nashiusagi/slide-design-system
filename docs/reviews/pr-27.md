# PR #27 レビュー

- **タイトル**: experiments 一式: starter / manifest / 採点 / 比較
- **レビュー日**: 2026-09-09
- **対象**: main...feature/10_experiments-scaffold
- **変更ファイル数**: 34

## 要約

blocker 0件、should 6件、consider 0件。6観点すべてから指摘が出たが、いずれも成立し、同じPR内で修正した。最も重かったのは検査ルールの実効性観点の指摘で、`evaluate-run.mjs` の採点用 ESLint 設定が本番の `noInlineConfig` を継承しておらず、生成AIが typecheck/build を通すために書く `eslint-disable` コメント一つで契約違反が0件に化ける状態だった。採点結果の信頼性そのものに関わるため最優先で直した。

## should

### inspection/rule-has-bypass — 採点用 ESLint 設定に `noInlineConfig` が無く、`eslint-disable` コメントで契約違反が消える

- **場所**: `scripts/evaluate-run.mjs`（`scoreLint` 内の `ESLint` インスタンス化）。比較対象は `eslint.config.js` の `src/**/*.{ts,tsx}` ブロック（`linterOptions: { noInlineConfig: true, ... }`）
- **指摘した観点**: 検査ルールの実効性
- **累計**: 9回目
- **何が起きているか**: `scoreLint` は独自の `overrideConfig` で ESLint インスタンスを組み立てており、本番の `eslint.config.js` が持つ `noInlineConfig: true` を引き継いでいなかった。実際に `/* eslint-disable slide/no-raw-color */` を付けたファイルを `scoreLint` に通すと、違反数が0件になることを確認した。
- **なぜ問題か**: `evaluate-run.mjs` の目的は「AI を起動せず、保存済みの生成結果を契約検査ルールで採点する」こと（DR-0020）。生成AIが typecheck/build を通すために抑止コメントを書くことは実際に起こり得る。その場合、実際には契約違反があるのに `scoring.json` / `compare-runs.mjs` の比較表は「pass」と報告し、baseline / harness のどちらが契約に沿っているかという実験結果の信頼性そのものを損なう。
- **失敗シナリオ**: harness 条件の生成コードに抑止コメントが混ざる → `evaluate-run.mjs score` を実行 → `no-raw-color` 等が計上されない → 比較表がそのセルを `pass` と表示 → 実際には契約違反があるのに harness の優位性の証拠として誤用される。
- **対応**: `scoreLint` の `overrideConfig` に `linterOptions: { noInlineConfig: true, reportUnusedDisableDirectives: 'error' }` を追加。回帰テスト（`scripts/evaluate-run.test.mjs` の「eslint-disable コメントでは違反が消えない」）を追加し、`no-raw-color` が引き続き1件計上されることを固定した。ruleId が無いメッセージのバケット名も `(parse error)` から `(no rule id)` に直した（noInlineConfig 通知は構文エラーではないため）。

### code/measure-failure-swallowed（新規） — `scoreMeasure` が measure-slides.mjs の実失敗を握り潰し、無関係な ENOENT で落ちる

- **場所**: `scripts/evaluate-run.mjs`（`scoreMeasure`）
- **指摘した観点**: コード品質
- **累計**: 1回目（新規カテゴリ）
- **何が起きているか**: `scoreMeasure` は子プロセスの失敗を無条件に `catch {}` で握り潰し、`measurements.json` を読みに行く前提だった。しかし `measure-slides.mjs` はビルド出力が壊れている場合（`.slide-canvas` が無い等）、`measurements.json` を書き出す前に例外で終了する。この場合 `readFileSync(outPath)` が ENOENT を投げ、本来の原因（ビルド出力の何が壊れているか）が失われる。実際に `.slide-canvas` を欠いた `dist/` を渡して再現を確認した。
- **なぜ問題か**: 採点基盤の失敗理由が実際の原因から無関係なメッセージにすり替わり、`stdio: 'pipe'` で握った stderr も表示されないため原因調査ができない。
- **失敗シナリオ**: ビルドが壊れた Run を `evaluate-run.mjs score` にかけると、素の `ENOENT: ... measurements.json` だけが出て、何が壊れているか分からない。
- **対応**: `catch` 節で `outPath` の存在を見て分岐するよう修正。`measurements.json` が無ければ子プロセスの stderr（またはエラーメッセージ）を含めた明示的なエラーを投げるようにした。あわせて `score` サブコマンドにも `save` と同様の try/catch を追加し、素の Node スタックトレースではなく一貫したエラー表示にした。回帰テスト2件（健全なビルドで `measured` を返すケース、`.slide-canvas` を欠くビルドで本来のエラーを伝えるケース）を追加した。

### phase/completion-criterion-not-verified-e2e — Run の実測採点パス（`scoreMeasure` の measured 分岐）が一切検証されないまま完了条件に含まれていた

- **場所**: `scripts/evaluate-run.test.mjs`（`scoreMeasure` / `scoreRun` のテストが `skipped` 分岐のみだった）
- **指摘した観点**: フェーズスコープ
- **累計**: 3回目（ルール化候補）
- **何が起きているか**: Issue #10 の完了条件「保存済み Run に対して採点と比較を何度でも再実行できる」のうち、`scoreLint` は実 ESLint による統合テストがあった一方、`scoreMeasure` は `dist` が無い `skipped` 分岐しかテストされておらず、実際に `measure-slides.mjs` を子プロセス起動して `measurements.json` を読み戻す本来の経路が一度も実行されていなかった。
- **なぜ問題か**: この経路はこの実験装置の主目的（Baseline/Harness の実測比較）が機能するかどうかを左右する中心的なコードパス。単体テストで一度も踏まれないまま完了条件のチェックが付くと、`--dist=`/`--out=` の受け渡しや `measurements.json` のマージが壊れていても `pnpm test` は緑のまま気付けない。
- **失敗シナリオ**: `measure-slides.mjs` 側の呼び出し規約に前提のズレがあった場合、実際に人が `dist` 付きの Run を保存してスコアリングを実行するまで（＝実験を1回走らせるまで）発覚しない。
- **対応**: 静的な `dist/index.html`（`.slide-deck` / `.slide-canvas` を持つ最小構成）を使った統合テストを2件追加。健全なビルドで `status: 'measured'` かつ `pass: true` を返すこと、壊れたビルドで正しくエラーになること（上の `code/measure-failure-swallowed` の回帰テストと兼用）の両方を実 Playwright 実行で固定した。

### contract/design-data-duplicated — `brief.md` が `design/decks/harness-intro.md` のキーメッセージ内容をほぼ逐語的に複製していた

- **場所**: `experiments/harness-intro/brief.md`（5層説明の箇条書き）。正本は `design/decks/harness-intro.md`（本PR差分外）
- **指摘した観点**: 設計契約との整合
- **累計**: 5回目
- **何が起きているか**: 新規追加された `brief.md` の5層説明が、正本デッキ `design/decks/harness-intro.md` の bullets スライドの文言（「色・余白・文字サイズなどの値そのもの」等）とほぼ一字一句一致していた。
- **なぜ問題か**: `brief.md` は「デザイン契約の形を先取りして書かないこと」と自ら明記する、baseline / harness 両条件へ同一に渡る入力。正本デッキの具体的な文言を書き写した結果、baseline が実質的に harness 用デッキの結論に近い文面を先に得ることになり、DR-0014 が前提とする「両条件の差は設計契約の有無だけ」という比較の公平性を弱める。また正本を編集しても `brief.md` は自動的に追従せず、静かに乖離しうる。
- **失敗シナリオ**: 将来 `design/decks/harness-intro.md` の文言を改訂しても `brief.md` が古い文言のまま残り、実験を再実行したときにどちらが「当時の説明」だったか判別できなくなる。
- **対応**: `brief.md` の5層説明を、正本の文言をなぞらない独立した言い回し・構造（箇条書きではなく地の文）へ書き直した。冒頭に「正本の文言をなぞって書かない」旨の注記も追加した。

### writing/term-inconsistency — `brief.md` が新規に「デザイン契約」を使い、標準語である「設計契約」との揺れを再発させていた

- **場所**: `experiments/harness-intro/brief.md:3`
- **指摘した観点**: 日本語ドキュメント
- **累計**: 9回目（既存のルール化候補）
- **何が起きているか**: 新規追加の `brief.md` 冒頭が「デザイン契約」を使っていたが、同じPR内の他ファイル（README.md、prompt.md、prepare-workspace.mjs 等）はすべて「設計契約」で統一しており、リポジトリ全体でも「設計契約」が標準語（30ファイル以上）。
- **なぜ問題か**: `writing.md` 自体がこの用語ペアを既知の揺れとして明示し、`docs/review_log.md` の `writing/term-inconsistency` は8件計上され未着手のまま。新規ファイルで表記がもう一箇所増え、収束すべき用語が逆に広がっていた。
- **対応**: `brief.md:3` の「デザイン契約」を「設計契約」に統一した。

### decisions/context-broader-than-decision（新規） — DR-0039 が「文脈」で名指しした検査対象（ビルド設定）を「決定」「帰結」で無言のうちに除外していた

- **場所**: `docs/decisions/0039-experiment-starter-checked-against-root-scaffold.md`（文脈節はビルド設定も含めて言及していたが、決定・帰結・実装（`MIRRORED_SRC_FILES`）はランタイム機構のみを対象にしていた）
- **指摘した観点**: 決定記録（DR）との整合
- **累計**: 1回目（新規カテゴリ）
- **何が起きているか**: DR-0039 の「文脈」節は starter と一致すべきものとして「ランタイム機構・ビルド設定」の両方を挙げていたが、「決定」「帰結」と実装（`checkStarterMatchesRoot` の `MIRRORED_SRC_FILES`）はランタイム機構のみを対象にしており、ビルド設定（`vite.config.ts` / `tsconfig*.json` / `package.json` の依存バージョン）を対象外にする理由がどこにも書かれていなかった。
- **なぜ問題か**: DR-0039 はまさに DR-0027 が残した「Baseline/Harness が異なるビルド条件で走る」という懸念を解消するための DR。しかし追加された検査はその「ビルド条件」の主要な構成要素をカバーしておらず、DR-0039 自身が「一致を検査せず、ずれを許容する」を却下した理由（「starter が古いままでも `pnpm check` は緑のままになり、誰も気付けない」）が、ビルド設定に関しては現状そのまま当てはまる。
- **失敗シナリオ**: 将来ルートの `vite` / `typescript` のメジャーバージョンを上げても `src/runtime/**` を変更しなければ `experiment:starter:check` は緑のまま。starter 側のビルド条件が古いまま実験が走っても誰も気付けない。
- **対応**: 「文脈」節からビルド設定への言及を外してランタイム機構のみに絞り、「決定」節と一致させた。あわせて「帰結」節に、ビルド設定はこの検査の対象外であることと、その理由（starter は workspace 前提でないルート `package.json` をそのまま一致対象にできない）、以後の追随は人の運用に委ねる旨を明記した。

## 落とした指摘

なし。6件すべて検証の上で成立を確認し、修正した。

## ルール化候補

- `inspection/rule-has-bypass` — 通算9回。「検査ルールの再実装（採点用・レビュー用など）が本番設定の `noInlineConfig` 等の防御を引き継いでいるか」を確認する仕組みが要る。汎用の静的ルールにはしにくいが、`scripts/validate-design.mjs` に「`scoreLint` の linterOptions が `eslint.config.js` の対応ブロックと同じ抑止耐性を持つか」を突き合わせる検査を足す余地がある
- `writing/term-inconsistency` — 通算9回。「設計契約」表記への統一は、grep ベースの簡易チェック（`pnpm design:check` に「デザイン契約」という文字列が新規ファイルに出現したら警告する等）で機械化できる可能性がある
- `phase/completion-criterion-not-verified-e2e` — 通算3回。完了条件に「保存済みRunへの再スコアリング」のような統合パスがあるとき、少なくとも1つの happy path テストが実サブプロセス/実ブラウザを通すことを、レビューのチェックリストとして明文化する余地がある
