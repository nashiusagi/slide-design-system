# PR #58 レビュー

- **タイトル**: 検査を新設する PR が、レビューの上限周回に構造的に当たる
- **レビュー日**: 2026-09-22
- **対象**: main...feature/54_third-exit-for-review-loop
- **変更ファイル数**: 7

## 要約

blocker 1件、should 11件、consider 5件。blocker は DR-0052 の決定4（上限に達する前でもレビューを実行できなければ確認未了で抜ける）が、同じ DR の決定1・決定2（どちらも上限到達を条件にしている）と矛盾し、正本と宣言した `SKILL.md` にも反映されていない点。**5観点が独立に同じ場所を指した。** 字義どおり読むと、1周目のレビューが落ちただけでループを抜けられ、上限を設けた意味（DR-0025）が消える。

should は3系統に分かれる。(1) 抜け方の判定が上限到達を前提にしているかどうかが文書間で揺れる、(2) 判定の入力（未対応がゼロか）を第三者が再現できる形で残していない、(3) `pr-review` の出力ファイルへの書き込みを、その書式を持たない側の Skill が指示している。

## blocker

### `decisions/inconsistent-partition` — 決定4 が決定1・2 と矛盾し、正本の `SKILL.md` はどちらも実装していない

- **場所**: `docs/decisions/0052-review-loop-third-exit.md`（決定1・決定2・決定4）、`.claude/skills/issue-workflow/SKILL.md`（手順5）、`.claude/skills/issue-workflow/references/unconfirmed-comment.md`（冒頭の適用条件）
- **指摘した観点**: 決定記録との整合 / 設計契約との整合 / 検査ルールの実効性 / 日本語ドキュメント / コード品質（5観点から独立に指摘）
- **累計**: 2回目
- **何が起きているか**: 決定1 は確認未了を「上限を終え、未対応がゼロ」と定義し、決定2 も「上限に達したとき」と書く。決定4 だけが「上限に達する前でも、レビュー自体を実行できなかった場合…確認未了として抜ける」と書く。正本の `SKILL.md` 手順5 は「3周を終えた時点で数える」という枠の中に3つを並べており、上限前の離脱は書かれていない。`unconfirmed-comment.md` も「上限周回を終え」を適用条件にしている。
- **なぜ問題か**: この DR の中心は「いつ抜けてよいか」で、そこが DR 内部でも DR と正本の間でも割れている。字義どおりなら、1周目のレビュアーが落ちて未対応ゼロ（まだ指摘が1件も出ていない）なら、実質0周で抜けられる。既存の運用（`docs/reviews/pr-55.md`「レビューとして成立していないので収束の判定には使わない」＝その周を数えずにやり直す）を、明示せずに覆してもいる。
- **失敗シナリオ**: 2周目のレビュアーが落ちる → 未対応ゼロ → DR に従えば確認未了で抜け、`SKILL.md` に従えば3周目を回す。同じ状態から終わり方が割れる。
- **提案**: 「レビューを実行できなかった周は指摘ゼロとして数えず、やり直す。やり直しても走らせられないまま上限に達したときに確認未了」へ揃え、DR と `SKILL.md` と書式の3つで同じ条件を書く。

## should

### `writing/ambiguous-criterion` — 手順5 の3つの見出しから「上限を終えて」が落ち、1周目でも判定が成立する

- **場所**: `.claude/skills/issue-workflow/SKILL.md`（手順5 の見出し3つ）
- **指摘した観点**: 日本語ドキュメント
- **累計**: 11回目
- **何が起きているか**: 「上から順に判定する」と宣言した太字の見出しが「2. 確認未了 — 未対応がゼロ」「3. 打ち切り — 未対応が残っている」で、上限到達の条件は地の文にしかない。2. は `blocker` / `should` に限る旨も落ちている。
- **なぜ問題か**: 見出しはそのまま判定表として読まれる。1周目のレビュー直後に 3. が成立し、直した直後に 2. が成立する。どちらも上限まで回さずに抜ける。
- **失敗シナリオ**: 1周目で `should` が3件出た時点で手順5 を上から読み、「打ち切り」として残指摘コメントを出して終わる。
- **提案**: 見出しに前提を戻し、判定の前に「上限に達するまで3と4を繰り返す」を置く。

### `inspection/exit-condition-mismatches-state` — `consider` だけが残った周では確認未了に落ちるが、未確認の範囲が空になる

- **場所**: `.claude/skills/issue-workflow/SKILL.md`（手順5）、`.claude/skills/issue-workflow/references/unconfirmed-comment.md`
- **指摘した観点**: 検査ルールの実効性
- **累計**: 1回目（新規）
- **何が起きているか**: 収束は「指摘ゼロのレビューを1周通った」で、`consider` を含めれば指摘ゼロではない。一方で数えるのは `blocker` / `should` だけなので、`consider` のみを出して見送った周は収束でも打ち切りでもなく確認未了になる。だがその状態では最新のコミットまでレビュー済みで、未確認のコミット範囲が存在しない。
- **なぜ問題か**: DR-0052 が打ち切りについて挙げた却下理由（書式が想定していない状態を本文で打ち消す）が、新設した確認未了の側で再発する。回避しようとすると「`consider` だけなら指摘ゼロと見なす」と解釈を曲げる誘因になり、収束へ都合よく抜けられる。
- **失敗シナリオ**: 3周目が `consider` 2件のみ → 理由を残して見送る → 未対応ゼロ → 確認未了と判定 → 未確認の範囲が空で書式が埋まらない。
- **提案**: 「指摘ゼロ」の外延を手順5 に書く。`consider` を理由つきで見送った周は収束を妨げない、`consider` を直したならその修正は未レビューなので確認未了、と決める。

### `inspection/exit-decision-unverifiable` — 「未対応がゼロか」を第三者が再現できる記録が無い

- **場所**: `.claude/skills/issue-workflow/SKILL.md`（手順4・手順5）
- **指摘した観点**: 検査ルールの実効性
- **累計**: 1回目（新規）
- **何が起きているか**: 判定の唯一の入力を「未対応の `blocker` / `should` の数」にしたが、指摘ごとに直したか見送ったかを残す場所が無い。手順4 が求めるのは「見送った指摘の理由」までで、直した側の突き合わせは残らない。
- **なぜ問題か**: DR-0052 は (a) 案を却下する根拠として「未対応がゼロかならレビュー記録を見れば誰でも同じ結果になる」と書いたが、その記録が無いので主張が成り立たない。重い `should` を残したまま確認未了（軽い方の抜け方）を選ぶ余地がある。
- **失敗シナリオ**: 3周目の `should` 3件のうち1件に手を付けず、見送りとも書かずに確認未了で抜ける → PR には「未対応の指摘は無い」と書かれ、残った `should` は誰の目にも入らない。
- **提案**: 未確認コメント・残指摘コメントに、その周までの `blocker` / `should` の対応結果一覧を必須項目として持たせる。

### `contract/record-written-by-non-owning-skill` — `pr-review` の出力ファイルへの書き込みを、書式を持たない側が指示している

- **場所**: `.claude/skills/issue-workflow/SKILL.md`（手順5 末尾）、`docs/decisions/0052-review-loop-third-exit.md`（帰結）、`.claude/skills/pr-review/references/review-file-format.md`
- **指摘した観点**: 設計契約との整合 / フェーズスコープ / 日本語ドキュメント / コード品質（4観点から独立に指摘）
- **累計**: 1回目（新規）
- **何が起きているか**: 「レビューを走らせられずに抜けた回は `docs/reviews/pr-<N>.md` にもその旨を残す」と足したが、そのファイルの書式は `pr-review` が持ち、実行できなかった周を書く節は定義されていない。`issue-workflow` 側は「レビューの中身を自前で作らない」と宣言している。#54 の非スコープは「`pr-review` 側の記録の仕組みの変更」を外している。
- **なぜ問題か**: 書く主体と書式の持ち主が食い違うので、周の通し番号を消費するか、1周目で落ちてファイルが無いときに何を作るかが決まらない。次に `pr-review` が追記する周と衝突する。
- **失敗シナリオ**: 1周目が実行できず、`issue-workflow` が自前の形でファイルを作る → 後日 `pr-review` が初回の書式で書き始め、記録が二重になるか番号がずれる。
- **提案**: この一文を落とし、記録先を未確認コメントの本文へ寄せる。`review-file-format.md` に節を足す作業は `pr-review` 側の変更として別に切る。

### `contract/source-of-truth-ambiguous` — 正本行が、この DR が決めていない範囲（上限の周回数）まで自分のものとして宣言している

- **場所**: `docs/decisions/0052-review-loop-third-exit.md`（正本行・帰結）、`docs/decisions/0025-issue-driven-development-flow.md`（正本行）
- **指摘した観点**: 決定記録との整合 / 設計契約との整合
- **累計**: 8回目
- **何が起きているか**: DR-0052 の正本行が「`SKILL.md`（上限の周回数と、3つの抜け方の判定）」となっており、上限の周回数について DR-0025 と2つの DR が正本行を持つ。加えて正本を指しながら、判定の中身を DR 本文にも書き下している。
- **なぜ問題か**: 「上限を設ける決定を覆すときはどちらの DR を置き換えるのか」が字面から決まらない。この PR の主眼（置き換えの境界をはっきりさせる）を冒頭の1行が崩している。
- **失敗シナリオ**: 該当なし（設計上の問題）。
- **提案**: 正本行から上限の周回数を外す。判定は決定そのものなので DR が持ち、手順への落とし方は `SKILL.md` が持つ、と役割を分ける。

### `decisions/decision-lacks-recorded-basis` — 決定4 に、文脈にも理由にも却下案にも根拠が無い

- **場所**: `docs/decisions/0052-review-loop-third-exit.md`（決定4、文脈、理由）
- **指摘した観点**: 決定記録との整合
- **累計**: 1回目（新規）
- **何が起きているか**: 文脈が挙げる事例は2件とも「検査が1周遅れで現れる」構造の話として書かれており、PR #57 については「上限に達し」と書いているが、実際は4周目を立てて全滅した（＝レビューを実行できなかった）事例である。決定4 が対象とする状態は、文脈でも理由でも却下案でも扱われていない。
- **なぜ問題か**: 他の選択肢（落ちた周はやり直す。`docs/reviews/pr-55.md` の実運用）が実在するのに、選んだ理由が残っていない。
- **失敗シナリオ**: 該当なし（設計上の問題）。
- **提案**: 文脈に PR #57 の4周目の実態を正確に書き、理由と却下案（やり直す案）を足す。

### `decisions/consequence-not-followed` — 部分置き換えという運用が、DR-0024 と README の規則に無い

- **場所**: `docs/decisions/0025-issue-driven-development-flow.md`（状態欄）、`docs/decisions/0024-decision-records-not-adr.md`（帰結）、`docs/decisions/README.md`（末尾の手順）
- **指摘した観点**: 決定記録との整合 / 日本語ドキュメント
- **累計**: 11回目
- **何が起きているか**: 規則は「旧 DR の状態を『置き換え済み』に変える」だが、この PR は「承認済み（…は DR-0052 が置き換えた）」という語彙に無い形を使った。判断としては妥当（全面置き換えは正本の複製を生む）だが、規則側が更新されていない。
- **なぜ問題か**: 次に部分置き換えをする人が、規則の字義と DR-0025 の実例のどちらに従うか決められない。
- **失敗シナリオ**: 次の部分置き換えで旧 DR が「置き換え済み」にされ、状態欄から有効・無効を判定できなくなる。
- **提案**: README（と必要なら DR-0024）に部分置き換えの状態表記を足す。

### `decisions/citation-incomplete-after-rewrite` — 冒頭の要約欄が、この PR の実態と揃っていない

- **場所**: `docs/decisions/0025-issue-driven-development-flow.md`（関連）、`docs/decisions/0052-review-loop-third-exit.md`（実装）
- **指摘した観点**: 決定記録との整合 / 設計契約との整合
- **累計**: 3回目
- **何が起きているか**: DR-0025 の「関連」に DR-0052 が入っていない。DR-0052 の「実装」に、この PR が決定の実装として書き換えた `completion-comment.md` と `pending-comment.md` が入っていない。
- **なぜ問題か**: 要約欄は「この決定に触るとき、どこを一緒に見るか」の索引として読まれる。片方だけ書き換える事故の入り口になる。
- **失敗シナリオ**: 該当なし（設計上の問題）。
- **提案**: 双方に足す。

### `writing/inaccurate-rationale` — 「5点のうち4点」が、同じ文の列挙とも DR-0025 の記述とも合わない

- **場所**: `docs/decisions/0052-review-loop-third-exit.md`（却下案「DR-0025 を全面的に置き換える」）
- **指摘した観点**: 日本語ドキュメント / 設計契約との整合
- **累計**: 16回目
- **何が起きているか**: 「5点のうち4点（…）は変わっていない」と書くが、括弧内の列挙は5項目。置き換え前の DR-0025 の帰結は6項目だった。
- **なぜ問題か**: この文は全面置き換えを却下する根拠そのもので、いくつが残るかが論の核になっている。加えて、列挙自体が DR-0025 の帰結の複製になっている。
- **失敗シナリオ**: 該当なし（設計上の問題）。
- **提案**: 列挙を落として「抜け条件以外は変わっていない」とリンクで示す。

### `writing/dangling-quote-reference` — 却下案のラベル `(c')` が宙に浮き、採用した `(c)` が DR 本文に現れない

- **場所**: `docs/decisions/0052-review-loop-third-exit.md`（検討した他の選択肢）
- **指摘した観点**: 日本語ドキュメント
- **累計**: 8回目
- **何が起きているか**: `(a)` `(b)` `(d)` は #54 の案とラベルが一致するが、`(c')` は #54 に無い別案（上限を無くす）である。`(c)` を採ったという記述は PR 本文にしかない。
- **なぜ問題か**: #54 と DR を並べて読む人は、ラベルが揃っている前提で対応を取る。採用案が却下されたように読める。
- **失敗シナリオ**: 後日 #54 から辿った人が、採用案が何だったかを DR から判定できない。
- **提案**: 文脈か決定の冒頭に「#54 が挙げた (c) を採る」と書き、`(c')` は #54 由来でないラベルへ変える。

### `writing/incomplete-pr-description` — PR 本文が着手時のまま

- **場所**: PR #58 本文
- **指摘した観点**: 日本語ドキュメント
- **累計**: 7回目
- **何が起きているか**: 「作業中。」のままで、完了条件4項目に対する状態も、部分置き換えにした判断も書かれていない。
- **なぜ問題か**: レビューする側が、未完成として読むべきか判断できない。
- **失敗シナリオ**: 該当なし（設計上の問題）。
- **提案**: 手順6 のとおり書き直す。

## consider

### `contract/canonical-value-scattered-within-source` — 上限の値が、正本と決めた節の中で3箇所に散っている

- **場所**: `.claude/skills/issue-workflow/SKILL.md`（手順5）
- **指摘した観点**: 設計契約との整合
- **累計**: 1回目（新規）
- **何が起きているか**: 手順5 に「上限は3周」という宣言は無く、「3周目のレビューが」「3周を終えた時点で」「3周で収束しないなら」の3つの記述で値が表現されている。この PR が1つ増やした。
- **なぜ問題か**: 値を変えるとき3箇所を直すことになり、片方だけ直すと同じ節の中で上限が食い違う。
- **失敗シナリオ**: 上限を4周にする際に2箇所だけ直し、収束の例示が3周目のまま残る。
- **提案**: 節の冒頭で一度だけ宣言し、以降は相対表現にする。

### `decisions/index-entry-out-of-order` — 索引の行が節内の昇順に無い

- **場所**: `docs/decisions/README.md`
- **指摘した観点**: 決定記録との整合 / 日本語ドキュメント
- **累計**: 1回目（新規）
- **何が起きているか**: `0052` の行が `0025` と `0039` の間に入っている。README 自身が「節の中は DR 番号の昇順に置く」と定めている。
- **なぜ問題か**: 索引が自分で定めた並び規則を破ると、次に追記する人が直前の行を真似て乱れが広がる。
- **失敗シナリオ**: 該当なし（設計上の問題）。
- **提案**: 節の末尾へ移す。

### `writing/notation-inconsistency` — 追加した DR 参照だけがリンクになっていない

- **場所**: `.claude/skills/issue-workflow/SKILL.md`（手順5 冒頭）
- **指摘した観点**: 日本語ドキュメント
- **累計**: 8回目
- **何が起きているか**: 「（DR-0052）」が素のテキストで、同じファイルの他の参照はリンク形式。
- **なぜ問題か**: 手順5 は判定の本体で、最も根拠へ飛びたくなる場所である。
- **失敗シナリオ**: 該当なし（設計上の問題）。
- **提案**: リンクに揃える。

### `writing/inaccurate-rationale`（同一カテゴリのため計上は1回） — 「最もよく壊れる形だ」が、根拠に挙げた集計と合わない

- **場所**: `docs/decisions/0052-review-loop-third-exit.md`（理由）
- **指摘した観点**: 日本語ドキュメント
- **何が起きているか**: 累計10回という数字は正しいが、指摘ログにはそれより多いカテゴリが6つある。
- **なぜ問題か**: 却下理由が比較の最上級に乗っているのに、直前の数字がそれを支えていない。
- **失敗シナリオ**: 該当なし（設計上の問題）。
- **提案**: 最上級を外し「繰り返し出ている形」とする。

### `decisions/dr-restates-canonical-value`（同一カテゴリのため計上は1回） — 帰結が `SKILL.md` の文を逐語で写し、文脈が上限の値を含む

- **場所**: `docs/decisions/0052-review-loop-third-exit.md`（帰結の最終項、文脈）
- **指摘した観点**: 決定記録との整合
- **何が起きているか**: 帰結の最終項が `SKILL.md` に足した一文のほぼ逐語の複製で、リンクも持たない。文脈は「3周目」という形で上限の値を含む。
- **なぜ問題か**: DR-0052 自身の帰結（値は複製しない）と衝突する。
- **失敗シナリオ**: 該当なし（設計上の問題）。
- **提案**: 帰結は参照に縮め、文脈の周番号は「当時の上限」と書く。

## 落とした指摘

なし。

## ルール化候補

このレビューで累計3回以上になったカテゴリのうち、新たに閾値へ達したものは無い（`writing/ambiguous-criterion` 11回、`writing/inaccurate-rationale` 16回、`decisions/consequence-not-followed` 11回などは既に候補表に在る）。

- `decisions/inconsistent-partition` — 通算2回。DR の中で立てた分類と、同じ DR の別の決定が噛み合わない形。決定どうしの条件が重ならないことは、いまのところ人しか見ていない

## 2周目（2026-09-22）

対象は1周目からの増分（`7f536dd..5e538c9`）。6観点すべてを立てた。blocker 1件、should 3件、consider 2件。フェーズは指摘ゼロ。1周目の blocker（決定4 と決定1・2 の矛盾）は解消を確認したが、**その直し方が新しい穴を開けた**——「実行できなかった回はやり直す」に上限を置かなかったため、上限に達する経路そのものが消えた。

### blocker

#### `inspection/retry-loop-unbounded-on-self-declared-failure` — やり直しが無制限で、しかも「上限に達した」状態へ到達できない

- **場所**: `.claude/skills/issue-workflow/SKILL.md`（手順5）、`docs/decisions/0052-review-loop-third-exit.md`（決定4）
- **指摘した観点**: 検査ルールの実効性 / 決定記録との整合（2観点から独立に指摘）
- **累計**: 1回目（新規）
- **何が起きているか**: 「レビューを実行できなかった回は周として数えず、やり直す」と定めたが、やり直せる回数に上限が無い。上限（3周）は数えた周でしか進まないので、失敗が続く限り上限には永遠に到達しない。ところが決定4 の後半は「やり直してもなお走らせられないまま**上限に達し**…確認未了として抜ける」と書いており、その出口は定義上到達不可能になっている。加えて「実行できなかった」と判定してよい条件が書かれておらず、6観点のうち一部だけが失敗した場合の扱いも決まっていない。
- **なぜ問題か**: 上限は強制退出のゲート（DR-0025）だが、「今回は実行できなかった」という自己申告ひとつで無制限に回避できる。都合の悪い指摘が返った周を失敗として無効化する経路が、字面の上では開いている。
- **失敗シナリオ**: 3周目で6観点中5観点が指摘を返し、1観点がタイムアウト → その周を「実行できなかった」として無効化 → 5観点分の指摘ごと再実行。上限は減らず、確認未了にも打ち切りにも到達しない。
- **提案**: (1) 「実行できなかった」を客観的に定義する（呼んだ観点が1つも結果を返さなかった回。一部でも返ったなら、返った分を1周の結果として扱う）。(2) やり直せる回数に上限を置き、それを超えたらその回を1周として数える。これで「上限に達し」が到達可能になる。

### should

#### `writing/contradictory-instruction` — 「対応済みの指摘」に `consider` を含めるかが、正本と2つの書式で食い違う

- **場所**: `.claude/skills/issue-workflow/SKILL.md`（手順5 末尾）、`references/pending-comment.md`、`references/unconfirmed-comment.md`
- **指摘した観点**: コード品質 / 日本語ドキュメント（2観点から独立に指摘）
- **累計**: 9回目
- **何が起きているか**: 正本と `pending-comment.md` は「各 `blocker` / `should` を1行ずつ」と書き、`unconfirmed-comment.md` だけが `consider` の見送りも並べるよう求めている。どちらも同じ目的（判定の材料を読み手が再現できる形で残す）で同じ周に足された節である。
- **なぜ問題か**: 使い分けが意図的なのか書き漏れなのか、どのファイルからも読めない。
- **失敗シナリオ**: 打ち切りのコメントに見送った `consider` が載らず、後から見た人が「見落とし」と「見送り」を区別できない。
- **提案**: 正本で `consider` の扱いを決め、3つの書式を揃える。

#### `inspection/exit-decision-unverifiable` — 収束（完了報告）だけ、見送った `consider` の記録義務が無い

- **場所**: `.claude/skills/issue-workflow/references/completion-comment.md`（この周では変更していない）、`.claude/skills/issue-workflow/SKILL.md`（手順5）
- **指摘した観点**: 検査ルールの実効性
- **累計**: この PR で計上済みのため +1 しない
- **何が起きているか**: 「`consider` を理由つきで見送った周は収束を妨げない」と明文化した一方、収束時に出す完了報告には見送りの一覧を求めていない。確認未了・打ち切りには足した。
- **なぜ問題か**: 収束は最も強い合図（マージ判断の材料）なのに、記録の義務がいちばん緩い。`consider` を見送って収束へ抜ける方が楽、という向きに力が働く。
- **失敗シナリオ**: `consider` 4件を理由も書かずに見送って完了報告 → 読み手は「残っている判断: なし」だけを見て、4件の存在に気づけない。
- **提案**: 完了報告にも見送った `consider` の一覧を必須項目として足す。

#### `decisions/undocumented-decision` — `consider` の扱い（収束の外延）が `SKILL.md` にだけあり、DR に無い

- **場所**: `.claude/skills/issue-workflow/SKILL.md`（手順5）、`docs/decisions/0052-review-loop-third-exit.md`（決定1）
- **指摘した観点**: 決定記録との整合
- **累計**: 16回目
- **何が起きているか**: 「`consider` を直したなら収束ではない / 見送ったなら収束を妨げない」という切り分けを手順にだけ書いた。DR は「収束の定義は変えない」と書くだけで、この外延に触れていない。
- **なぜ問題か**: 他の選び方（`consider` を直しても収束扱いにする）がありえた判断で、理由を後から聞かれる。正本と決定が別々に判断を持つ形になっている。
- **失敗シナリオ**: 該当なし（設計上の問題）。
- **提案**: 決定1 に外延を1文足す。

### consider

#### `contract/source-of-truth-claim-duplicated-in-document` — 正本の所在の宣言が、同じ DR の2箇所にある

- **場所**: `docs/decisions/0052-review-loop-third-exit.md`（冒頭の地の文と帰結の最終項）
- **指摘した観点**: 設計契約との整合
- **累計**: 1回目（新規）
- **何が起きているか**: 「上限の周回数の正本は `SKILL.md` 手順5」という同じ宣言が、冒頭と帰結に独立して書かれている。
- **なぜ問題か**: 正本の所在が動いたとき、片方だけ直ると同じ DR の中で在り処が2つに割れる。正本を一意にすることがこの PR の主眼であるだけに、逆行している。
- **失敗シナリオ**: 該当なし（設計上の問題）。
- **提案**: どちらか一方へ集約する。

#### `writing/ambiguous-criterion`（同一カテゴリのため計上は1回） — 「収束するまで、上限に達するまで」が逐次実行とも読める

- **場所**: `.claude/skills/issue-workflow/SKILL.md`（手順5 冒頭）
- **指摘した観点**: 日本語ドキュメント
- **何が起きているか**: 2つの「まで」を接続助詞なしに並べたので、「収束するまで回し、続けて上限に達するまで回す」とも読める。
- **なぜ問題か**: 手順5 全体の前提を宣言する最初の一文で、ここを誤読すると以降の分岐の読み方が変わる。
- **失敗シナリオ**: 該当なし（設計上の問題）。
- **提案**: 「収束するか、上限に達するまで」と書く。

### 落とした指摘

なし。

### ルール化候補（2周目）

新たに閾値へ達したカテゴリは無い。

## 3周目（2026-09-22）

対象は2周目からの増分（`5e538c9..fe782e6`）。差分が Skill・書式・DR に閉じているため、2周目に指摘が出た5観点（設計契約 / 検査 / 決定記録 / 日本語 / コード品質）を立てた。blocker 1件、should 6件、consider 2件。

2周目の blocker（やり直しが無制限で上限へ到達できない）は解消を確認した。やり直しに上限が入り、「実行できなかった」が**呼んだ観点のどれ一つとして結果が返らなかった**ことに限定され、「上限に達し」が到達可能になっている。2周目の should 3件（`consider` の扱いの食い違い、完了報告の記録義務、DR への外延の記録）も解消を確認した。

**だが2周目と同じ形で、直し方が新しい穴を開けている。** 「一部の観点が返したなら、返った分がその周の結果になる」と書いたことで、**1観点だけが指摘ゼロで返った周が収束に使えるようになった**。2周目が塞いだ「周ごと失敗と申告して消す」経路が、「都合の悪い観点だけ落ちたことにする」形で半分開き直っている。

### blocker

#### `inspection/convergence-ignores-review-coverage` — 一部の観点しか返らなかった周が、収束に使える

- **場所**: `.claude/skills/issue-workflow/SKILL.md`（手順5、レビューが返らなかった回の扱いと「1. 収束」）、`docs/decisions/0052-review-loop-third-exit.md`（決定4）
- **指摘した観点**: 検査ルールの実効性 / コード品質（2観点から独立に指摘）
- **累計**: 1回目（新規）
- **何が起きているか**: 決定4 と手順5 は「呼んだ観点のどれ一つとして結果が返らなかった」回だけをやり直し対象にし、「一部の観点が返したなら、返った分がその周の結果になる」と定めた。一方で収束の条件は「指摘ゼロのレビューを1周通った」で、その周に**何観点が実際に走ったか**を問わない。応答した観点の下限はどこにも書かれていない。
- **なぜ問題か**: 6観点のうち5観点が落ち、残った1観点がたまたま指摘ゼロで返った周が、6観点すべてが見て指摘ゼロだった周と同じ扱いになる。収束はこの DR 自身が「最も強い合図」と位置づけ、完了報告を出す唯一の抜け方だ。それを成立させる審査の網羅性に下限が無い。2周目の blocker が塞いだ「都合の悪い周を失敗と申告して消す」経路が、「都合の悪い観点だけ落ちたことにする」形で開き直っている。
- **失敗シナリオ**: 3周目、6観点を呼ぶ。5観点が外部の制約で結果を返さず、1観点だけが「報告すべきものは無い」と返す → 手順5 により、やり直し対象にはならず、返った分（0件）がその周の結果になる → 収束が成立し、完了報告が出る → 残り5観点は2周目の修正を一度も見ないまま、マージ判断の材料になる。
- **提案**: 収束に使えるのは「呼んだ観点がすべて結果を返した周」だけとする。一部が落ちた周は、返った指摘をその周の結果として扱い上限も進めるが、収束には使えない（確認未了へ倒す）。決定4 と手順5 の両方へ書く。

### should

#### `writing/inaccurate-rationale` — 「その周は指摘ゼロではない」が、同じ節の「指摘ゼロ」の定義と食い違う

- **場所**: `.claude/skills/issue-workflow/SKILL.md`（手順5、やり直しの上限）
- **指摘した観点**: 日本語ドキュメント
- **累計**: この PR で計上済みのため +1 しない
- **何が起きているか**: 「3度目も1件も返らなければ、その回を1周として数える。数えた以上は上限が進み、**その周は指摘ゼロではない**ので収束には使えない」と書いた。だが6行後の定義は「指摘ゼロ」を「`blocker` / `should` がゼロで、かつ `consider` を直していないこと」としており、何も走っていない周はこの定義に字義どおり当てはまる。DR-0052 決定4 は同じ除外を「指摘の有無を**判定できていない**ので」という別の根拠で書いている。
- **なぜ問題か**: 正本と DR で除外の理由が違い、しかも正本側の理由が同じ節の定義と噛み合わない。後からこの一文を根拠に、定義の側を書き換える誤った修正を招く。
- **失敗シナリオ**: 該当なし（結論は両文書とも「収束に使えない」で一致しており、動作は割れない）。
- **提案**: 理由を DR-0052 と揃え、「指摘の有無を判定できていないので」に書き換える。

#### `decisions/consequence-not-followed` — 帰結が、完了報告にも記録義務を課したこの周の修正に追従していない

- **場所**: `docs/decisions/0052-review-loop-third-exit.md`（帰結の最終項）、`.claude/skills/issue-workflow/references/completion-comment.md`
- **指摘した観点**: 決定記録との整合 / 日本語ドキュメント（2観点から独立に指摘）
- **累計**: この PR で計上済みのため +1 しない
- **何が起きているか**: 帰結の最終項は「判定に使った材料は、未確認コメントと残指摘コメントが持つ」と3つの出口のうち2つしか挙げていない。この周で `completion-comment.md` に「対応済みの指摘」節を新設し、手順5 も「3つのコメントのどれにも」と明文化したのに、帰結が古いまま残った。
- **なぜ問題か**: 直したのはまさに「収束だけ記録義務が緩い」という2周目の指摘で、その修正の結果として帰結が実態を過小に述べる形になった。帰結は「この決定が何を変えたか」の索引として読まれる。
- **失敗シナリオ**: 該当なし（設計上の問題）。
- **提案**: 帰結の最終項を、3つの書式すべてが対応結果の一覧（見送った `consider` を含む）を持つ旨へ書き換える。

#### `decisions/dr-restates-canonical-value` — 決定4 が、正本と宣言した「やり直せる回数」の値を本文に書き写している

- **場所**: `docs/decisions/0052-review-loop-third-exit.md`（冒頭の正本行・決定4）、`.claude/skills/issue-workflow/SKILL.md`（手順5）
- **指摘した観点**: 設計契約との整合 / 決定記録との整合（2観点から独立に指摘）
- **累計**: この PR で計上済みのため +1 しない
- **何が起きているか**: 冒頭は「上限の周回数と、やり直せる回数は、どちらも値であり、正本は `SKILL.md` の手順5」と宣言する。ところが決定4 は「やり直しは同じ周について**2回まで**」「**3度目**も返らなければ」と値をそのまま書いている。既存の値（上限の周回数）については DR 本文に数字が一度も現れず、常に「上限に達し」と相対的に参照している——同じ DR の中で、2つの値の扱いが割れている。
- **なぜ問題か**: 正本を一意にすることがこの PR の主眼で、1周目の同種指摘（帰結が正本の文を逐語で写していた）はこの周に参照へ直した。その同じ周に、新しい値で同じ複製を作っている。
- **失敗シナリオ**: 将来やり直しの回数を緩めるとき、`SKILL.md` だけ直して DR を直し忘れると、正本と決定記録で値が食い違う。
- **提案**: 決定4 から具体的な回数を落とし、「やり直せる回数にも上限を置く（値は `SKILL.md` 手順5）」へ相対化する。既存の値と同じ書き方に揃える。

#### `decisions/decision-lacks-recorded-basis` — 「2回まで」という値を選んだ根拠が、理由にも却下案にも無い

- **場所**: `docs/decisions/0052-review-loop-third-exit.md`（決定4・理由）
- **指摘した観点**: 決定記録との整合
- **累計**: この PR で計上済みのため +1 しない
- **何が起きているか**: 理由に足したのは「なぜ何らかの上限が要るか」（無制限だと上限へ到達できない）だけで、「なぜ1でも3でもなく2なのか」はどこにも無い。
- **なぜ問題か**: 閾値は他の値もありえた判断で、後から理由を聞かれる。1周目に決定4 全体が「根拠が無い」と指摘された穴が、この周に足した値1点で再発している。
- **失敗シナリオ**: 該当なし（設計上の問題）。
- **提案**: 値そのものは正本（`SKILL.md`）が持つので、DR には値を決める**規準**を書く。「1つの周についての呼び出しを、上限の周回数と同じ回数までに収める」なら、値を書かずに根拠が残り、上の `dr-restates-canonical-value` とも両立する。

#### `code/template-example-omits-required-row` — 「対応済みの指摘」の例に `[blocker]` の行が無く、直前の指示と食い違う

- **場所**: `.claude/skills/issue-workflow/references/completion-comment.md`、`.claude/skills/issue-workflow/references/pending-comment.md`
- **指摘した観点**: コード品質
- **累計**: 1回目（新規）
- **何が起きているか**: 直前の指示は「各 `blocker` / `should` を1行ずつ」なのに、例として並ぶのは `[should]` と `[consider]` の2行だけ。同じ役割の `unconfirmed-comment.md` は3種とも例を持つ。
- **なぜ問題か**: 例は雛形として写される。`blocker` の行が抜けると、抜け方の判定に使う材料がそこだけ欠け、読み手は「blocker は本当に直ったのか」を一覧から確認できない。
- **失敗シナリオ**: 2周目までの `blocker` を直し切り、3周目に `should` だけが残って打ち切る場合。書き手が例に倣って `should` / `consider` の行だけを書き、直した `blocker` を落とす。
- **提案**: 両方へ `[blocker]` の例行を足し、`unconfirmed-comment.md` と揃える。

#### `inspection/exit-decision-unverifiable` — 「やり直しは2回まで」を守ったことが、どこにも残らない

- **場所**: `.claude/skills/issue-workflow/SKILL.md`（手順5）、`.claude/skills/issue-workflow/references/unconfirmed-comment.md`
- **指摘した観点**: 検査ルールの実効性
- **累計**: この PR で計上済みのため +1 しない
- **何が起きているか**: やり直しの上限を新設したが、実際に何回やり直したかを書く場所が無い。未確認コメントは「なぜその周を回せなかったか…とその事情」という自由記述を持つだけだ。
- **なぜ問題か**: 「3度目も返らなかった」かどうかは書き手の申告以外に確かめる手段が無い。DR-0052 決定4 の理由が「自己申告で上限を回避できる」ことを問題視しているのに、そのために置いた上限自体が同じ弱点を持っている。
- **失敗シナリオ**: 該当なし（判定の入力が記録されないという構造の問題）。
- **提案**: 未確認コメントの書式へ、やり直した回数（何回目で1周として数えたか）を必須項目として足す。

### consider

#### `decisions/citation-overclaims-source-scope` — 書式2つが、DR-0052 に無い規則の根拠として DR-0052 を引いている

- **場所**: `.claude/skills/issue-workflow/references/completion-comment.md`、`.claude/skills/issue-workflow/references/pending-comment.md`
- **指摘した観点**: 設計契約との整合
- **累計**: 7回目
- **何が起きているか**: 「見送った `consider` も同じ一覧に入れる」「記録の義務を他の2つの抜け方より緩くしない」の根拠として `DR-0052` を引くが、この規則を書いているのは `SKILL.md` 手順5 だけで、DR 本文には無い（DR で `consider` に触れるのは決定1 の収束の外延のみ）。
- **なぜ問題か**: 根拠を辿る読み手が DR に見つけられず、DR 側へ同じ規則を書き足す——つまり複製する——誘因になる。
- **失敗シナリオ**: 該当なし（設計上の問題）。
- **提案**: 上の `decisions/consequence-not-followed` を直して帰結が3つの書式を持つ形になれば、引用は成立する。同じ修正で解消する。

#### `writing/ambiguous-criterion` — 完了報告の「残っている判断」欄と、新設した見送り一覧の使い分けが書かれていない

- **場所**: `.claude/skills/issue-workflow/references/completion-comment.md`
- **指摘した観点**: 日本語ドキュメント
- **累計**: この PR で計上済みのため +1 しない
- **何が起きているか**: 既存の要約欄「残っている判断: <あれば。無ければ「なし」>」と、新設した「対応済みの指摘」（見送った `consider` を並べる）の関係が書かれていない。
- **なぜ問題か**: 見送った `consider` を一覧だけに書くか、要約欄にも重ねて書くかで解釈が割れる。前者なら要約欄が形骸化し、後者なら同じ情報が2箇所に載って片方だけ古くなる。
- **失敗シナリオ**: 該当なし（書式のあいまいさ）。
- **提案**: 「残っている判断」は指摘に紐付かない開いた論点（Issue の前提、設計判断）に限る旨を一言添える。

### 落とした指摘

なし。

### ルール化候補（3周目）

新たに閾値へ達したカテゴリは無い。`inspection/convergence-ignores-review-coverage` と `code/template-example-omits-required-row` はどちらも通算1回。

- `inspection/convergence-ignores-review-coverage` — 通算1回。「その周に何観点が結果を返したか」を記録させれば、機械で数えられる形になる。記録が先で、検査はその後

## 4周目（2026-09-22）

対象は3周目からの増分（`fe782e6..1af2124`）。上限（3周）は3周目で終えており、この周は確認未了で抜けた PR に対して**人が選んだ追加のレビュー**（[DR-0052](../decisions/0052-review-loop-third-exit.md) の帰結：未確認の範囲を人が見るか、もう1周回すか）。3周目と同じ5観点を立てた。blocker 1件、should 5件、consider 0件。

3周目の指摘はすべて解消を確認した。だが**4周連続で、前の周の修正が開けた穴が blocker として出ている**。3周目に足した「収束に使えるのは、呼んだ観点がすべて結果を返した周だけ」という条件は、観点が**落ちる**経路は塞いだが、そもそも**呼ばない**経路を塞いでいない。`pr-review` は再レビューで観点を絞ってよいと定めており、この PR 自身、3周目と4周目はフェーズスコープを呼んでいない。

### blocker

#### `inspection/convergence-ignores-review-coverage` — 「呼んだ観点がすべて返った周」は、観点を絞ることによる回避を塞いでいない

- **場所**: `.claude/skills/issue-workflow/SKILL.md`（手順5「1. 収束」）、`docs/decisions/0052-review-loop-third-exit.md`（決定4）、`.claude/skills/pr-review/SKILL.md`（手順4。今回の対象外だが、この節が上の条件の効力を決めている）
- **指摘した観点**: 検査ルールの実効性 / 設計契約との整合（2観点から独立に指摘）
- **累計**: この PR で計上済みのため +1 しない
- **何が起きているか**: 3周目に足した条件が見ているのは「**呼んだのに返らなかった**観点があるか」だけだ。「そもそも呼ばなかった」場合は対象外のまま残っている。`pr-review` 手順4 は、再レビューで前周に指摘ゼロだった観点を立てなくてよいと定めている（差分がドキュメント・契約・Skill に閉じているとき）。この2つを組み合わせると、「前周に指摘ゼロだった観点を呼ばない」→「絞った観点がすべて指摘ゼロで返る」→ 条件を字義どおり満たして収束、という経路が残る。
- **なぜ問題か**: 条件を足したときの理由づけが「1観点だけが指摘ゼロで返った周を収束と呼べるなら、上限は観点を落とすことで回避できてしまう」だった。自分で名指しした抜け道を塞げていない。**この PR 自身がその実例になっている**——2周目にフェーズスコープが指摘ゼロになり、3周目・4周目は手順4 に従って5観点しか立てていない。フェーズスコープは2周目以降の修正を一度も見ていない。
- **失敗シナリオ**: 4周目の5観点がすべて指摘ゼロで返る → 「呼んだ観点（5）がすべて返った」を満たし収束 → 完了報告が出る。フェーズスコープは3周目・4周目の修正を見ていないが、収束の判定には影響しない。
- **提案**: 収束に使える周を「**観点を絞らずに呼んだ周**」に限る。絞った周は返った指摘がその周の結果になり上限も進むが、収束には使えない。「絞るかどうか」を決めるのは `pr-review` なので、呼ぶ側は「収束させたい周は絞らないでほしい」と伝える側に回る（手順3 の責務）。あわせて、その周に呼んだ観点を完了報告へ書かせ、読み手が確かめられるようにする。

### should

#### `contract/convergence-criterion-crosses-undeclared-boundary` — 収束の判定が、`issue-workflow` が正式に受け取っていない情報に依存している

- **場所**: `.claude/skills/issue-workflow/SKILL.md`（手順3・手順5）、`.claude/skills/pr-review/SKILL.md`（手順4）、`.claude/skills/pr-review/references/review-file-format.md`
- **指摘した観点**: 設計契約との整合
- **累計**: 1回目（新規）
- **何が起きているか**: 「呼んだ観点がすべて結果を返したか」を判定するには、その周に何観点が立ったかを知る必要がある。だが観点を決めるのは `pr-review` 手順4 で、`review-file-format.md` にはその周に呼んだ観点を記録する項目が無い。一方 `issue-workflow` 手順3 は「`pr-review` が何を変えるかをここに書き写さない。呼ぶ側にも同じ条件分岐を置くと、片方だけ変更されたときに食い違いが検出できなくなる」と宣言している。
- **なぜ問題か**: 手順3 が掲げた原則が、手順5 の新しい条件で破れている。呼ぶ側が、受け取っていない情報を前提に判定することになる。`pr-review` 手順4 の絞り込み基準が将来変わっても、`issue-workflow` 側には検知する術が無い。
- **失敗シナリオ**: `pr-review` の絞り込み基準が変わる → `issue-workflow` は自分が渡した「前周で指摘が出た観点」を根拠に「すべて返った」と判定するが、実際に立った観点はそれと違う → 一部しか見ていない周を収束と誤判定する。
- **提案**: 判定の入力を「呼ぶ側が決められること」へ移す。収束させたい周は観点を絞らないよう頼む、と手順3 に書き、手順5 の条件を「絞らずに呼んだ周」に変える。`review-file-format.md` へ「呼んだ観点」の項目を足すのは `pr-review` 側の変更であり、#54 の非スコープ（別 Issue）。

#### `decisions/one-sided-coupling` — 収束の条件が決定4 にあり、収束を定義する決定1 から辿れない

- **場所**: `docs/decisions/0052-review-loop-third-exit.md`（決定1・決定4）
- **指摘した観点**: 決定記録との整合
- **累計**: 6回目
- **何が起きているか**: 「収束に使えるのは全観点が返った周だけ」を決定4（見出しは「レビューが1件も返らなかった回は…」）へ書いた。収束を定義しているのは決定1 で、そちらには参照も言及も無い。正本の `SKILL.md` は同じ条件を「1. 収束」の直下に置き、前段からは「（下の「1. 収束」）」と前方参照を張っている——DR だけがその構造を持たない。
- **なぜ問題か**: 決定1 だけを読むと「一部の観点しか返らなくても指摘ゼロなら収束」と読める。正本より DR の方が構成として後退している。
- **失敗シナリオ**: 決定1 を参照して収束の条件を別の文書へ要約するとき、決定4 にしかない制約が漏れる。
- **提案**: 決定1 の収束の説明の直後に、観点の網羅について決定4 を参照する一文を足す。

#### `inspection/exit-decision-unverifiable` — やり直し回数の記録義務が、3つの抜け方のうち1つにしか無い

- **場所**: `.claude/skills/issue-workflow/SKILL.md`（手順5）、`references/completion-comment.md`、`references/pending-comment.md`
- **指摘した観点**: 検査ルールの実効性 / 日本語ドキュメント（2観点から独立に指摘）
- **累計**: この PR で計上済みのため +1 しない
- **何が起きているか**: 3周目に新設した「何回やり直したかを書く」義務は、未確認コメントにしか無い。やり直しはどの周でも起こりうるので、1周目にやり直しがあって3周目に収束した場合、完了報告にはその痕跡が残らない。
- **なぜ問題か**: 同じ周に DR-0052 の帰結へ「収束はマージ判断の材料として最も強い合図なので、そこだけ記録を緩くしない」と書いた。その原則と、同じ周に新設した記録義務が矛盾している。
- **失敗シナリオ**: 1周目でやり直しの上限を使い切り、3周目に収束 → 完了報告にはやり直しの欄が無く、上限を守ったかどうかを誰も確かめられない。
- **提案**: 3つの書式すべてへ、やり直しが発生したならその回数を書く欄を足す（無ければ「無し」）。手順5 の一文も「3つのコメントのどれにも」へ揃える。

#### `decisions/dr-restates-canonical-value` — 新しい規則の文と理由が、正本と DR へ逐語で二重に書かれた

- **場所**: `.claude/skills/issue-workflow/SKILL.md`（手順5）、`docs/decisions/0052-review-loop-third-exit.md`（決定4・理由）
- **指摘した観点**: コード品質
- **累計**: この PR で計上済みのため +1 しない
- **何が起きているか**: 「収束に使えるのは、呼んだ観点がすべて結果を返した周だけだ」という一文と、それに続く理由（「1観点だけが指摘ゼロで返った周を収束と呼べるなら…」）が、両ファイルへほぼ同じ形で入った。同じ周に、値の複製（`decisions/dr-restates-canonical-value`）を1件直している。
- **なぜ問題か**: 値ではなく規則の文そのものを複製する形で、同じ壊れ方が再発した。どちらかを変えるとき両方を直す必要があるが、それを強制する仕組みは無い。
- **失敗シナリオ**: 条件を将来変えるとき片方だけ直し、正本の手順と決定記録が食い違う。
- **提案**: 役割で切る。**何をするか**（手順・値）は `SKILL.md`、**なぜそうするか**（決定と理由）は DR。正本側の理由文を削る。

#### `contract/canonical-value-scattered-within-source` — やり直し回数の規準文が、正本の中で値を二重に表現している

- **場所**: `.claude/skills/issue-workflow/SKILL.md`（手順5）、`docs/decisions/0052-review-loop-third-exit.md`（理由）
- **指摘した観点**: 設計契約との整合
- **累計**: この PR で計上済みのため +1 しない
- **何が起きているか**: 3周目に足した規準「1つの周についての呼び出し（初回＋やり直し）が上限の周回数と同じ回数に収まる」が、正本の手順5 と DR の理由の両方にある。正本の同じ段落には値の明示（「2回まで」）と、この規準文から逆算できる値が並んでいる。
- **なぜ問題か**: 上限を4周へ緩めたとき、規準文どおりなら「3回まで」になるが、明示の「2回まで」を直し忘れると、同じ段落の中で値が割れる。1周目の同カテゴリ（上限の値が3箇所に散っていた）と同じ壊れ方が、別の値で再発した。
- **失敗シナリオ**: 上限を変えるとき「上限は3周」だけを直し、「2回まで」が残る。同じ段落の中で2つの値が食い違う。
- **提案**: 正本は値だけを持ち、規準は DR の理由だけに置く。

### 落とした指摘

なし。

### ルール化候補（4周目）

新たに閾値へ達したカテゴリは無い。

- `inspection/convergence-ignores-review-coverage` — 通算1回（3周目に続き同じ PR で2周連続）。同じ PR では計上しないが、**2周続けて同じカテゴリの blocker が出た**ことは記録しておく。「その周に呼んだ観点」がレビュー記録に残れば機械で数えられる。記録先を持つのは `pr-review` 側で、#54 の非スコープ
