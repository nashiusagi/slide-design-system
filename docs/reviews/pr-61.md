# PR #61 レビュー

- **タイトル**: DR の参照・索引・書式の整合を機械で検査する
- **レビュー日**: 2026-09-23
- **対象**: main...feature/45_decision-reference-check
- **変更ファイル数**: 7
- **立った観点**: 設計契約との整合 / 検査ルールの実効性 / 決定記録との整合 / フェーズスコープ / 日本語ドキュメント / コード品質

## 要約

blocker 2件、should 11件、consider 8件。指摘ゼロの観点は無い。

**検査を新設する PR は、その検査自身の抜け道を問われる。** [#54](https://github.com/nashiusagi/slide-design-system/issues/54) が「検査を新設する PR は構造的に上限周回に当たる」と書いた形が、そのまま出た。`inspection/rule-has-bypass` は累計18回で、このリポジトリで最も多い壊れ方だ。

blocker は2つとも**緑が嘘になる**形。(1) 閉じていないコードフェンスが1つあると、そのファイルの以降すべてが無検査になる——しかもフェンスの閉じ忘れは PR #60 で実際に起きている。(2) `extractPaths` がルート直下のファイルと末尾スラッシュのディレクトリを取りこぼし、既存の冒頭欄23件が一度も検査されていない。決定2（正本に実装コードを挙げない）も、ルート直下に書けば回避できる。

should は3系統。(1) **宣言と実装の食い違い**——走査対象・リンク検査の範囲・状態欄の主張が、どれも実装より広いか狭い。(2) **既存 DR の直し残し**——DR-0040 の帰結がまだ「正本」と呼び、DR-0046 の同型が手つかず。(3) **テストの検出力**——ミューテーションで3つの分岐が生き残った。

## blocker

### `code/unclosed-fence-disables-remaining-checks` — 閉じていないコードフェンスが1つあると、そのファイルの以降が丸ごと無検査になる

- **場所**: `scripts/check-decisions.mjs`（`stripCodeBlocks` と、それを呼ぶ3つの検査）
- **指摘した観点**: 検査ルールの実効性 / コード品質（2観点から独立に指摘）
- **累計**: 1回目（新規）
- **何が起きているか**: `stripCodeBlocks` は ``` 行ごとに `inFence` を反転させるだけで、ファイル末尾で `true` のまま残っても報告しない。閉じ忘れ以降の行はすべて空行になり、3つの検査が「何も書かれていない」とみなす。統合時に実際に確かめた——`# x` + 開いたままのフェンス + `DR-9999` + 壊れたリンク、という入力に対し、剥がした結果は `"# x\n\n\n\n"` で、参照の検査は空配列を返した。
- **なぜ問題か**: この PR が捕まえると宣言した `decisions/wrong-dr-citation` と `code/relative-link-wrong-depth` が、上流のフェンス1つで無効化される。**フェンスの閉じ忘れはこのリポジトリで実際に起きている**（PR #60 の `code/unclosed-fence-swallows-trailing-prose`）。DR-0054 の却下理由は「緑が何も保証しない検査は、無い方がましだ」と書いているが、ファイル単位でその状態が作れる。
- **失敗シナリオ**: `docs/` の文書にフェンスの閉じ忘れが入る → その後ろに書いた DR 参照・リンクが何本壊れても `pnpm check` は緑 → DR-0054 の帰結「どれも `main` へ入る前に落ちる」を信じた人が壊れたまま通す。
- **提案**: `stripCodeBlocks` が閉じ忘れも返す形にし、5つ目の検査として載せる。副産物として `code/unclosed-fence-swallows-trailing-prose` そのものも機械で捕まえられる。

### `inspection/rule-has-bypass` — `extractPaths` がルート直下と末尾スラッシュを取りこぼし、既存の冒頭欄23件が無検査のまま

- **場所**: `scripts/check-decisions.mjs`（`extractPaths` の判定と、それを使う冒頭欄の検査）
- **指摘した観点**: 検査ルールの実効性
- **累計**: 19回目
- **何が起きているか**: 判定が「スラッシュを1つ以上含み、末尾がスラッシュでない」ことを要求している。統合時に確かめた——`package.json` / `DESIGN.md` / `design/layouts/` / `eslint.config.js` / `src/runtime/` はすべて空配列を返す。既存 DR の `**正本**:` / `**実装**:` 行のうち、抽出できたのは54件、パスなのに落ちたのが23件。バッククォート無しで書いたパスも全部素通りする（docstring は「素の形も拾う」と書いているのに）。
- **なぜ問題か**: 冒頭欄のパスが実在するかは決定1の4項目目そのもので、3割超が対象外になっている。さらに**決定2（`**正本**:` に実装コードを挙げるのを禁じる）は拡張子判定に依存しており、パスが抽出されなければそこへ到達しない**。この PR は DR-0039 / DR-0040 を直したが、`eslint.config.js` のようにルート直下へ書けば同じ違反が通る。
- **失敗シナリオ**: 冒頭欄に `- **正本**: \`eslint.config.js\`, \`存在しない.md\`` と書く → 4項目すべて緑、終了コード0。実装コードが正本として通り、存在しないファイルも検出されない。
- **提案**: 抽出条件から「スラッシュ必須」を外し、拡張子を持つか末尾がスラッシュかで判定する。ディレクトリを正本に挙げた場合の扱いを DR-0054 へ書く。docstring も実装に合わせる。

## should

### `inspection/rule-has-bypass`（同一カテゴリのため計上は1回） — 走査対象が `experiments/` `design/` と `.md` 以外を外しており、宣言と食い違う

- **場所**: `scripts/check-decisions.mjs`（`SCAN_ROOTS` と `.md` フィルタ）、`docs/decisions/0054-decision-reference-checked-by-machine.md`（決定1）
- **指摘した観点**: 検査ルールの実効性 / 設計契約との整合 / 日本語ドキュメント / フェーズスコープ（4観点から独立に指摘）
- **何が起きているか**: 走査は77ファイル。追跡中の `.md` のうち外れているのは `experiments/harness-intro/*.md` 6本と `design/decks/harness-intro.md`。`scripts` / `packages` は `SCAN_ROOTS` にあるが `.md` フィルタのため README 2本しか拾わず、`scripts/*.mjs` のコメント中の DR 引用（約79箇所）と `design/**/*.json` の `$comment`（52箇所）は1件も見ない。DR-0054 は「走査対象は `docs/reviews/` を除く」とだけ書き、スクリプトも「リポジトリ内の Markdown を広く見る」と書いている——どちらも除外の宣言の形だが、実装は7つの根を列挙する包含リストだ。
- **なぜ問題か**: `experiments/` には `[DR-0015](../../docs/decisions/0015-first-experiment.md)` の形の相対リンクが既に5本あり、`runs/README.md` は3階層と、**深さが分かれている唯一のディレクトリ**。DR-0054 が実害の例に挙げた「階層の取り違え」が最も起きやすい場所が、まるごと対象外になっている。あわせて `check-canonical-duplication.mjs` は `experiments` を走査対象に入れており、2つの `SCAN_ROOTS` が理由の書かれないまま食い違っている。
- **失敗シナリオ**: `experiments/harness-intro/brief.md` の DR リンクを1階層ずらす → `pnpm check` は緑。同じ誤りを `docs/` で犯せば落ちる。
- **提案**: `experiments` と `design` を足す（`runs/` `starter/` の扱いは宣言する）。`.md` 以外も DR 番号の実在だけは見る。広げないなら、外した理由を `docs/reviews/` と同じく名指しで書く。

### `contract/scan-scope-declarations-diverge` — 2つの検査が走査場所の宣言を別々に持ち、既に食い違っている

- **場所**: `scripts/check-decisions.mjs`（`SCAN_ROOTS`）、`scripts/check-canonical-duplication.mjs`（`SCAN_ROOTS`）
- **指摘した観点**: 設計契約との整合
- **累計**: 1回目（新規）
- **何が起きているか**: 除外（`docs/reviews/`）は揃っているが、canonical 側が理由つきで含めている `experiments` が decisions 側に無く、逆に `packages` ディレクトリ全体は decisions 側だけにある。
- **なぜ問題か**: 走査場所の宣言が2箇所に独立して存在し、片方にだけ新しい文書ルートが足される形なので、今後もずれ続ける。どちらの検査がどこを見ているかは2箇所を突き合わせないと分からない。
- **失敗シナリオ**: 新しい文書ディレクトリを足し、片方の `SCAN_ROOTS` にだけ追加する → もう片方に穴が空いたことに誰も気づかない。
- **提案**: 走査の列挙（`SCAN_ROOTS` を受け取って `.md` を列挙する関数、exclude の判定、`REPO_ROOT` / `resolve`）を `scripts/lib/` へ出す。リスト自体は別でよいが、**違いが意図であること**をどちらか一方へ一度だけ書く。

### `inspection/invalid-case-untested` — 新設した検査の3つの分岐が、テストを1つも通っていない

- **場所**: `scripts/check-decisions.test.mjs`、`scripts/check-decisions.mjs`（索引のリンク先実在、`**実装**` のパス実在、`REQUIRED_FIELDS` の `状態`、`matchesExclude` の `**` 展開）
- **指摘した観点**: 検査ルールの実効性 / コード品質（2観点から独立に指摘。どちらもミューテーションで実測）
- **累計**: 6回目
- **何が起きているか**: 実装を1箇所ずつ壊して33テストが落ちるかを測った結果、次が生き残った。(1) 索引行のリンク先実在検査を `if (false)` にしても33 passed。(2) 冒頭欄のパス検査を `['正本']` に縮めても33 passed。(3) `REQUIRED_FIELDS` から `状態` を外しても33 passed。(4) `matchesExclude` の `**` 展開を壊しても33 passed。
- **なぜ問題か**: テストファイル自身が冒頭で「valid だけを確かめても検査が機能している証拠にならない」と宣言しているのに、この4経路はまさにその形になっている。`**実装**` は22本の DR が使っている欄だ。
- **失敗シナリオ**: 将来この欄ループを整理して `実装` を落としても `pnpm test` は緑のまま通り、リネームの取りこぼしが検出されない状態が残る。
- **提案**: 生き残った4経路に invalid ケースを1本ずつ足す。`matchesExclude` は export するか、ファイル一覧を注入できる形にする。

### `decisions/implementation-labeled-as-canonical` — DR-0040 の帰結が、まだ実装コードを「正本とする」と書いている

- **場所**: `docs/decisions/0040-audit-secret-pattern-selection.md`（冒頭欄は直したが、帰結は据え置き）
- **指摘した観点**: フェーズスコープ / 設計契約との整合 / 決定記録との整合（3観点から独立に指摘）
- **累計**: 3回目
- **何が起きているか**: 冒頭欄は `**正本**:` → `**実装**:` へ直したが、帰結は「`scripts/audit-public-data.mjs` の `STATIC_LEAK_PATTERNS` を**正本とする**」のまま。DR-0039 側に同種の記述は無い（grep 0件）。新設の検査は `## ` が現れるまでの冒頭欄しか読まないので、この行は検査の外にあり緑のまま通る。
- **なぜ問題か**: 同じ DR の中に「実装」と「正本」の二つの宣言が同居する。DR-0054 の帰結は「決定の内容は変えていない」と書くが、帰結節に置かれた宣言は決定内容そのものだ。DR-0040 の別の帰結は「変更はこの DR の『決定』を更新して行う」と書いており、正面から食い違う。
- **失敗シナリオ**: `STATIC_LEAK_PATTERNS` を変更する人が帰結を読み、「コードが正本だから DR は直さなくてよい」と判断する。
- **提案**: 帰結から「正本とする」を落とし、在り処の記述へ直す。DR-0054 の帰結も実態に合わせる。

### `decisions/implementation-labeled-as-canonical`（同一カテゴリのため計上は1回） — DR-0046 の同型を取りこぼしている

- **場所**: `docs/decisions/0046-prose-checked-for-canonical-duplication.md`（`**正本**:` 行）、`docs/decisions/0054-decision-reference-checked-by-machine.md`（決定2）
- **指摘した観点**: 決定記録との整合
- **何が起きているか**: 決定2 は「値がコードの中にしか無い場合も例外にしない。**その2本**はこの PR で直した」と書くが、DR-0046 の `**正本**:` 行は「走査する場所は同スクリプトの `SCAN_ROOTS`」と、実装コードの中の識別子を正本として指定している。ファイル名を書かず識別子名で書いているため、拡張子判定に掛からず機械でも捕まらない。
- **なぜ問題か**: 決定2 が「例外にしない」と宣言した対象が1本残る。しかもこの書き方（ファイル名を書かずに「同スクリプトの `XXX`」と書く）は、決定2 の禁止を回避する定型として後続へ広がりうる。
- **失敗シナリオ**: 該当なし（設計上の問題）。
- **提案**: DR-0046 も同じ扱いに揃えるか、「これは判定の調整であり正本ではない」という切り分けを決定2 へ明記する。

### `writing/inaccurate-rationale` — 「この検査は DR へのリンクだけを見る」が、実装の挙動と逆

- **場所**: `docs/decisions/0054-decision-reference-checked-by-machine.md`（決定1・却下理由）、`scripts/check-decisions.mjs`（`checkLinksResolve`）
- **指摘した観点**: 日本語ドキュメント / フェーズスコープ（2観点から独立に指摘）
- **累計**: 18回目
- **何が起きているか**: `checkLinksResolve` は `.md` で終わる相対リンクを**すべて**解決して実在を確かめる。DR 番号による絞り込みは無く、番号の一致判定だけが DR 固有だ。走査対象の相対 `.md` リンク652本のうち102本はリンクテキストに DR 番号を持たないが、それらも実在を要求される。一方 DR-0054 は「この検査は DR へのリンクだけを見る」と書き、それを却下理由（文書全体のリンク検査は別の仕事）にしている。
- **なぜ問題か**: 却下理由は「この却下で何が起きないか」の記録で、後から範囲を広げるときの出発点になる。実装が既に却下した側の挙動を持っていると、次に立てる人は「まだ無い」前提から始めて二重に実装する。
- **失敗シナリオ**: DR と無関係の相対リンクを壊す → `decisions:check` が落ちる → DR-0054 に「DR へのリンクだけを見る」と書いてあるので、別の原因を探しにいく。
- **提案**: 決定1と却下理由を実装に合わせる（走査対象の中の相対 `.md` リンクはすべて見る、番号の一致判定は DR リンクに限る）。却下案は「走査対象の外へ広げること」に絞る。

### `writing/status-overclaims-coverage` — 「ルール化済み」が、カテゴリ全体を覆うかのように書かれている

- **場所**: `docs/review_log.md`（`decisions/citation-points-to-wrong-file` の状態欄）、`scripts/check-decisions.mjs`（冒頭の一覧）
- **指摘した観点**: 日本語ドキュメント
- **累計**: 1回目（新規）
- **何が起きているか**: 状態欄を「ルール化済み」としたが、このカテゴリの要約は「引用したファイル・**節**が、実際にはその内容を持たない」で、節の内容の突き合わせは判定していない。同じ PR の隣接行は同種の部分被覆を「一部（…は人が読む）」と書いている。スクリプト冒頭の一覧も `wrong-dr-citation`（実在する誤番号は見ない）と `index-section-mismatch`（節の分類は見ない）を無条件に並べている。あわせて、この表は既存の状態を「仕組み化済み」と呼んでおり、「ルール化済み」は3つ目の呼び名になる。
- **なぜ問題か**: 状態欄は「このカテゴリはもう人が見なくてよいか」の判断材料だ。過大に書くと、レビュー側が観点から落とす。
- **失敗シナリオ**: `decisions:check` が緑 → 状態欄が「ルール化済み」 → レビューが観点から落とす → 「DR が『`DESIGN.md` の余白節にある』と書き、その節に該当記述が無い」という元の壊れ方が誰にも見られなくなる。
- **提案**: 隣接行と同じ「一部（…は人が読む）」へ揃え、語彙も「仕組み化済み」に統一する。スクリプト冒頭の一覧にも覆う範囲を添える。

### `decisions/index-section-mismatch` — 索引の登録先が「実験と運用」で、内容（`pnpm check` の検査）と合わない

- **場所**: `docs/decisions/README.md`
- **指摘した観点**: 決定記録との整合
- **累計**: 4回目
- **何が起きているか**: DR-0054 を「実験と運用」節の末尾へ置いた。だが決定1 は `pnpm check` へ検査を1本足すもので、同型の DR-0046 は「検査」節にある。索引の「検査」節には DR-0028 / DR-0046 が並び、どちらも DR-0054 が関連欄に挙げている。
- **なぜ問題か**: 同型の決定が別節に散ると、索引から「どんな検査が `pnpm check` にあるか」を引けなくなる。
- **失敗シナリオ**: 該当なし（設計上の問題）。
- **提案**: 「検査」節へ移す（DR-0046 の次で、番号の昇順も満たす）。

### `decisions/consequence-not-followed` — DR-0028 の帰結「どの段に入るかを併せて決める」を満たしていない

- **場所**: `docs/decisions/0054-decision-reference-checked-by-machine.md`（決定1・帰結）、`package.json`
- **指摘した観点**: 決定記録との整合
- **累計**: 12回目
- **何が起きているか**: DR-0028 の帰結は「検査を足す Issue は、`pnpm check` のどの段に入るかを併せて決める」と定める。DR-0054 は「DR-0028 に従って `pnpm check` から呼ぶ」と書くだけで段を決めていない。実際の位置（`canonical:check` の直後）は `package.json` にしか無い。同じ経路を通った DR-0046 / DR-0039 は段と根拠を帰結に書いている。
- **なぜ問題か**: DR-0028 を明示的に引用しながら、その帰結の要求を満たしていない。**しかもこの PR 自身が、このカテゴリを「機械化せず人が見る」と決めた当のものだ。**
- **失敗シナリオ**: 該当なし（設計上の問題）。将来 `pnpm check` の並びを整理する人が `decisions:check` を build 後段へ移しても、DR に反したことにならない。
- **提案**: 「ファイルを読むだけで走りビルド出力を要さないので、静的検査段（`canonical:check` の直後）に置く」を帰結へ足す。

### `decisions/cited-tally-not-reproducible` — 「累計11回で最多」の母集団が書かれていない

- **場所**: `docs/decisions/0054-decision-reference-checked-by-machine.md`（決定4・却下案）
- **指摘した観点**: 決定記録との整合 / 設計契約との整合（2観点から独立に指摘）
- **累計**: 2回目
- **何が起きているか**: 「`decisions/consequence-not-followed` は累計11回で最多」と2箇所に書いたが、`docs/review_log.md` の全カテゴリ表では `inspection/rule-has-bypass` 18、`writing/inaccurate-rationale` 17、`writing/term-inconsistency` 15、`decisions/undocumented-decision` 15 が上にある。`decisions/` 系に絞っても最多は `undocumented-decision` の15。DR-0044 は「単一カテゴリの最多は `inspection/rule-has-bypass`」と書いており、正面から食い違う。
- **なぜ問題か**: 決定4 と却下案の両方が、この「最多」を効果の大きさの根拠にしている。母集団が無いと根拠の当否を確かめられない。
- **失敗シナリオ**: 該当なし（設計上の問題）。
- **提案**: 母集団を明示するか、「最多」を落として「累計11回」だけにする。

### `writing/failure-message-lacks-resolved-path` — NG 行が解決後のパスを出さず、階層を人が数え直すことになる

- **場所**: `scripts/check-decisions.mjs`（リンク先が実在しないときのメッセージ）
- **指摘した観点**: 日本語ドキュメント
- **累計**: 1回目（新規）
- **何が起きているか**: 書かれたままの相対パスだけを出し、`normalize(join(...))` で解決した結果を捨てている。他の失敗メッセージは直し方まで示している（昇順違反は実際の並びを出す、正本の違反は「在り処を示すなら **実装** を使う」と書く）。
- **なぜ問題か**: DR-0054 の理由がまさに「リンクの階層は人が数えにくい。機械なら解決して確かめるだけで済む」だ。解決結果を捨てると、直す人は結局手で数えることになり、この検査が肩代わりしたはずの作業が戻ってくる。
- **失敗シナリオ**: `../../../docs/decisions/0052-x.md` が実在しないと出る → 読んだ人はファイル名の綴りを疑い、合っていると結論する → 階層のずれに気づくまで往復する。
- **提案**: 解決後のパスを添える（`（解決先: .claude/docs/decisions/0052-x.md）`）。値は既に手元にある。

### `writing/test-title-ambiguous` — テストタイトルが、実際に固定している不変条件より広い

- **場所**: `scripts/check-decisions.test.mjs`（走査対象のテスト、`extractPaths` のテスト）
- **指摘した観点**: 日本語ドキュメント
- **累計**: 9回目
- **何が起きているか**: 「走査対象に Skill・DR・**スクリプト**を含む」と書いているが、アサーションは2つだけで `scripts/` を確かめていない。「パスでないコード片は拾わない（関数名・**節の名前**）」も、渡しているのは関数名と定数名で、節の名前のケースが無い。
- **なぜ問題か**: タイトルは「このテストが落ちたら何が壊れたか」の唯一の説明だ。3つ挙げて2つしか固定していないと、`SCAN_ROOTS` から `scripts` を外す変更が緑のまま通る。
- **失敗シナリオ**: `SCAN_ROOTS` から `scripts` を落とす → テストは緑のまま → `scripts/README.md` の DR 参照が無検査になる。
- **提案**: アサーションを足すか、タイトルを実態へ狭める。

## consider

### `code/duplicate-resolve-readjson-helpers` — 走査基盤が `check-canonical-duplication.mjs` から複製されている

- **場所**: `scripts/check-decisions.mjs`、`scripts/check-canonical-duplication.mjs`、`scripts/lib/fs-walk.mjs`
- **指摘した観点**: コード品質
- **累計**: 3回目
- **何が起きているか**: `REPO_ROOT` の求め方とその根拠コメント、`resolve`、走査の列挙手順、`escapeForRegExp` の再実装（`*` を外した差分のみ）が2本目のコピーになった。`scripts/lib/fs-walk.mjs` は冒頭で「片方だけ更新して食い違うことを防ぐ」ための置き場だと宣言している。
- **なぜ問題か**: 同じ形の事故（片方だけ更新して穴が空く）が、このリポジトリで既に3度起きている。
- **失敗シナリオ**: 該当なし（設計上の問題）。
- **提案**: `REPO_ROOT` / `resolve` / 走査の列挙を `scripts/lib/` へ出す。`SCAN_ROOTS` 自体は別でよいが、違いが意図であることを一度だけ書く。

### `inspection/rule-has-bypass`（同上） — 拾わないリンク記法・番号記法がある

- **場所**: `scripts/check-decisions.mjs`（`MARKDOWN_LINK`、`DR_REFERENCE`）
- **指摘した観点**: 検査ルールの実効性
- **何が起きているか**: タイトル付きリンク `[DR-0052](./missing.md "決定記録")`、参照リンク `[DR-0052][dr52]`、`<>` 囲み、リンクテキストの入れ子角括弧、番号だけのリンク `[0052](...)`、桁数違いの `DR-00541`——いずれも失敗0件で沈黙する。
- **なぜ問題か**: 検査の価値は「その書き方を使うと必ず落ちる」ことにある。タイトル付きリンクと `<>` 囲みは Markdown として正当で、意図せず発生しうる。
- **失敗シナリオ**: 関連欄に `[DR-0052](./0052-typo.md "レビューループ")` と書く → 1件も返さず緑。
- **提案**: 正規表現を広げるか、拾えない書き方をコメントへ明記する。現在のコメントは「このリポジトリのリンクはすべてその形」と書いているが、それを保証する検査は無い。

### `inspection/rule-has-bypass`（同上） — 索引の昇順検査が `###` 以外の見出しを見ない

- **場所**: `scripts/check-decisions.mjs`（`checkIndexCovers` の節の区切り）
- **指摘した観点**: 検査ルールの実効性
- **何が起きているか**: 節は `/^###\s+(.+)$/` でのみ区切られ、最初の `###` より前の索引行はどの節にも入らず昇順検査を受けない。`##` の下に並べた行も同様。
- **なぜ問題か**: 索引の構造を少し変えただけで無言で効かなくなる。
- **失敗シナリオ**: 「最近の決定」節を `##` で足して行を並べる → 中が降順でも緑。
- **提案**: 節に属さない索引行を落とすか、`^#{2,4}\s+` を区切りとして扱う。

### `writing/scope-claim-mismatches-content` — 帰結の範囲宣言が、実際の検査項目より狭い

- **場所**: `docs/decisions/0054-decision-reference-checked-by-machine.md`（帰結）、`scripts/check-decisions.mjs`（冒頭・`runChecks` のラベル）
- **指摘した観点**: 日本語ドキュメント
- **何が起きているか**: 帰結は「参照が指す先が実在し、番号が一致していることだけを見る」と書くが、冒頭欄の検査は必須欄の有無・日付の書式・自己参照・拡張子も落とす。**自己参照の禁止はテストで固定されているのに、DR にも docstring にも書かれていない。**
- **なぜ問題か**: 決定1（4項目）と帰結（参照と番号だけ）が同じ DR の中で食い違い、どこにも書かれていない規則が緑/赤を決めている。
- **失敗シナリオ**: 関連欄に自分自身を書いた人が落ちる → DR を読んでもその規則が無い → 検査のバグだと考える。
- **提案**: 帰結を実際の項目に合わせ、自己参照を決定1 と docstring へ書く。

### `decisions/decision-lacks-recorded-basis` — 冒頭欄の書式検査（必須欄・日付）に根拠が無い

- **場所**: `docs/decisions/0054-decision-reference-checked-by-machine.md`（決定1・文脈・理由）
- **指摘した観点**: 日本語ドキュメント
- **何が起きているか**: 文脈が挙げる累計は参照と索引の話だけで、「冒頭欄が欠けた」「日付の書式が揃わない」という指摘の記録は挙がっていない。理由の5項目もそこに触れない。`docs/review_log.md` には `decisions/citation-incomplete-after-rewrite`（累計3）があり、この検査が部分的に覆う位置にあるが、文脈でも状態欄でも触れていない。
- **なぜ問題か**: 必須欄の集合や日付書式を変えたい人が、DR を読んでも判断できない。
- **失敗シナリオ**: 該当なし（設計上の問題）。
- **提案**: 文脈へ `citation-incomplete-after-rewrite` を足し、理由を1項目書く。状態欄も更新するか、しない理由を書く。

### `decisions/consequence-omits-decision` — 決定3 に対応する帰結が無く、決定4 との書き分けが非対称

- **場所**: `docs/decisions/0054-decision-reference-checked-by-machine.md`（決定3・帰結）
- **指摘した観点**: 決定記録との整合
- **累計**: 1回目（新規）
- **何が起きているか**: 決定4 は理由・却下案・帰結の3点が揃うが、決定3 は帰結に対応項目が無い。実際にはこの PR で `decisions/one-sided-coupling` の状態が決定3 由来で変わり、決定1 由来でも3行が変わっている。帰結はそのうち決定4 由来の1件しか書いていない。
- **なぜ問題か**: 「この DR を根拠に何が変わったか」を DR から辿れない。記録漏れか意図的な省略かを判別できない。
- **失敗シナリオ**: 該当なし（設計上の問題）。
- **提案**: 状態欄が変わった5行を帰結へ1項目でまとめる。

### `decisions/dr-restates-canonical-value` — 判定の調整値（拡張子の一覧）を決定本文へ書いており、DR-0046 の扱いと割れる
 
- **場所**: `docs/decisions/0054-decision-reference-checked-by-machine.md`（決定2）、`scripts/check-decisions.mjs`（`IMPLEMENTATION_EXTENSIONS`）
- **指摘した観点**: 決定記録との整合
- **累計**: 12回目
- **何が起きているか**: 決定2 が拡張子の一覧を書き、実装も同じ一覧を持つ。DR-0046 は同種の判定パラメータについて「判定の調整は実装側の裁量で、正本でも決定でもない」と明示している。
- **なぜ問題か**: 次に `.cjs` や `.mts` を足す人が、DR を更新すべきか実装だけでよいかを判断できない。
- **失敗シナリオ**: 該当なし（設計上の問題）。
- **提案**: 「判定の軸を拡張子に取る」までを決定とし、一覧は実装へ。残すなら「拡張子を足す変更はこの決定を更新して行う」を帰結へ書く。

### `contract/format-requirement-duplicated-outside-format-file` — README の規則の理由が3箇所に並ぶ

- **場所**: `docs/decisions/README.md`、`docs/decisions/0054-decision-reference-checked-by-machine.md`（理由）、`scripts/check-decisions.mjs`（コメント）
- **指摘した観点**: 設計契約との整合 / 決定記録との整合（2観点から独立に指摘）
- **累計**: 2回目
- **何が起きているか**: 決定2 は「この規則自体は README が既に書いている」と正しく切り分けているが、理由文とスクリプトのコメントが README の文言をほぼ逐語で再掲している。
- **なぜ問題か**: README 側を直したときに2箇所が古くなる。
- **失敗シナリオ**: 該当なし（設計上の問題）。
- **提案**: どちらも参照へ縮める。

## 落とした指摘

- **索引検査の中身が #45 のやること（節の分類の一致）と入れ替わっている**（フェーズスコープ、consider）— 指摘としては成立するが、提案が「このまま入れてよい。DR へ一行足すだけ」であり、`writing/scope-claim-mismatches-content` と `decisions/decision-lacks-recorded-basis` の提案に含まれる。二重に数えない
- **`scripts/README.md` の表に `check-decisions.mjs` の行が無い**（コード品質が観点外として自主的に落としたもの）— 統合側で拾う。次の周の修正に含める

## ルール化候補

新たに閾値へ達したカテゴリは無い。

- `code/unclosed-fence-disables-remaining-checks` — 通算1回。**これ自身が機械で落とせる**。PR #60 の `code/unclosed-fence-swallows-trailing-prose` と同じ原因で、`stripCodeBlocks` に数行足せば両方を捕まえられる。この PR の blocker の提案がそのまま候補になっている
