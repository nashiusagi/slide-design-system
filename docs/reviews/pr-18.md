# PR #18 レビュー

- **タイトル**: 足場: pnpm workspace + Vite + React + TypeScript
- **レビュー日**: 2026-09-06
- **対象**: main...feature/2_scaffold-vite-react-ts
- **変更ファイル数**: 18

## 要約

blocker 0 件、should 4 件、consider 7 件。`pnpm check`（typecheck / lint / test / build）は全て緑で通ることをレビュアーが実行して確認している。指摘は「足場そのものが壊れている」型ではなく、**この足場の上に載る検査が効かなくなる穴**（`packages/*` が検査対象外、インライン `eslint-disable` が通る、warn がゲートを落とさない）と、**足場の技術選定が DR に残っていない**ことに集中した。

## blocker

なし。

## should

### `code/check-skips-workspace-packages` — `pnpm check` が workspace パッケージを型検査もテストもしない

- **場所**: `package.json`:17（`check` / `test`）、`tsconfig.json`:3-6、`vite.config.ts`:11、`pnpm-workspace.yaml`:2
- **指摘した観点**: コード品質、検査ルールの実効性（2観点から独立に挙がった）
- **累計**: 1回目
- **何が起きているか**: `pnpm-workspace.yaml` に `packages/*` を宣言し、`eslint.config.js` のコメントが契約検査プラグインを `packages/eslint-plugin-slide` に置くと明記している。一方 `tsc -b` の references は app / node の2つのみ、vitest の `include` は `src/**/*.test.{ts,tsx}`、`test` は `vitest run`（`-r` なし）。検査レビュアーが `packages/probe/probe.test.ts` に必ず失敗するテストを置いて `vitest run` した結果、`Test Files 1 passed (1)` で exit 0 だった。`packages/` を見るのは `eslint .` だけ。
- **なぜ問題か**: DR-0011 の lint 5 ルールの実効性を担保するのは RuleTester の invalid ケースだけであり、その置き場所が丸ごとテストランナーと型検査の視界の外にある。README は「`pnpm check` が受け入れの入口になる」と宣言しているのに、検査基盤そのものが入口を通らない。
- **失敗シナリオ**: 後続 Issue で `packages/eslint-plugin-slide/src/rules/no-raw-color.ts` と invalid ケース付きのテストを追加する → 実装をミスして何も報告しないルールになっても、テストは実行されず型エラーも見られないので `pnpm check` は緑 → 生の色値が素通りしたまま「全ルール pass」で採点される。
- **提案**: vitest の `include` を `{src,packages}/**/*.test.{ts,tsx}` へ広げる（または `pnpm -r --if-present test`）、`tsconfig.json` の references にパッケージを足す運用を明記する。今回閉じないなら「今の `check` は `packages/` を型検査・テストしない」ことを残す。

### `inspection/rule-has-bypass` — インライン `eslint-disable` で契約ルールを一行ずつ無効化できる

- **場所**: `eslint.config.js`:9-30
- **指摘した観点**: 検査ルールの実効性
- **累計**: 2回目
- **何が起きているか**: flat config に `linterOptions.noInlineConfig` も `reportUnusedDisableDirectives` も無い。`src/__probe.tsx` に `/* eslint-disable @typescript-eslint/no-unused-vars */` を付けた違反コードを置いて `eslint` を実行した結果、報告 0 件・exit 0 だった。
- **なぜ問題か**: 検査対象は AI が生成したスライドソースであり、生成物に抑止コメントが混ざりうる。抑止が効くなら `no-raw-color` / `layout-approved` / `deck-conformance` はいずれも「書き方を変えると素通りする」ルールになる。さらに disable された行は報告に残らないため、DR-0011 の帰結にある「実行されたルール ID の集合を残す」方式でも検出できない（ルール自体は実行済み扱いになる）。
- **失敗シナリオ**: 生成された `src/decks/foo/Slide03.tsx` に `// eslint-disable-next-line slide/no-raw-color` と `color: '#ff0000'` が入る → `pnpm lint` 通過 → 契約外の色を持つスライドが pass として採点される。
- **提案**: スライドソースに当たるファイル群へ `linterOptions: { noInlineConfig: true, reportUnusedDisableDirectives: 'error' }` を設定する。

### `writing/ambiguous-criterion` — 判断基準が曖昧で、契約として実行できない（2箇所）

- **場所**: `docs/decisions/0026-typescript-5-for-eslint-ast.md`:37、`README.md`:31
- **指摘した観点**: 日本語ドキュメント
- **累計**: 3回目（ルール化候補）
- **何が起きているか**:
  1. DR-0026 の帰結が「バージョンの上げ下げを検討するときは、まず `pnpm lint` が動くかを基準にする」と書いている。「動くか」は (A)「`eslint .` が exit 0 を返す」とも (B)「自作ルールが違反を実際に検出できる」とも読める。同じ DR の却下理由は (A) の状態こそ危険だと述べているので、読み方が割れると DR が実効を失う。
  2. README の「契約に基づく検査（lint / measure）は、この枠の中へ後から足していく」の「枠」が、`pnpm check` のコマンド列とも pnpm スクリプト群とも読める。measure は `dist/` に対して実行する（DR-0011 / DR-0022 の帰結）ため、現在の並び（build が最後）では前者の読みで実装すると空振りする。
- **なぜ問題か**: どちらも後続 Issue の実装方針を直接左右する一文である。
- **失敗シナリオ**: TypeScript を上げる Issue で `pnpm lint` の exit 0 だけを見て「基準を満たした」と判断し、パーサ変更で自作ルールが1件も発火しなくなっていることを見逃す。measure 実装の Issue で `check` の途中に measure を挿入し、`dist/` が無い状態で常に fail または空振りする。
- **提案**: DR-0026 は検出側で基準を書く（プラグイン実装前の現時点では「`pnpm lint` が正常終了し、かつ `typescript-eslint` の peer 範囲を満たすこと」に限定する）。README は「lint は `check` の lint 段へ、measure は build 後に走る段として足す」と実行位置を明示する。

### `decisions/undocumented-decision` — 足場の技術選定と `pnpm check` の設計が DR に無い

- **場所**: `package.json`:6-8,20-35、`pnpm-workspace.yaml`、`packages/.gitkeep`、`README.md`:22,28,31
- **指摘した観点**: 決定記録（DR）
- **累計**: 3回目（ルール化候補）
- **何が起きているか**: pnpm workspace（`packages/*` に ESLint プラグインを置く）、テストスタック（Vitest + jsdom + Testing Library）、Node.js 22 下限、そして「受け入れの入口を `pnpm check` 単一コマンドに集約し、typecheck → lint → test → build の順で直列に繋ぐ」という判断が、どの DR にも無い。理由が残っているのは Issue #2 の一行と README だけ。DR-0026（TypeScript のバージョン）という細かめの決定は DR にした一方でこれらを落としているため、記録の粒度も不揃いになっている。あわせて、直下の足場と DR-0021 の共通 starter（`experiments/`）の関係——再利用か、コピーか、生成か——も未記録のまま先送りされている。
- **なぜ問題か**: いずれも他の選択肢が実在する判断である（単一パッケージ / Jest / Node 20 / 検査ごとに別コマンド）。`pnpm check` は DR-0011 の帰結（実行ルール ID の集合、measure はビルド出力に対して実行）が実装される土台そのものであり、根拠が README にしか無いと、後続の変更が突き合わせなしに起きる。starter との関係は、複製になった場合に DR-0021 の公平性の根拠（starter は完全に同一）を静かに壊す。
- **失敗シナリオ**: 半年後に「なぜ workspace か」「なぜ Vitest か」を再議論する。`experiments/starter/` にビルド設定をコピーして作り、直下だけ更新した結果、Baseline と Harness が異なるビルド条件で走る。
- **提案**: (1) 足場の構成（workspace / テストスタック / Node 下限）、(2) 検査の実行口を `pnpm check` に一本化する決定、の 2 本を DR に起こし、却下案と順序の理由を書く。starter との関係は (1) の帰結に一文残す。

## consider

### `contract/contract-structure-duplicated` — DR-0026 が lint ルール ID の一覧を書き写している

- **場所**: `docs/decisions/0026-typescript-5-for-eslint-ast.md`:12（既存の一覧は `docs/decisions/0011-lint-and-measure.md`:19-23、正本は `design/rules.json`）
- **指摘した観点**: 設計契約
- **累計**: 2回目
- **何が起きているか**: 新規 DR が lint 側 5 ルールの ID をすべて列挙している。同じ列挙は DR-0011 にあり、その DR 自身が正本を `design/rules.json` と宣言している。
- **なぜ問題か**: ID の追加・改名時に照合されない写しが増える。DR-0026 の論旨は ID を列挙しなくても DR-0011 への参照だけで成立する。
- **失敗シナリオ**: 該当なし（設計上の問題）。ID を改名したとき DR-0026 だけ古い記述が残る。
- **提案**: 列挙をやめ、DR-0011 と正本への参照に置き換える。

### `phase/out-of-scope-addition` — 足場の CSS が #4 の持つデザイン値とクラス名を先取りしている

- **場所**: `src/index.css`:5-8、`src/App.tsx`:3
- **指摘した観点**: フェーズスコープ、設計契約（2観点から挙がった）
- **累計**: 3回目（ルール化候補）
- **何が起きているか**: `:root` に `font-family: system-ui, sans-serif` と `line-height: 1.5` を直接書いている。これらは Issue #4 の `design/tokens.json` の `type` トークンが正本になる値で、Issue #2 の非スコープに「デザイントークン・契約（#4, #5）」が明記されている。`App.tsx` の `className="app"` に対応する CSS 規則も無く、DR-0018 は「クラス名は契約が定める」としている。
- **なぜ問題か**: #4 で `theme.css` を生成した後、`index.css` の指定が残ったまま両方効く状態になりうる。手書きの `index.css` 側は lint の想定対象外なので、検査は pass したまま乖離だけが残る。
- **失敗シナリオ**: トークンを変えても表示が変わらない、または読み込み順に依存して結果が変わる。
- **提案**: `src/index.css` は `body { margin: 0 }` だけにし、`className="app"` も外す。残すなら「#4 でこの2行を削除する」と削除条件を明記する。

### `inspection/lint-warning-not-failing` — `eslint .` に `--max-warnings=0` が無く、warn 級のルールはゲートを落とさない

- **場所**: `package.json`:15
- **指摘した観点**: 検査ルールの実効性
- **累計**: 1回目
- **何が起きているか**: ESLint は warn だけなら exit 0 を返す。現時点で `typescript-eslint` の recommended に warn 級のルールが含まれていないことは確認済みなので、今すぐ壊れてはいない。
- **なぜ問題か**: DR-0011 は `design/rules.json` に severity を持たせる設計であり、実装側が warn で登録された瞬間にゲートとして無効になる。
- **失敗シナリオ**: 後続 Issue で `no-raw-scale` を warn で登録する → 違反ありでも `pnpm check` は緑。
- **提案**: `"lint": "eslint . --max-warnings=0"`。

### `code/tsconfig-include-no-op` — `tsconfig.node.json` の `include` にある `eslint.config.js` が実際には対象になっていない

- **場所**: `tsconfig.node.json`:17
- **指摘した観点**: コード品質
- **累計**: 1回目
- **何が起きているか**: `allowJs` が無いため tsc は `.js` を入力として拾わない。`tsc -p tsconfig.node.json --listFiles` の出力は `vite.config.ts` の1件だけ。エラーも警告も出ないので、設定を読むと検査されているように見える。
- **なぜ問題か**: 検査範囲の見かけと実態がずれる。
- **失敗シナリオ**: `eslint.config.js` で存在しないエクスポート名を import しても `pnpm typecheck` は緑のまま通り、失敗地点が lint へずれる。
- **提案**: `include` から外すか、`allowJs` / `checkJs` を足すか、`eslint.config.ts` にする。

### `decisions/dr-restates-canonical-value` — DR-0026 が peer 範囲とバージョン制約の値を本文へ書き写している

- **場所**: `docs/decisions/0026-typescript-5-for-eslint-ast.md`:11,17
- **指摘した観点**: 決定記録（DR）
- **累計**: 2回目
- **何が起きているか**: `typescript-eslint` の peer 範囲と、採用する `^5` を本文に直接書いている。前者の正本は upstream の `peerDependencies`、後者は `package.json`。`**正本**:` 行は無い。
- **なぜ問題か**: upstream の peer 範囲は次のマイナーで動く値であり、DR 側だけ古い数字が残ると、この DR が想定する見直しの判断材料が誤った値になる。
- **失敗シナリオ**: `typescript-eslint` が peer 範囲を広げた後も DR は旧値のままで、DR だけを読んだ人が解消済みの制約を根拠に判断する。
- **提案**: 数値を残すなら時点を明記し、`**正本**:` 行で `package.json` を指す。

### `writing/term-inconsistency` — 「受け入れの入口」という新しい語が、issue-workflow の「完了条件」と別語のまま置かれている

- **場所**: `README.md`:31（`.claude/skills/issue-workflow/SKILL.md`:33,69）
- **指摘した観点**: 日本語ドキュメント
- **累計**: 3回目（ルール化候補）
- **何が起きているか**: README が「受け入れ」という語を初めて使う。開発フローの正本である issue-workflow は「完了条件」を Draft 解除の基準としており、「『動くかどうか』を基準にしない」と明記している。
- **なぜ問題か**: 「`pnpm check` が通れば受け入れ判定を通過できる」と読まれると、Issue の完了条件を突き合わせずに Draft を外す判断が起こりうる。
- **失敗シナリオ**: 該当なし（運用上の問題）。
- **提案**: 「`pnpm check` が通ることはどの Issue でも共通の必要条件。個々の受け入れ判断は Issue の完了条件で行う」と既存語に寄せる。

### `writing/notation-inconsistency` — README の DR 参照がコードブロック内の裸書きになっている

- **場所**: `README.md`:27（既存の書式は 18, 62, 70, 76 行）
- **指摘した観点**: 日本語ドキュメント
- **累計**: 3回目（ルール化候補）
- **何が起きているか**: 他の節はすべて節末に「根拠は [DR-00NN](...)。」とリンク付きで置いているが、今回の参照はコードブロック内のコメントに `（DR-0022）` と裸で書かれ、リンクにならない。
- **なぜ問題か**: DR への到達方法が README 内で二通りになる。
- **失敗シナリオ**: 該当なし。
- **提案**: コメントからは外し、ブロック直後の本文へリンク付きで置く。

## 落とした指摘

なし。6観点から挙がった指摘はすべて、統合時にファイルを読んで成立を確認した。重複は 2 件あり、いずれも統合した（`packages/*` が検査対象外＝コード品質 + 検査、足場 CSS の値の先取り＝フェーズ + 設計契約）。

## ルール化候補

このレビューで累計3回に達したカテゴリ。**いずれもドキュメント・運用の問題であり、`design/rules.json`（スライド生成物の検査）へそのまま載る性質のものではない。** 機械化するならリポジトリ側の検査（DR とドキュメントの整合を見るスクリプト）になる。判断は人が行う。

- `writing/ambiguous-criterion` — 通算3回。「動く」「枠」のような多義語を基準の位置で使わない、という規約は自動判定が難しい。判定できるとすれば、DR の「帰結」節に検証手段（コマンドまたはテスト）が書かれているかの検査。
- `decisions/undocumented-decision` — 通算3回。PR 差分に新しい依存関係や新しい npm スクリプトが入ったとき、同じ PR で `docs/decisions/` に追加または更新があるかを見る検査にできる。
- `phase/out-of-scope-addition` — 通算3回。Issue の「非スコープ」節に挙がったパス・語が差分に現れていないかの検査にできるが、今回のように「値の種類」で違反する場合は捕まえにくい。
- `writing/term-inconsistency` — 通算3回。用語集を正本として持ち、新出の語を検出する形なら機械化できる。
- `writing/notation-inconsistency` — 通算3回。`DR-\d{4}` がリンクになっているかの検査は容易で、これが5件の中では最も機械化しやすい。

---

## 2周目（2026-09-06）

対象は1周目のレビュー時点（`c983b2c`）からの増分。実装コードを含むため観点は絞らず6観点、モデルは Sonnet。

**1周目の指摘（should 4 / consider 7）はすべて解消を確認した。** 検査・コード品質の2観点が、それぞれ独立に一時ファイルを置いて実測している（`packages/*` のテストと型エラーが検出される、`src/**` のインライン `eslint-disable` が無効化される、warn で `pnpm check` が落ちる、`eslint.config.js` が `tsc` の入力に入る）。DR整合・フェーズスコープ・コード品質の3観点は指摘ゼロ。

新たに出たのは should 1 件、consider 2 件。

### should

#### `writing/term-inconsistency` — DR-0028 に「受け入れ」が残り、同じ文の中で「完了条件」と揺れている

- **場所**: `docs/decisions/0028-single-check-entry-point.md`:34,39
- **指摘した観点**: 日本語ドキュメント
- **累計**: この PR で計上済み（1周目に計上）
- **何が起きているか**: 1周目の同カテゴリの指摘に対し README（`README.md`:33）は「個々の Issue を終えてよいかは Issue の完了条件で判定する」へ直したが、同じ増分で新設した DR-0028 には「個々の**受け入れ**は Issue の**完了条件**で判定する」と二語が1文に残り、却下理由にも「受け入れの判定」が残っている。issue-workflow の正本（`.claude/skills/issue-workflow/SKILL.md`）は一貫して「完了条件」とだけ呼ぶ。
- **なぜ問題か**: 「受け入れ＝PR のマージ可否」「完了条件＝Issue のチェックリスト」と別の判定軸に読み分けられ、`pnpm check` 通過をもって受け入れ可とする読み替えの余地が残る。修正が README 1 箇所にしか及ばなかった形の再発である。
- **失敗シナリオ**: 後続 Issue で「`pnpm check` が通ったので DR の言う受け入れは満たした」と判断し、完了条件を照合せずに Draft を外す。
- **提案**: DR-0028 の当該2箇所を「Issue を終えてよいか」「Issue の完了条件」に統一する。

### consider

#### `contract/workflow-constant-duplicated` — `packages/README.md` が3つの設定ファイルの glob を書き写している

- **場所**: `packages/README.md`:9-11（正本は `tsconfig.node.json`:19、`eslint.config.js`:31、`vite.config.ts`:11）
- **指摘した観点**: 設計契約
- **累計**: 2回目
- **何が起きているか**: 1周目の `code/check-skips-workspace-packages` への対応として置いた表が、3ファイルの glob の実値をそのまま埋め込んでいる。現時点では一致していることを確認済み。
- **なぜ問題か**: この表は「`packages/` が `pnpm check` の対象に入っている」ことの裏付けとして書かれている。表と実体が食い違えば、読み手は対象に入っていると誤信したまま、実際には検査から外れたコードを緑判定する。1周目に壊れていたまさにその箇所に、再発を検出できない複製を置いたことになる。
- **失敗シナリオ**: 後続 Issue で `test.include` を絞ったが表を更新し忘れ、テストが拾われなくなったことに気づけない。
- **提案**: 「対象の指定」列から実値を消し、参照先ファイルと設定キーだけを示す。

#### `inspection/rule-scope-inconsistent` — `noInlineConfig` の保護範囲が `src/` と `packages/` で非対称になった

- **場所**: `eslint.config.js`:28-36
- **指摘した観点**: 検査ルールの実効性
- **累計**: 2回目
- **何が起きているか**: `noInlineConfig` / `reportUnusedDisableDirectives` は `src/**` ブロックにのみ入り、`packages/**` には無い。レビュアーが `packages/` 側に `eslint-disable` 付きの違反を置いて実測し、報告0件・exit 0 を確認している。
- **なぜ問題か**: `packages/eslint-plugin-slide` は契約検査プラグイン本体とそのテストが置かれる場所で、DR-0011 の実効性はここのコード品質にも懸かる。非対称が意図的かどうかが設定のコメントに書かれていない（`packages/**` のコメントは「Node で動く」としか言っていない）。
- **失敗シナリオ**: プラグイン実装側で抑止コメントを使い、本来落ちるべき警告を隠しても `pnpm lint` が検出しない。
- **提案**: 意図的なら理由をコメントに残す。意図的でないなら同じ `linterOptions` を足す。

### 2周目で落とした指摘

なし。

### 2周目のルール化候補

新たに閾値へ達したカテゴリは無い（`writing/term-inconsistency` は1周目に到達済み。同一 PR では二重に数えない）。
