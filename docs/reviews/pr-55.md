# PR #55 レビュー

- **タイトル**: カタログに #/components（部品一覧・未実装マーカー付き）を追加する
- **レビュー日**: 2026-09-21
- **対象**: main...feature/36_components-catalog-page
- **変更ファイル数**: 14

## 要約

blocker 0件、should 7件、consider 10件。契約を読んで描く部分（4部品の契約表示、未実装の明示、契約を足せば現れる形）は Issue #36 の完了条件に対応しており、契約の値・一覧の複製も無い。指摘が集まったのは**検査が主張どおりに効いていない箇所**と、**同じ PR で書いた DR の帰結を同じ PR が守っていない箇所**である。

とくに、契約の文言の複製検査を全ページへ広げたはずが、文字列を折り返すだけで素通りする。リンクの行き先（`id`）は誰も検査していない。どちらも実測で確認した。

## blocker

なし。

## should

### `inspection/rule-has-bypass` — 契約文言の複製検査が、文字列を割るだけで素通りする

- **場所**: `src/docs/pages/no-contract-prose.test.ts`:52-55
- **指摘した観点**: 検査ルールの実効性、コード品質（あちらは `code/test-cannot-detect-regression` として挙げた。同一箇所・同一機序なので1件にまとめる）
- **累計**: 17回目
- **何が起きているか**: 判定が `expect(source).not.toContain(text)` の部分一致1本なので、契約文言をソース上で分割すると一致しない。レビュアーが実測した。同じ文言を1行で貼ると落ち、JSX 本文として2行に折り返すと**緑のまま通る**。`'A' + 'B'` の連結でも通る。
- **なぜ問題か**: 避けるのに悪意が要らない。長い文言を JSX へ貼れば、このリポジトリ自身の書き方（`Components.tsx` / `Layouts.tsx` の `doc-page__lead` が現に折り返されている）に従った整形で分断される。「守られている」という誤った安心だけが残る。
- **失敗シナリオ**: 次のページを足した人が契約の `role` をリード文へ写し、行長に合わせて折り返す。複製が入ったまま `pnpm check` は緑。契約側だけ直したときに、カタログが古い文言を出し続ける。
- **提案**: 突き合わせの前に、ソース側と契約側の双方から空白・改行を落として正規化する。

### `inspection/rule-scope-inconsistent` — 複製検査の対象が `src/docs/pages/**/*.tsx` だけで、`.ts` と `pages/` の外が外れている

- **場所**: `src/docs/pages/no-contract-prose.test.ts`:22
- **指摘した観点**: 検査ルールの実効性、コード品質
- **累計**: 6回目
- **何が起きているか**: glob が `./**/*.tsx`。コメントは「切り出し先を1段深いディレクトリへ置くだけで同じ抜け道が開く」として `**` にしたと書くが、拡張子と起点の穴が残る。レビュアーが3通り実測し、`src/docs/pages/helper.ts`、`src/docs/DocsApp.tsx` のいずれへ契約文言を置いても**緑のまま通った**。
- **なぜ問題か**: 塞いだと宣言している抜け道が、拡張子を `.ts` にするだけで開く。テスト名も `describe('カタログのページ')` で、`DocsApp.tsx` が対象外である線引きはどこにも書かれていない。
- **失敗シナリオ**: 表の描画ヘルパを `src/docs/pages/tables.ts` へ切り出し、そこへ契約の `usage` を直書きする。複製が入ったまま緑。
- **提案**: 起点を `src/docs` に取り、`**/*.{ts,tsx}` へ広げる（`.test.` の除外はそのまま）。広げない理由があるなら、対象外の範囲をコメントで明示する。

### `phase/completion-criterion-not-verified-e2e` — 新しく張った節リンクの行き先（`id`）を、どのテストも検証していない

- **場所**: `src/docs/pages/Layouts.tsx`:98 / `src/docs/layouts.test.ts`:59-73 / `src/docs/DocsApp.test.tsx`（節スクロールのテスト）
- **指摘した観点**: フェーズスコープ、検査ルールの実効性（あちらは `inspection/link-target-unchecked` として挙げた。同一の問題なので1件にまとめる）、設計契約
- **累計**: 7回目
- **何が起きているか**: `Components.tsx` の `allowedIn` は `#/layouts/<節ID>` を張り、着地点は `Layouts.tsx` の `<section id={layoutSectionId(layout.name)}>` である。`Layouts.tsx` からこの `id` と import を取り除いても、**10 files / 76 tests すべて緑のまま**だった（レビュアーと統合時の双方で実測）。`layouts.test.ts` の往復テストは hash の書式しか見ず、`DocsApp.test.tsx` の節スクロールのテストは `document.body` に自前で作った `<div id="scroll-target">` を使うため、ページ側が `id` を出しているかを踏まない。
- **なぜ問題か**: Issue #36 の完了条件「`allowedIn` から `#/layouts` の該当レイアウトへ移動できる」が、断片に分かれた単体テストだけで、繋がりとしては検証されていない。`layouts.test.ts` のコメントが守ると宣言している失敗（「リンクは付いたまま、押すまで壊れたことが分からない」）が、そのまま無検査で残っている。
- **失敗シナリオ**: `Layouts.tsx` のカードを別コンポーネントへ切り出すときに `id` を落とす。部品ページのリンクを押すとレイアウトページの先頭が出るだけになるが、テストも lint も緑。`DOCS_PAGES` のページ ID を改名した場合も同様。
- **提案**: リンク元とリンク先を1本で結ぶテストを足す。`Components` が出す `href` の集合と、`Layouts` が出す `id` の集合を突き合わせる形にすれば、どちら側が欠けても落ちる。

### `contract/value-outside-source-of-truth` — `#/layouts` のページ ID がリテラルで、`DOCS_PAGES` から引いていない

- **場所**: `src/docs/pages/Components.tsx`:54 / `src/docs/pages/Components.test.tsx`:76（正本は `src/docs/pages.ts` の `DOCS_PAGES`）
- **指摘した観点**: 設計契約
- **累計**: 5回目
- **何が起きているか**: リンクを `formatDocsHash('layouts', ...)` と組み立てており、ページ ID が文字列リテラルで置かれている。テスト側も同じリテラルを書いているので、両方が同時にずれても検査は通る（`components.test.ts` の `CONTRACT_PATHS` がわざと別の綴り方を選んでいるのと逆の形）。
- **なぜ問題か**: DR-0048 決定3 が節 ID について定めた一元化（片方だけ変えるとリンクが黙って外れる）が、ページ ID には適用されていない。
- **失敗シナリオ**: `pages.ts` の `{ id: 'layouts' }` を改名すると、リンクは `#/layouts/...` を出し続け、`resolvePage` が先頭ページへ落とす。リンクは付いたまま「基礎」へ飛ぶ。テストは両側が同じリテラルなので通る。
- **提案**: ページ ID を `pages.ts` 側から引く。少なくともテストは `DOCS_PAGES` から組み立て、リテラルの一致ではなく登録との一致を見る。

### `decisions/consequence-not-followed` — DR-0048 の帰結が要求した節 ID の一元化を、同じ PR の部品ページが守っていない

- **場所**: `src/docs/pages/Components.tsx`:129（`id={component.name}`）/ `src/docs/components.ts`（`componentSectionId` 相当が無い）/ `docs/decisions/0048-docs-hash-carries-section.md`（決定3・帰結）
- **指摘した観点**: 決定記録、設計契約（あちらは `contract/section-id-mapping-not-centralized` として挙げた。同一の問題なので1件にまとめる）
- **累計**: 8回目
- **何が起きているか**: DR-0048 の帰結は「節を持つページを足すときは、節 ID の対応を contract 側の読み込み口（`layoutSectionId` に相当するもの）へ置き、リンク側と `id` 側の両方がそこから引く」と書く。レイアウト側は従っているが、同じ PR で追加した部品ページは `id={component.name}` と直書きで、`components.ts` に対応する関数が無い。`layouts.test.ts` にある往復テストに相当するものも無い。
- **なぜ問題か**: DR の帰結を、その DR を導入した PR 自身が破っている。前例がその場でできると、#37 / #38 が `#/components/<部品名>` へリンクを張る段で、どちらの書き方に倣うかが分からなくなる。
- **失敗シナリオ**: 後から部品契約の `name` が変わり、リンク側と `id` 側の片方だけが直る。リンクは付いたまま先頭ページへ落ちる——DR-0048 がまさに避けようとした壊れ方。
- **提案**: `components.ts` に `componentSectionId` を置き、`Components.tsx` の `id` をそこから引く。往復テストも `layouts.test.ts` と同型で置く。過剰だと判断するなら、DR-0048 の帰結を「リンク先を持つページだけが一元化する」と書き換えて部品ページを例外として明示する。**どちらにせよ DR と実装のどちらかを動かす必要がある。**

### `writing/inaccurate-rationale` — 理由付けが、言語の挙動・実装・同じ DR の別の記述と食い違う（5箇所）

- **指摘した観点**: 日本語ドキュメント
- **累計**: 13回目
- 1. **`docs/decisions/0049-...md`:34, 41 / `src/docs/components.ts`:72-74** — 「既定値にすると呼び出し側が別の表を渡す道が塞がる」。TypeScript の既定値引数は呼び出し側が明示すれば上書きされるので、道は塞がらない。実際に効いている理由（モジュールの束縛を読むため、テストが差し替えた表が反映されない／省略できると何を見ているかがコードから読めない）が書かれていない。
- 2. **`docs/decisions/0049-...md`:65-66 / `src/docs/components.test.ts`:93-95** — 「#37 が実装するとこのテストは落ちる」。落ちる契機は `COMPONENT_PREVIEWS` への**登録**であって実装ではない。同じ DR の帰結1が「登録漏れは機械では捕まらない」と書いているのと食い違う。#37 の担当者が「実装したら検査が知らせてくれる」と受け取る。
- 3. **`docs/decisions/0048-...md`:24, 34 / `src/docs/DocsApp.tsx`:20** — 「節が無ければスクロールせずページの先頭を出す」。実装は何もしないだけで、スクロール位置は直前のまま残る。「何もしない」と「先頭が出る」は別のことで、後者は実装にもテストにも対応先が無い。
- 4. **`src/docs/pages/Components.tsx`:5** — 「ここが持つのは見出しの日本語と、どの図形で描くかだけだ」。`Layouts.tsx` からの写しで、部品ページは図形を描いていない。同じ JSDoc の下にある「部品の見た目をここで先取りしない」とも噛み合わない。
- 5. **`src/docs/components.ts`:6-7** — 「守るのは人である」で切れている。同じ PR が `no-contract-prose.test.ts` を新設して文言の複製を機械で見るようにしたので、何を機械が見て何を見ていないかまで書かないと、2つの新規ファイルが同じ規約について別のことを言う。
- **提案**: いずれも実態に合わせて書き直す。

### `code/test-cannot-detect-regression` — `PropTable` の「必須」列と `AllowedInTable` の「最大数」列は、定数へ固定しても落ちない

- **場所**: `src/docs/pages/Components.tsx`:87, 57 / `src/docs/pages/Components.test.tsx`:38-57, 85-107
- **指摘した観点**: コード品質
- **累計**: 4回目
- **何が起きているか**: テストの期待値を実装と同じ式で組み立てている。いまの契約データは props 4件すべて `required: true`、slots 5件すべて `max: 1` で、値の種類が1つしか無い。実装を定数に置き換えても一致する。レビュアーと統合時の双方で実測し、`<td>{'必須'}</td>` / `<td>{... : 1}</td>` のどちらへ固定しても**全テスト緑**だった。
- **なぜ問題か**: この2列は「カタログが契約から引いている」ことの表示そのもので、テストの主目的にあたる。検出できるかどうかが「データにたまたま種類があるか」だけで決まっている（`AllowedInTable` の必須列は `emphasis` が `required: false` を持つおかげで落ちる）。
- **失敗シナリオ**: 実装が値を見なくなっても黙って通り続ける。将来 `required: false` の prop や `max: 2` のスロットが契約へ入ったときに、初めて表示が嘘だと分かる。
- **提案**: `vi.doMock` で `required: false` の prop と `max` が 1 でないスロットを1件通し、「任意」と当該の数が出ることを固定する。同ファイルは既にこの手を使っている。

## consider

### `contract/design-data-duplicated` — 契約名がテストへリテラルで書き写されている

- **場所**: `src/docs/components.test.ts`:112-113 / `src/docs/pages/Components.test.tsx`:164, 195
- **累計**: 12回目
- **何が起きているか**: `previewFor` のテストは登録表の引き方だけを見ており名前は任意でよいのに、実在の契約名を使っている。`Components.test.tsx` のフィクスチャも実在のレイアウト名を使う。
- **なぜ問題か**: とくに orphan のテストは「`allowedIn` が指すレイアウトにスロットが無い」を見ているつもりだが、`title` が実在しなくなっても `slotOf` は `undefined` を返すため、意図した枝を踏んでいるか区別できないまま通る。
- **失敗シナリオ**: `design/layouts/title.json` を改名すると、orphan のテストは別の事象（レイアウトが実在しない）を見るようになるが、期待値は変わらず通る。
- **提案**: `previewFor` のテストは架空の名前にする（同ファイルの `componentsFrom` のテストが既にその形）。orphan のテストは `LAYOUTS` から組み立てる。

### `contract/allowlist-key-tracks-canonical-values` — `COMPONENT_PREVIEWS` のキーが契約名と一致することを誰も検査しない

- **場所**: `src/docs/components.ts`:67 / `src/docs/components.test.ts`:96-102
- **累計**: 2回目
- **何が起きているか**: DR-0049 は「登録を忘れると未実装と表示され続ける」を帰結に挙げるが、逆（契約名と一致しないキーが入る）には触れていない。いまは表が空で `toEqual({})` が固定しているため起きないが、#37 がこれを書き換えた後は、キーの妥当性を見る検査が残らない。
- **失敗シナリオ**: #37 が `{ 'slide_title': SlideTitle }` と綴り誤って登録すると、実装済みの部品が「未実装」と表示され続け、テストも型検査も通る。
- **提案**: 「`Object.keys(COMPONENT_PREVIEWS)` はすべて `COMPONENTS` の名前である」を1件足す。空の表でも成立し、#37 が埋めた後も残る。

### `decisions/dr-restates-canonical-value` — DR-0048 が `docs/review_log.md` の累計回数を本文へ写した

- **場所**: `docs/decisions/0048-docs-hash-carries-section.md`:34
- **累計**: 8回目
- **何が起きているか**: 理由の3点目が「`writing/dangling-quote-reference` が累計5回出ているのと同じ形」と書く。この「5」は review_log の累計列の写しで、PR ごとに増える値である。累計回数を引用している DR は他に無い。
- **失敗シナリオ**: 該当なし（設計上の問題）。次にこのカテゴリが1件出た時点で、DR-0048 の「5回」だけが取り残される。
- **提案**: 回数を書かず、カテゴリ名と参照先だけを示す。

### `decisions/undocumented-decision` — 契約文言の複製検査を共通ファイルへ移した判断が、どちらの DR にも無い

- **場所**: `src/docs/pages/no-contract-prose.test.ts`（新規）/ `src/docs/pages/Layouts.test.tsx`（同等の検査を削除）
- **累計**: 14回目
- **何が起きているか**: DR-0042 決定2 をテストで支える検査の置き場所が変わり、対象も部品契約へ広がった。選択肢は実在する（ページごとに置く）ことを新ファイルのコメント自身が比較して述べているが、判断は DR に無い。
- **なぜ問題か**: このリポジトリは「DR-0042 決定2 をどこで担保するか」を DR の帰結として書く運用になっている（DR-0042 の帰結、DR-0047 の帰結）。今回だけコード側のコメントに閉じると、#38 でページを足す人が検査の在り処を DR から辿れない。
- **失敗シナリオ**: 該当なし（設計上の問題）。#38 の実装者が旧テストを探して見つからず、ページごとに同種のテストを書き足す。
- **提案**: DR-0048 か DR-0049 の帰結へ1行置く。

### `inspection/rule-coverage-partial` — 複製検査の列挙が契約の `$comment` を含まない

- **場所**: `src/docs/pages/no-contract-prose.test.ts`:26-40
- **累計**: 9回目
- **何が起きているか**: 集めているのは `role` / `whenToUse` / `whenNotToUse` / `classes` / `usage` / `props[].description` で、`$comment` が入っていない。レビュアーが `design/layouts/title.json` の `$comment` を丸ごと貼って実測し、緑のまま通ることを確認した。
- **なぜ問題か**: 「契約が持つ文章」という定義に対し、一部が抜けている。`$comment` は設計意図の説明なので、カタログの解説文へ写されやすい位置にある。
- **提案**: `$comment` も対象へ加えるか、対象外とする根拠をコメントに書く。

### `writing/notation-inconsistency` — `#37` と `#/components` が同じ形で並び、参照の種類が読み分けられない

- **場所**: `src/docs/docs.css`:304-305 / `src/docs/pages/Layouts.tsx`:8-9
- **累計**: 7回目
- **何が起きているか**: 「（実装は #37、一覧は `#/components`）」で、Issue 番号とカタログの hash が同じ形で並ぶ。`Layouts.tsx` の「一覧とプレビューは #36 / #37 が持つ」は #36 のまま残っている（この PR が #36 を実装した）。
- **失敗シナリオ**: 該当なし（設計上の問題）。
- **提案**: 「実装は Issue #37、一覧はカタログの `#/components`」のように種類を語で示す。`Layouts.tsx` の #36 の参照も実態へ合わせる。

### `writing/ungrammatical-sentence` — 係り受けがねじれている文が2箇所

- **場所**: `src/docs/pages/Components.tsx`:124-125 / `docs/decisions/0048-docs-hash-carries-section.md`:20
- **累計**: 2回目
- **何が起きているか**: (1) リード文「プレビューは実装がある部品だけが描かれ」は主語が二重。(2) DR-0048 決定1「節を指していなければ `sectionId` は `null` を返す」は、`sectionId` が何かを返す形になっている。実際は値が `null` になる。
- **なぜ問題か**: (2) は戻り値の形を規定する契約文で、「`null` を返す」を関数の戻り値と読むと、書式違反のとき（本当に `null` を返す）と区別がつかない。
- **提案**: (1)「実装がある部品にだけ描かれ」。(2)「`sectionId` は `null` になる」。

### `code/test-expectation-contradicts-implementation`（新規） — 期待値が、実装のフォールバックと逆を書いている

- **場所**: `src/docs/pages/Components.test.tsx`:104
- **累計**: 1回目
- **何が起きているか**: 期待値が `String(slot?.max)` で、slot が引けないときは `'undefined'` を期待する。実装は同じ場合に `'—'` を描き、直後のテストはそちらを正しい姿として固定している。レビュアーが `emphasis` の `allowedIn` へ実在しないスロットを足して実測し、`expected "undefined" to be "—"` で落ちることを確認した。
- **なぜ問題か**: 同じファイル内の2つのテストが、同じ状況に別の正解を書いている。落ちたときのメッセージが実装の意図と逆を指すので、読んだ人が実装側を直しにいきかねない。
- **提案**: 期待値を実装と同じフォールバックに揃える。

### `code/redundant-effect-dependency`（新規） — `useScrollToSection` の `pageId` は `hash` から一意に決まる

- **場所**: `src/docs/DocsApp.tsx`:26-36, 64
- **累計**: 1回目
- **何が起きているか**: `page` は `resolvePage(hash)` で、`hash` とモジュール定数だけから決まる。`pageId` が変わるのは `hash` が変わるときに限られ、依存配列は実質 `[hash]` と同じ。コメントは「ページが変われば、同じ節 ID でも別の要素を指す」と書くが、`hash` 固定でページが変わる経路が無い。
- **失敗シナリオ**: 該当なし（設計上の冗長）。依存を `[hash]` に落としても全テストが通る。
- **提案**: 引数と依存を `hash` だけにする。残すなら、現状は冗長であることをコメントに書く。

### `code/duplicate-contract-loader`（新規） — `componentsFrom` が `layoutsFrom` のほぼ逐語の複製

- **場所**: `src/docs/components.ts`:38-49 / `src/docs/layouts.ts`:51-62（テストも2組が写しになっている）
- **累計**: 1回目
- **何が起きているか**: 「`Object.entries` → 0件なら例外 → パス順にソート → 値だけ返す」が完全に同じで、違うのは例外メッセージのディレクトリ名と型引数だけ。
- **なぜ問題か**: #38 以降で契約の種類が増えるたびに、関数2つとテスト2組が増える形になっている。並び順やガードの方針を変えるときに片方だけ直す事故が起きる。
- **失敗シナリオ**: 該当なし（設計上の問題）。
- **提案**: `contractsFrom<T>(modules, dir)` を1つ置き、両方がそれを呼ぶ。

## 落とした指摘

なし。6観点から挙がった指摘はすべて、実ファイルを読むか、コードを一時的に壊して実測して成立を確認した。重複していた4組（複製検査の抜け道、リンクの行き先が無検査、節 ID の一元化、契約文言の複製検査の移設）は、同一の問題に別々のIDが与えられていたため、それぞれ1件へまとめた。

## ルール化候補

このレビューで新たに閾値へ届いたカテゴリは無い。すでに候補になっているもののうち、今回さらに増えたのは次の4つ。

- `inspection/rule-has-bypass` — 通算17回。[DR-0044](../decisions/0044-bypass-fixtures-required.md) / #43 で `design/rules.json` の検査ルールには bypass フィクスチャを必須化したが、**リポジトリ側の検査（テスト）はその仕組みの外にある。** PR #53 に続いて2つの PR 連続で、新設した検査の抜け道がレビューで見つかっている。要求の範囲を広げるかどうかが次の判断になる（#54 が扱う範囲と重なる）
- `phase/completion-criterion-not-verified-e2e` — 通算7回。完了条件が「繋がり」を要求しているのに、テストが断片に分かれる形が繰り返している
- `writing/inaccurate-rationale` — 通算13回。今回は5箇所
- `decisions/consequence-not-followed` — 通算8回。今回は、同じ PR で書いた DR の帰結を同じ PR が破るという形で出た

## 2周目（2026-09-21）

- **対象**: `46e4897..f178598`（1周目のレビュー時点からの増分）
- **立てた観点**: 6観点すべて（差分に実装コードを含むため絞らない）
- **結果**: blocker 0件、should 3件、consider 2件。1周目の7件の should と10件の consider は、各観点が実際にコードを壊して解消を確認した。新しい指摘は**すべて1周目の修正そのものが作った穴**である。

### should

#### `inspection/rule-has-bypass`（この PR で計上済み） — 複製検査が `${''}` を1つ挟むだけで素通りする

- **場所**: `src/docs/pages/no-contract-prose.test.ts`:76-78（`packed`）
- **指摘した観点**: 検査ルールの実効性
- **累計**: この PR では1周目に計上済みのため、回数は増やさない
- **何が起きているか**: `packed` が落とすのは空白・引用符・`` ` ``・`+` だけで、`$` `{` `}` は残る。契約文言をテンプレートリテラルへ貼り、文字の間へ `${''}` を1つ挟むと一致しなくなる。レビュアーと統合時の双方で実測し、**緑のまま通る**ことを確認した。
- **なぜ問題か**: `${''}` は画面に出る文字列を1文字も変えない。検査が「複製は無い」と保証している状態で、契約文言がそのまま複製されている。1周目の修正は「行の折り返し」「`+` 連結」という具体例だけを塞いだ形になっており、同じ性質（表示に影響しない文字を挟んで文字列を分断する）を持つ別の書き方には効かない。
- **失敗シナリオ**: テンプレートリテラルと式が混ざった書き方で文言を貼ると、複製が入ったまま緑。契約側だけ直しても、カタログは古い文言を出し続ける。
- **提案**: 文字クラスを足していくのは、いたちごっこになる（`${0}` / `${ }` など変種が作れる）。ソースを素通しで見るのをやめ、文字列リテラル・テンプレートリテラルの**静的に決まる部分**だけを取り出して突き合わせる形にする。

#### `code/test-cannot-detect-regression`（この PR で計上済み） — `$comment` を守るはずのテストが、外されたことを検出できない

- **場所**: `src/docs/pages/no-contract-prose.test.ts`:108-116
- **指摘した観点**: コード品質
- **累計**: この PR では1周目に計上済みのため、回数は増やさない
- **何が起きているか**: 判定が `label.includes(contract.name)` という名前の部分一致で、そのエントリの `texts` を全く参照していない。`CONTRACT_PROSE` から `...commentOf(layout)` を取り除いても**緑のまま通る**（レビュアーと統合時の双方で実測）。加えて `statement` はレイアウト名と部品名の両方に在るため、どちらの契約を見ているかも区別できていない。
- **なぜ問題か**: このテスト自身のコメントが「持たなくなれば `commentOf` は静かに空を返し、上の検査は `$comment` について何も見ていない状態で通り続ける」と書いている、まさにその壊れ方を、このテスト自身が検出できていない。
- **失敗シナリオ**: `CONTRACT_PROSE` の生成から `commentOf` の呼び出しが消えても緑。以後、契約の `$comment` をページの説明文へ貼っても複製検査は気づかない。
- **提案**: 生成元の契約とエントリを1対1で対応づけ、`texts` に `$comment` の値が実際に含まれていることを見る。

#### `writing/inaccurate-rationale`（この PR で計上済み） — DR だけを直し、同じことを言う実装側のコメントを直さなかった／新しい帰結が実装より広く主張している

- **指摘した観点**: 日本語ドキュメント
- **累計**: この PR では1周目に計上済みのため、回数は増やさない
- 1. **`src/docs/components.test.ts`:70** — DR-0049 の帰結は「登録が契機」へ直したが、その帰結が指しているテストの直上コメントは「実装が入ったらこのテストは落ちる」のまま。同じ PR の中で、DR と実装コメントが同じ検査について逆のことを言っている。1周目の指摘（理由付けが実装や同じ DR の別の記述と食い違う）が、場所を変えて残った形。
- 2. **`docs/decisions/0049-...md`:69** — 新しい帰結が「ページや契約の種類を足しても、そこへ書き足す必要は無い」と言い切るが、`CONTRACT_PROSE` は `LAYOUTS` と `COMPONENTS` を手で並べた配列である。自動で入るのは既存の種類の中身が増えたときだけで、**契約の種類**が増えたら1行足す必要がある。
- **失敗シナリオ**: (1) 読み手が「実装すればテストが落ちる」と受け取り、登録漏れが機械で捕まらないことに気づかない。(2) #38 以降で3つ目の契約の種類を足す人が、この帰結を根拠に `no-contract-prose.test.ts` を触らず、新しい契約の文言が検査の対象外のまま残る。
- **提案**: (1) コメントを「`COMPONENT_PREVIEWS` へ登録すると」に揃える。(2) 「ページを足しても書き足す必要は無い（対象は glob）」と「契約の種類を足すときは列挙を1行足す」を書き分ける。

### consider

#### `decisions/undocumented-decision`（この PR で計上済み） — `contractsFrom` への共通化が DR に無い

- **場所**: `src/docs/contracts.ts`（新規）
- **指摘した観点**: 決定記録
- **累計**: この PR では1周目に計上済みのため、回数は増やさない
- **何が起きているか**: `contracts.ts` のコメント自身が、他の選択肢（`import.meta.glob` の呼び出しごと共通化する）を比較して却下理由まで書いている。これは「他の選択肢がありえて、理由を後から聞かれうる」判断にあたるが、DR には無い。1周目に同種の事例（複製検査の共通化）を DR-0049 の帰結へ1行置いて解消したのに、こちらだけ同じ扱いを受けていない。
- **失敗シナリオ**: 該当なし（設計上の問題）。#38 が3つ目の契約の種類を足すとき、共通化の方針を変える根拠が `contracts.ts` のコメントにしか無い。
- **提案**: DR-0049 の帰結へ1行置く。

#### `writing/dangling-quote-reference` — `componentSectionId` のコメントが DR-0048 の帰結を鉤括弧付きで不正確に引用している

- **場所**: `src/docs/components.ts`:48
- **指摘した観点**: 日本語ドキュメント
- **累計**: 6回目
- **何が起きているか**: 「DR-0048 の帰結が「節を持つページを足すときは対応を読み込み口へ置く」と定めている」と鉤括弧で引くが、実際の帰結は「節を持つページを足すときは、**節 ID の**対応を **contract 側の**読み込み口（…）へ置き、リンク側と `id` 側の両方がそこから引く」で、文字列として一致しない。
- **なぜ問題か**: 鉤括弧は原文そのままを示す記法として読まれる。要約なら鉤括弧を外すべきで、このままだと他の箇所の鉤括弧引用の信頼性も下がる。
- **失敗シナリオ**: 該当なし（設計上の問題）。
- **提案**: 鉤括弧を外して要約と分かる書き方にするか、実際の文言に合わせる。

### 落とした指摘（2周目）

なし。

### 作業上の記録

並行して走らせたレビュアーが、別のレビュアーによる一時的なワークツリー改変を目撃し、「実体の無い注入内容」と判断した報告があった。実際には、検証のためにファイルを書き換える観点（検査・コード品質）が同時に走っていたことによる。**統合時に `git status` で復元を確認し、クリーンであることを確かめた。** 検証にファイルの書き換えを伴う観点を並列で立てる限り、この干渉は起こりうる。

### ルール化候補（2周目）

増えたものは無い。`inspection/rule-has-bypass` が、PR #53 に続いてこの PR でも**同じ PR の中で2周連続**、新設した検査の抜け道として出ている。#54 が扱う範囲と重なる。

## 3周目・1回目（2026-09-21）— 中断

- **対象**: `f178598..f18a14c`（2周目のレビュー時点からの増分）
- **立てた観点**: 6観点すべて（4本のサブエージェント）
- **結果**: **4本すべてがセッションのレート制限（HTTP 429）で異常終了し、レビューは成立しなかった。**

### 中断から得られたもの

検査ルールの実効性を見ていたレビュアーが、落ちる直前に次の一行を残していた。

> Now I test the JSX-text-node bypass (no quotes needed for JSX children).

これを手掛かりに統合側で実測したところ、**2周目の修正が新しい穴を作っていた**ことが分かった。

#### `inspection/rule-has-bypass`（この PR で計上済み） — 静的な文字列だけを見る形にしたため、JSX のテキストとして貼った複製が素通りする

- **場所**: `src/docs/pages/no-contract-prose.test.ts`（`staticStrings`）
- **何が起きているか**: 2周目の修正で、ソースの部分一致をやめ「文字列リテラルとテンプレートリテラルの静的に決まる部分だけを取り出す」形にした。JSX の子要素として書いたテキスト（`<p>あいう</p>`）は引用符を持たないので、取り出しの対象に入らない。契約の `role` をページのリード文へ JSX テキストとして貼ると、**緑のまま通る**ことを実測した。
- **なぜ問題か**: ページのリード文がまさにその形である。契約文言を貼るとしたら最も起こりやすい場所が、修正によって検査の外へ出た。1周目の版（生のソースを見る形）はこの形を捕まえていたので、**2周目の修正は一方を塞いで他方を開けた**ことになる。
- **失敗シナリオ**: 次のページを足す人が契約の `role` をリード文へ貼る。複製が入ったまま `pnpm check` は緑。
- **対応**: 反映した。生のソース（空白を落としたもの）と、静的に決まる文字列の**両方**に対して突き合わせる。JSX テキスト・JSX の折り返し・素の文字列・`+` 連結・`${''}`・`${変数}`・`pages/` の外・`.ts` ファイル・`$comment` の9通りで検出することを実測した。

### 作業上の記録

レート制限で落ちたレビュアーが、**検証用に仕込んだ契約文言の複製をワークツリーへ残したまま終了していた。** 統合側が `git status` と全契約文言の走査で気づき、コミット済みの状態へ戻した。2周目に記録した干渉（並行レビュアーが互いの一時改変を目撃する）と同じ根で、こちらは異常終了によって復元が行われなかった形である。

**検証のためにファイルを書き換える観点は、異常終了すると汚染を残す。** 統合側は毎周 `git status` を確認する必要がある。

### 3周目の扱い

レビューとして成立していないので、収束の判定には使わない。`issue-workflow` の手順5が数える「周」は、指摘の有無を判定できた周を指す。ここでは判定できていない。

## 3周目・やり直し（2026-09-21）

- **対象**: `f178598..da705d7`（2周目のレビュー時点からの増分。中断した周と同じ起点）
- **立てた観点**: 6観点すべて（4本のサブエージェント）
- **検証の隔離**: 中断した周でワークツリーが汚染されたため、コードを壊す検証は `git worktree` の複製で行うよう指示した。**汚染は起きなかった**（統合時に `git status` で確認）。
- **結果**: blocker 0件、should 3件、consider 0件。設計契約とフェーズは指摘なしで、Issue #36 の完了条件6項目もレビュアーが実ファイルで確認した。**新しい指摘は3件とも、前の周の修正が作った穴である。**

### should

#### `inspection/rule-has-bypass`（この PR で計上済み） — JSX の地の文を `{''}` で割ると素通りする

- **場所**: `src/docs/pages/no-contract-prose.test.ts`
- **指摘した観点**: 検査ルールの実効性
- **何が起きているか**: 3周目の直前に「生のソースと静的な文字列の両方を見る」形へ直したが、JSX の地の文の途中へ `{''}` を1つ挟むと、どちらの見方も捕まえない。生のソースには `{''}` の3文字が残って文言が連続せず、静的な文字列の側は引用符を持たない地の文をそもそも見ない。レビュアーが隔離した複製で実測し、統合時にも再現した。
- **なぜ問題か**: 2周目に `${''}` をテンプレートリテラルで塞いだのと**同じ手口の JSX 版**である。画面に出る文字は1文字も変わらない。`{変数}` や `{f({ y: 1 })}` でも同じことが起きる。
- **失敗シナリオ**: 契約の `role` をリード文へ貼り、途中に式を1つ挟む。複製が入ったまま `pnpm check` は緑。
- **対応**: 反映した。**改行を含まない** `{...}` を内側から落とした3つ目の見方を足した。改行で区切るのは関数の本体を落とさないためで、制限なしに落とすと次の周で関数の本体が最も内側になり、中の JSX ごと消える（実際に一度そうなり、検出がゼロになった）。JSX の素貼り・折り返し・`{''}`・`{変数}`・`{f({ y: 1 })}`、文字列の素貼り・`+` 連結・`${''}`・`${変数}`、`pages/` の外、`.ts` ファイル、`$comment` の**12通りで検出する**ことを実測した。改行を挟んだ式で割る形は残っており、塞いでいる範囲としてコメントに書いた。

#### `code/test-cannot-detect-regression`（この PR で計上済み） — `$comment` を守るテストが、集める側と期待値の側で同じ関数を呼んでいる

- **場所**: `src/docs/pages/no-contract-prose.test.ts`
- **指摘した観点**: コード品質
- **何が起きているか**: `expect(texts).toContain(commentOf(contract))` の形で、`texts` を組み立てる側も期待値の側も `commentOf` を呼ぶ。レビュアーが隔離した複製で `commentOf` を「全契約について非 null だが中身の違う値」を返すよう壊したところ、3件とも緑のまま通った。
- **なぜ問題か**: 2周目に塞いだのは「`commentOf` の呼び出しが消える」形で、「`commentOf` の中身が間違う」形は残っていた。同じカテゴリの穴が形を変えて残っている。
- **対応**: 反映した。期待値を `commentOf` から独立させ、契約の実体から直に `$comment` を読む形にした。

#### `writing/inaccurate-rationale`（この PR で計上済み） — DR-0049 の帰結が、実装より狭く書かれている

- **場所**: `docs/decisions/0049-catalog-detects-implementation-by-registry.md`
- **指摘した観点**: 日本語ドキュメント
- **何が起きているか**: 帰結は「判定はソースの部分一致では行わない。静的に決まる部分だけを取り出して繋げてから突き合わせる」と言い切るが、実装は生のソース（空白だけを落としたもの）との部分一致も併用している。テスト側のコメントは「片方だけでは、もう片方の形が素通りする」と両方必要だと書いており、DR とコードが逆を言っている。
- **なぜ問題か**: #38 以降でこの検査を触る人が「素の部分一致はやめた」と読み、JSX テキストを捕まえている側を不要として削りうる。**2周目から3周目にかけて実際に起きた事故（部分一致をやめたら JSX テキストが抜けた）を、DR が再発させる形になっている。**
- **対応**: 反映した。「判定は1つの見方では行わない」として3つの見方を列挙し、**見方を1つに絞る変更をするときは、どの形が素通りするようになるかを先に確かめること**を帰結へ書いた。この PR で実際に起きたことも添えた。

### 落とした指摘（3周目）

なし。

### ルール化候補（3周目）

増えたものは無い。ただし記録しておく。**`inspection/rule-has-bypass` が、この PR の中で3周連続、すべて「前の周の修正が作った穴」として出た。**

| 周 | 塞いだもの | 開いたもの |
|---|---|---|
| 1周目 | （検査が無い） | 文字列の折り返し・`+` 連結 |
| 2周目 | 折り返し・`+` 連結 | `${''}` |
| 3周目直前 | `${''}`（静的な取り出しへ一本化） | **JSX の地の文が丸ごと** |
| 3周目 | JSX の地の文・`{''}`・`{変数}` | 改行を挟んだ式（残置と明記） |

PR #53 でも同じ形が3周続いた。**検査を新しく書く PR では、その検査自身の抜け道を探す周が要る**という観測が2例になった。#54 が扱う範囲そのものである。
