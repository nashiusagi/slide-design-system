# PR #19 レビュー

- **タイトル**: スライドランタイム: Deck / Slide / Fragment
- **レビュー日**: 2026-09-07
- **対象**: main...feature/3_slide-runtime
- **変更ファイル数**: 17

## 要約

blocker 1件、should 8件、consider 3件。

blocker は URL からの位置復帰と段階送りの組み合わせで段階が飛ぶ実バグで、再現テストで確認した。should の多くは新設 DR 2本（0029 / 0030）の記述が実装と食い違うか、決めていないことを決まったものとして書いている点に集中している。実装そのもの（キャンバスのスケーリング、hash の書式解析、Fragment の登録）は境界値のテストが実際に効いており、この観点での指摘は出なかった。

## blocker

### `code/state-reset-not-recovered` — 同一スライド内で hash が変わると段階数が 0 のまま復帰せず、以降の段階が飛ばされる

- **場所**: `src/runtime/Deck.tsx`:131（`handleHashChange` 内の `setStepCount(0)`）、`src/runtime/Slide.tsx`:56-60
- **指摘した観点**: コード品質
- **累計**: 1回目
- **何が起きているか**: `handleHashChange` はスライドが変わったかどうかに関わらず `setStepCount(0)` する。段階数を Deck へ報告するのは `Slide` の `useLayoutEffect` で、依存は `[reportStepCount, stepCount]`。同じスライドに留まる hash 変更では `Slide` は再マウントされず Slide 側の `stepCount` も変わらないため、この effect は再実行されない。Deck の `stepCount` は 0 に落ちたまま復帰しない。
- **なぜ問題か**: 段階数が 0 だと `goNext` の `position.step < stepCount` が常に偽になり、残りの Fragment を出さずに次のスライドへ飛ぶ。DR-0029 は URL を現在位置の表現とし、measure（DR-0011）も URL を組み立てて全段階を巡回するので、「URL を触った後は段階送りが壊れる」は巡回の前提を壊す。
- **失敗シナリオ**: 再現テストで確認した。(1) `ArrowRight` で 2 枚目へ移動（Fragment 2 個）、(2) `window.location.hash = '#/2/1'`、(3) `ArrowRight` を 1 回。期待は「二つ目」の表示だが、実際は 3 枚目へ遷移し hash は `#/3` になる。段階 2 が丸ごと飛ぶ。
- **提案**: リセットという操作自体をやめる。`Slide` が「どのスライドの段階数か」を添えて報告し、Deck は現在位置と一致する報告だけを採る。そうすれば `goToSlide` と `handleHashChange` の `setStepCount(0)` は両方とも不要になり、古い報告は自動的に無視される。

## should

### `phase/out-of-scope-addition` — `notes` を `data-notes` として DOM へ出力しており、公開物にスピーカーノートが載る

- **場所**: `src/runtime/Slide.tsx`:69
- **指摘した観点**: フェーズスコープ、決定記録（複数観点から指摘された）
- **累計**: 4回目
- **何が起きているか**: DR-0004 の決定は「`Slide` は `notes` を props として受け取るが表示はしない」までで、DOM へ出すことは決めていない。同じ PR の DR-0030 は `data-layout` を出す理由を書いているが、`data-notes` はどの DR にも無い。
- **なぜ問題か**: 「ノートを最終 HTML に載せる」という DOM 契約の追加であり、Phase 1.5 の発表者ビューの実装方式を先に固定する。加えて DR-0006（オンライン共有優先）と DR-0022（素のビルド出力）の下では、発表者向けの覚書が閲覧者に読める形で配られる。
- **失敗シナリオ**: `<Slide notes="ここは早口で流す">` を静的ホスティングへ公開すると、`<section data-notes="ここは早口で流す">` としてページソースから読める。
- **提案**: `data-notes` の出力をやめ、`notes` は型の上で受け取るだけにする。DR-0004 の「受け取り口だけは最初から作る」はこれで満たせる。

### `decisions/consequence-not-followed` — キャンバス寸法が CSS から TS 定数へ移ったのに、DR-0021 の帰結が旧い前提のまま

- **場所**: `src/runtime/canvas.ts`:12-13、`src/runtime/runtime.css`:6、`docs/decisions/0021-starter-contains-runtime-only.md`（帰結の3点目）
- **指摘した観点**: 設計契約、決定記録、日本語ドキュメント（複数観点から指摘された）
- **累計**: 2回目
- **何が起きているか**: DR-0021 の帰結は「starter の**キャンバス CSS は寸法を持つ**」「starter の CSS を tokens から生成するか、両者の一致を検査する」と書く。この PR は `runtime.css` に「キャンバスの寸法も書かない」と明記し、寸法を `canvas.ts` の定数へ移した。DR-0021 は更新されておらず、`canvas.ts` のコメントは根拠として「DR-0021 の帰結」を挙げている。
- **なぜ問題か**: 帰結が指す検査対象（starter の CSS）が実在しなくなった。DR を正として検査を書けば比較対象が取れず 0 件比較で緑を返す。DR-0021 自身が「ずれると `no-overflow` の基準面が条件ごとに変わり lint では検出できない」と書いている箇所で、検出できない種類の破れにぶら下がっている。
- **失敗シナリオ**: #4 で `design/tokens.json` の `canvas` を別の値で作る → 一致検査が無いのでどのコマンドも落ちない → measure は 1280x720 のキャンバスを測るのに、契約側は別の寸法でレイアウトを組む。
- **提案**: DR-0021 の帰結を「寸法は `src/runtime/canvas.ts` の定数が持ち、`design/tokens.json` の `canvas` との一致を #4 で検査する」の形へ更新し、`canvas.ts` / `runtime.css` のコメントをそれに合わせる。#4 の Issue にも作業項目として残す。

### `writing/ambiguous-criterion` — DR-0029 の3箇所が二通りに読め、うち1つは実装が経路ごとに分かれている

- **場所**: `docs/decisions/0029-position-in-url-and-explicit-fragment-index.md`:15,17,52、`src/runtime/Deck.tsx`:122-133、`src/runtime/Fragment.tsx`:7
- **指摘した観点**: 日本語ドキュメント、決定記録（複数観点から指摘された）
- **累計**: 4回目
- **何が起きているか**: 3点ある。(1)「書式に合わない hash は無視して先頭のスライドを表示する」が「何もしない」とも「先頭へ移動する」とも読め、実装は読み込み時が後者、`hashchange` 時が前者になっている。(2) `Fragment` の `index` の基数が DR に無い。「段階は 0 始まり」の一文から `index=0` を正当と解釈しうるが、実装は 1 始まり。(3)「範囲を超えるスライド番号は端へ丸める」の外延に `#/0` が入るか不明で、実装は `#/0` を書式違反として扱っている。
- **なぜ問題か**: この DR は measure が URL を組み立てて巡回するための規則で、「規則が曖昧だと巡回する側とランタイムで解釈がずれる」ことを文脈自身が問題として挙げている。(1) は URL と表示が乖離したまま残る不変条件の破れでもある。
- **失敗シナリオ**: measure が巡回中に `#/nowhere` を投げ「先頭に戻ったはず」として次を測る → 実際は直前のスライドが表示されたままで、そのスライドを二重に測り、先頭は一度も測られない。#7 の lint 実装者が `index=0` を正当としてルールを書くと、`#/3` と `#/3/1` の見た目が同じになる。
- **提案**: 決定の箇条書きを経路ごとに分けて書く。`index` は 1 始まりで段階 N では `index <= N` が出ていることを明記する。`#/0` と負値が書式違反であることを明記する。実装側は、表示中に書式外の hash が来たら現在位置で URL を書き戻す。

### `decisions/undocumented-decision` — 操作系・履歴の扱い・未到達段階の非描画が、どの DR にも無い

- **場所**: `src/runtime/Deck.tsx`:88-107,116,179、`src/runtime/Fragment.tsx`:42-43
- **指摘した観点**: 決定記録
- **累計**: 4回目
- **何が起きているか**: (1) 進むのは `ArrowRight` / `Space` / デッキ上の任意クリック、戻るのは `ArrowLeft`、修飾キー付きは無視 —— Issue #3 の本文にしか無い。(2) ページ送りで履歴を積まない（`replaceState`）—— 根拠はコード注記だけ。(3) 未到達の Fragment を隠さず描画そのものを止める —— コメントで DR-0011 を根拠に説明しているが DR 側に記録が無い。
- **なぜ問題か**: いずれも他の選択肢が実在する。特に (2) と (3) は測定側に効く。(3) は DR-0011 の「measure は各スライドの全 Fragment 段階について測る」の前提（未到達段階が要素として残らない）を作っており、後から「隠すだけ」に変えると `no-overflow` の判定が静かに変わる。Issue とコード注記はマージ後に読まれない。
- **失敗シナリオ**: 該当なし（設計上の問題）。
- **提案**: (1)(2) は操作と履歴の DR を 1 本立てる。(3) は Fragment の段階を扱う DR-0029 の決定へ足す。

### `decisions/dr-restates-canonical-value` — DR-0030 の帰結が正本 `design/layouts/` のレイアウト名を書き写している

- **場所**: `docs/decisions/0030-slide-class-derived-from-layout.md`:6,42
- **指摘した観点**: 決定記録、設計契約（複数観点から指摘された）
- **累計**: 3回目
- **何が起きているか**: DR-0030 は正しく `**正本**: design/layouts/` を置きながら、帰結で `.slide--title` / `.slide--bullets` / `.slide--statement` を列挙している。理由節は「レイアウトが 3 種から増えてもランタイムは変わらない」と書いており、帰結の列挙と噛み合わない。
- **なぜ問題か**: DR-0024 の帰結は「値を含む決定は `**正本**:` 行を持ち、値そのものは書かない」。DR-0010 は Phase 1.5 での `section` / `code` / `figure` の追加を予定しているので、増えた時点で DR-0030 の帰結だけが古い 3 種を主張する箇所として残る。
- **失敗シナリオ**: 該当なし（設計上の問題）。
- **提案**: 帰結を「`design/layout.css` は `design/layouts/` の各 variant に対応する `slide--<layout>` を実装する」と規則の形にし、具体名は正本に任せる。

### `contract/source-of-truth-ambiguous` — DR-0030 が layout 契約のファイル形式とフィールド名を、契約が無い段階で断定している

- **場所**: `docs/decisions/0030-slide-class-derived-from-layout.md`:12,18,28
- **指摘した観点**: 設計契約、日本語ドキュメント（複数観点から指摘された）
- **累計**: 3回目
- **何が起きているか**: DR-0030 は layout 契約を `design/layouts/<name>.json`、クラス名を持つフィールドを `layout.classes` と書く。どちらもリポジトリ内で DR-0030 が初出で、形式を決めた DR は無い（DR-0016 が決めているのは deck 契約の形式だけ）。
- **なぜ問題か**: 形式とフィールド名は #5 で決まるもので、#5 が別の形を選ぶと DR の記述はどの実ファイルも指さなくなる。DR-0030 はその名前を `pnpm design:check` の検査契約として使っているので、検査を書く側が存在しないフィールドを読みに行く。
- **失敗シナリオ**: #5 が `design/layouts/title.md`（frontmatter に `className`）を採ると、DR-0030 の記述が実体を指さなくなる。
- **提案**: 形式に依存しない表現（「`design/layouts/` の契約が持つクラス名」）へ緩め、形式とフィールド名の決定は #5 に委ねる。

### `inspection/rule-has-bypass` — キャンバスの `overflow: hidden` により、はみ出しが描画結果に痕跡を残さない

- **場所**: `src/runtime/runtime.css`:21
- **指摘した観点**: 検査ルールの実効性
- **累計**: 3回目
- **何が起きているか**: `.slide-canvas` に `overflow: hidden` が入り、はみ出した要素は画面にもスクリーンショットにも出ない。DR-0011 は `no-overflow` を「キャンバスから要素がはみ出していない」としか書いておらず、レイアウト矩形で比較すること（描画された可視性で比較しないこと）はどこにも書かれていない。
- **なぜ問題か**: `no-overflow` は DR-0011 が本命と呼ぶルールで、プロジェクトの主張そのものを支えている。違反が視覚的に痕跡を残さない状態が作られたので、measure の実装方法しだいで検査が丸ごと空振りする。
- **失敗シナリオ**: `<ul>` に長い項目を積みテキストが y=720 を超える。`overflow: hidden` で描画結果には出ない。#8 の measure がスクリーンショット比較や `checkVisibility()` で実装されると「収まっている」と判定され `no-overflow` が緑で通る。
- **提案**: `overflow: hidden` の直上に「ここでクリップするので `no-overflow` は必ずレイアウト矩形で比較すること。可視性やスクリーンショットで判定すると違反が消える」を残す。

### `inspection/measure-state-unspecified` — 現在スライドの段階数がランタイムから観測できず、measure が段階を取りこぼしても検出できない

- **場所**: `src/runtime/Deck.tsx`:178-180、`docs/decisions/0029-position-in-url-and-explicit-fragment-index.md`（帰結）
- **指摘した観点**: 検査ルールの実効性
- **累計**: 2回目
- **何が起きているか**: `Deck` はスライド数・スライド位置・現在段階を DOM へ出すが、段階数は出さない。DR-0029 の帰結は段階数の導出を measure 側の静的解析に委ね、かつ段階の丸めをしないと決めているので、`#/2/99` は `#/2/99` のまま表示は最終段階と同じになる。
- **なぜ問題か**: DR-0011 は「未実行を pass と区別する」ために実行済みルール ID を残すと決めているが、段階についてはその区別の手段が無い。段階数は Fragment の**最大 index** であって個数ではないので、measure 側が個数で数えると欠番のあるスライドで最終段階に到達しない。
- **失敗シナリオ**: `index={1}` と `index={3}` を持つスライドで measure が段階数を 2 と導出し `#/2` `#/2/1` `#/2/2` だけを巡回する。`index={3}` は一度も描画されないまま `no-overflow` が緑で通る。
- **提案**: `Deck` のルートに `data-step-count` を足し（`data-slide-count` と形が揃う）、DR-0029 の帰結へ「measure は巡回後に `data-step` が `data-step-count` に達したことを確認する」と「段階数は最大 index であり個数ではない」を加える。

## consider

### `decisions/index-section-mismatch` — 新規 DR 2本の索引登録が、節と並びの両方で既存の規則から外れている

- **場所**: `docs/decisions/README.md`:35,47
- **指摘した観点**: 決定記録、日本語ドキュメント（複数観点から指摘された）
- **累計**: 2回目
- **何が起きているか**: DR-0029 は「技術スタック」節に置かれたが、この節の他の6本はいずれも「どの技術を採用するか」の決定である。DR-0029 が決めているのは URL の書式と Fragment の書き方で、deck 契約と lint を縛る。加えて既存の全表は DR 番号の昇順に並んでいるが、0029 は 0003 と 0018 の間、0030 は 0010 と 0016 の間へ挿入されており、この2表だけ並びが崩れている。
- **なぜ問題か**: 索引は「どの節を見ればその種の決定が見つかるか」で引かれる。表ごとに並び基準が違うと、次に DR を足す人がどこへ入れるべきか判断できない。
- **失敗シナリオ**: 該当なし（設計上の問題）。
- **提案**: 両方を「契約の構造」節の末尾へ移し、昇順に戻す。

### `code/effect-timing-mismatch` — Slide の「paint 前に段階数を確定させる」意図が、Fragment 側が `useEffect` のため成立していない

- **場所**: `src/runtime/Fragment.tsx`:30、`src/runtime/Slide.tsx`:56-60
- **指摘した観点**: コード品質
- **累計**: 1回目
- **何が起きているか**: `Slide` は `useLayoutEffect` で報告し「描画前に段階数を確定させる」と理由を書くが、その入力となる Slide 側 `stepCount` を動かすのは `Fragment` の `useEffect`（paint 後に flush）。最初の layout effect が報告するのは常に 0 で、正しい値の報告は paint 後の再レンダーで起きる。
- **なぜ問題か**: コメントが述べている保証が実際には得られていない。層が増えたときに誤った前提で読まれる。
- **失敗シナリオ**: 実行時の失敗としては再現できていない（Testing Library の `render` は passive effect まで flush し、ブラウザでも paint 直後の1フレームに限られる）。確認できたのは「layout effect が読む値が passive effect でしか更新されない」という時系列の不一致まで。
- **提案**: `Fragment` の登録を `useLayoutEffect` に変え、`Slide` の layout effect が最初から正しい値を持つようにする。

### `writing/notation-inconsistency` — DR-0029 だけ `**正本**:` 行を持たず、正本の所在が散文で書かれている

- **場所**: `docs/decisions/0029-position-in-url-and-explicit-fragment-index.md`:3-5,20
- **指摘した観点**: 日本語ドキュメント
- **累計**: 4回目
- **何が起きているか**: DR-0029 はヘッダに `**正本**:` 行を持たず、決定の末尾に「書式の実体は `src/runtime/hash.ts` にある」と散文で書いている。同じ PR の DR-0030 はヘッダに正本行を置いている。
- **なぜ問題か**: `**正本**:` 行は「この決定の値がどこにあるか」を引くための場所で、同じ役割の情報が DR ごとに違う場所へ書かれると、正本の一覧を取る操作が成立しない。
- **失敗シナリオ**: 該当なし（設計上の問題）。
- **提案**: ヘッダへ `- **正本**: src/runtime/hash.ts（書式）` を足す。

## 落とした指摘

なし。6観点から挙がった指摘はいずれも、ファイルを読んで成立を確認できた。blocker は再現テストを書いて確認した。

同じ問題が別のカテゴリIDで複数観点から挙がったものは、1つのIDへ寄せて計上した。

- キャンバス寸法の置き場所: `contract/value-outside-source-of-truth` / `writing/contradictory-instruction` を `decisions/consequence-not-followed` へ寄せた
- DR-0030 のレイアウト名列挙: `contract/contract-structure-duplicated` を `decisions/dr-restates-canonical-value` へ寄せた
- DR-0030 の形式断定: `writing/term-inconsistency` を `contract/source-of-truth-ambiguous` へ寄せた
- 索引の登録位置: `writing/notation-inconsistency`（並び）を `decisions/index-section-mismatch` へ寄せた
- DR-0029 の hash 記述: 新設候補だった `decisions/implementation-contradicts-dr` は立てず、既存の `writing/ambiguous-criterion` を使った

## ルール化候補

- `decisions/dr-restates-canonical-value` — 通算3回。`**正本**:` 行を持つ DR が、その正本に属する値や名前の一覧を本文へ書いていないかを検査できる。正本ディレクトリのファイル名と DR 本文の文字列照合になる
- `contract/source-of-truth-ambiguous` — 通算3回。DR が参照する契約ファイルのパスとフィールド名が、実在するか（あるいは未作成なら「予定」と明示されているか）を検査できる
- `inspection/rule-has-bypass` — 通算3回。検査ルールごとに「この書き方をすると素通りする」条件を書く欄を設け、空でないことを検査する形が考えられる。今回のように実装側の CSS が抜け道を作る場合まで機械的に捕まえるのは難しい
