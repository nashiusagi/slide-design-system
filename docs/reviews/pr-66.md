# PR #66 レビュー

- **タイトル**: 再レビューで前周のレビュー全文を配るのをやめ、観点ごとに選別して渡す
- **レビュー日**: 2026-09-24
- **対象**: main...feature/64_select-prior-review-by-perspective
- **変更ファイル数**: 7
- **立った観点**: 設計契約との整合 / 検査ルールの実効性 / 決定記録（DR）との整合 / フェーズスコープ / 日本語ドキュメント / コード品質

## 要約

blocker 3件、should 12件、consider 6件。blocker はいずれも同じ根を持つ——**選別スクリプトのパーサが、実在のレビュー記録の書式と乖離している**。3観点から独立に挙がった。DR-0055 に記録した実測値（削減55%）は、27本あるレビュー記録のうち唯一この書式で書かれた pr-61.md 1本で取られており、他の26本では成立しない。

## blocker

### `inspection/rule-has-bypass` — パーサが実在のレビュー記録の45%の指摘を黙って取りこぼす

- **場所**: `scripts/select-review-findings.mjs`:30-32（`SEVERITY_HEADING` / `FINDING_HEADING`）, :152-158（`## ` で severity を null に戻す分岐）
- **指摘した観点**: コード品質 / 設計契約との整合 / 検査ルールの実効性（3観点から独立に挙がった）
- **累計**: 20回目
- **何が起きているか**: 指摘を `` ^### `prefix/id` ``、重要度の節を `^## (blocker|should|consider)` に固定している。実データの大半は2周目以降を `## <N>周目` → `### should` → `` #### `id` `` の3階層で書き、古い周ではカテゴリIDに backtick が無い。`docs/reviews/*.md` 全27本へ当てて数えると、カテゴリID形の見出し532件に対し解析できたのは293件（**欠落239件、45%**）。pr-21 / 23 / 24 / 26 / 27 / 28 / 30 / 39 の8本は**解析結果が0件**になる。実行して確認した。
- **なぜ問題か**: 欠落するのは2周目以降の節、つまり**直前の周の指摘**で、再レビューで最も渡す価値が高いものだ。DR-0055 決定3 は「読めない指摘は全観点へ渡す」と安全側に倒しているが、その退避路（`targetsFor` の `basis: 'unresolved'`）は**指摘として認識できた場合にしか働かない**。見出しの形が違って立ち上がらなかったぶんは `findingCount` にすら入らず、退避路の手前で消える。安全側の設計が、最も起きやすい失敗の形をカバーしていない。さらに `summarize` の before/after は取りこぼしを両方から除くので、`--report` の削減率は正常値に見え、新設した「渡した前周の指摘」節にも欠落が現れない。
- **失敗シナリオ**: `--review=docs/reviews/pr-26.md --perspective=agents/inspection.md` は標準出力が空・終了コード0。`--review=docs/reviews/pr-60.md --report` は「指摘14件」と出すが実際は20件（2周目5件・3周目1件が不可視）。3周目のレビュアーは2周目の指摘を渡されないまま走り、同じ指摘を再生産する——DR-0055 が防ごうとした当のこと。
- **提案**: (a) `FINDING_HEADING` を `` ^#{3,4} `?prefix/id`? `` へ、`SEVERITY_HEADING` を `^#{2,3} (blocker|should|consider)` へ広げ、節の切り替えは「現在の重要度見出しと同レベル以上」で判定する。(b) `review-file-format.md` に、再レビュー節での重要度見出しの階層とカテゴリIDの backtick を明記して機械が読める形に固定する。(c) 解析0件のときに空を返して正常終了せず、全文へフォールバックするか非ゼロで落ちる。

### `inspection/canonical-value-unmatchable` — 正本の観点名に全角括弧が含まれ、注記剥がしで1観点が永久に解決しない

- **場所**: `scripts/select-review-findings.mjs`:100-105（`splitPerspectiveField`）
- **指摘した観点**: 検査ルールの実効性 / 決定記録（DR）との整合（2観点から独立に挙がった）
- **累計**: 1回目
- **何が起きているか**: `splitPerspectiveField` は各要素から `（…）` を全て剥がす。ところが観点表の正式名の一つは `決定記録（DR）との整合` で、**名前そのものに全角括弧を含む**。剥がすと `決定記録との整合` になり `loadPerspectiveMap()` の鍵と一致しない。実行して確認: `設計契約との整合 / 決定記録（DR）との整合（2観点から独立に挙がった）` → `['設計契約との整合', '決定記録との整合']` → `['agents/contract.md', undefined]`。加えて実データの `指摘した観点` 欄は表記が3通りに割れており（`決定記録との整合` 60件 / `決定記録（DR）との整合` 28件 / `決定記録` 27件）、区切りも `/` が71欄・`、`が35欄で、`、` は分割されず丸ごと1要素になって解決しない。
- **なぜ問題か**: DR-0055 決定3 の2つ目「複数観点から挙がった指摘はその全部へ渡る」が、決定記録の観点については成立しない。しかも決定3 の3つ目の安全弁も効かない——接頭辞が読めれば `basis: 'prefix'` で確定し、「欄の一部が読めなかった」ことが検出されないからだ。結果、当該の指摘については DR が**却下したはずの「接頭辞だけで切る形」へ退行**する。単独観点の指摘は接頭辞に落ちて偶然正しい先へ行くため、表からは見えない。
- **失敗シナリオ**: pr-61.md で `--perspective=agents/decisions.md` を実行すると、記録上「日本語ドキュメント / 決定記録との整合」から挙がったと書かれている `writing/inaccurate-rationale` が出力に含まれない。この指摘は Issue #65 が「接頭辞では拾えない観点をまたぐ指摘」の実例として名指ししているものそのもの。
- **提案**: 照合を「注記を剥がす前の形」を優先し、剥がした形は後段のフォールバックにする。区切りは `/` に加えて `、` `,` も受ける。表記ゆれ（`決定記録` 等の短縮形）をどこまで許すかは `review-file-format.md` 側で固定し、正本の観点名と一致することを機械で確かめる。テストへ、正本の観点表をそのまま使った照合ケースを置く。

### `phase/completion-criterion-not-verified-e2e` — 選別結果をレビュアーへ渡す経路が手順に無く、機能が誰にも届かない

- **場所**: `.claude/skills/pr-review/SKILL.md` 手順2（追加分）, 同 手順4 のプロンプト雛形, 同 :93
- **指摘した観点**: フェーズスコープ
- **累計**: 10回目
- **何が起きているか**: 手順2 に選別コマンドの実行を足したが、**実際にサブエージェントへ渡る唯一の文面である手順4 のプロンプト雛形が今回の差分で一切変更されていない**。雛形は「対象」「既知の指摘カテゴリ」「制約」「出力形式」だけで、選別した指摘を差し込むスロットが無い。手順2 のどこにも「この出力を雛形のどこへ入れるか」が書かれていない。加えて :93 は「前周のレビュー**全文**という強い足場がある」のままで、再レビューを Sonnet で立てる根拠が全文前提で残っている。
- **なぜ問題か**: Issue #64 の完了条件の先頭は、スクリプトが存在することではなく**渡る中身が変わること**を条件にしている。DR-0055 帰結の1点目も、この経路が無ければ成立しない。報告者は should としたが、マージすると「スクリプトは走るが出力の行き先が無い」状態が残り、機能が無言で不作動になるため blocker へ上げた。
- **失敗シナリオ**: マージ後、次の PR の2周目で手順2 がスクリプトを実行して出力を得るが、雛形に入れる場所が無いので雛形どおりに6観点を spawn する。レビュアーは前周の指摘を一切受け取らない。削減率は100%になる代わりに、既に指摘済みのカテゴリが再生産される。
- **提案**: 手順4 の雛形へ、再レビュー時のみ埋まるブロックを足し、雛形が観点ごとに異なる入力を取ることを明示する。あわせて :93 の「全文」を選別後の表現へ直す。

## should

### `decisions/consequence-not-followed` — `--report` が「渡さなかった指摘」を出力せず、DR-0055 決定5 の記録が取れない

- **場所**: `scripts/select-review-findings.mjs`（`main()` の report 分岐）, `.claude/skills/pr-review/references/review-file-format.md`:52-56
- **指摘した観点**: フェーズスコープ / 日本語ドキュメント / 決定記録（DR）との整合（3観点から独立に挙がった）
- **累計**: 13回目
- **何が起きているか**: `summarize()` は観点ごとの `dropped`（カテゴリID・重要度）を組み立てているが、`--report` の出力は件数・文字数・削減率だけで、カテゴリIDを1件も印字しない。一方 `review-file-format.md` は「`--report` の結果から…観点ごとに渡さなかった指摘のカテゴリIDを書く」と指示している。出力の観点列は `agents/*.md` の指示ファイル名だが、書式は `<観点名>` を求めており単位も揃っていない。
- **なぜ問題か**: DR-0055 決定5 はこの記録を、選別が成立しているかを判定する唯一の手段としている。道具が出さない情報を書式が必須にしており、書く側は手で再現するか「なし」と書くしかない。記録が書き手の申告に落ちる。
- **失敗シナリオ**: 2周目の統合役が指示どおり `--report` を実行し、dropped が無いので `**渡さなかった指摘**: なし` と書く。実際には観点ごとに20件以上落ちている。記録が事実と逆になる。
- **提案**: `--report` の出力に観点ごとの dropped を印字する。あわせて書式の `<観点名>` と出力の単位を揃える（どちらか一方に決めて、正本の対応を二重に持たない）。

### `writing/inaccurate-rationale` — docstring と DR は接頭辞を「フォールバック」と説明するが、実装は常に和集合を取る

- **場所**: `scripts/select-review-findings.mjs`:15（docstring）, :205-217（`targetsFor`）, `docs/decisions/0055-prior-review-selected-per-perspective.md`
- **指摘した観点**: 日本語ドキュメント / 決定記録（DR）との整合（2観点から独立に挙がった）
- **累計**: 19回目
- **何が起きているか**: docstring は接頭辞を「1 が無い・読めないときの土台」と書き、DR の却下案も「欄のほうが実データに近い」と欄を優先する立場を述べる。実装は `[...new Set([...fromField, ...fromPrefix])]` で、欄が読めていても接頭辞の対応先を常に足す。`basis: 'field+prefix'` という値がその存在を示している。
- **なぜ問題か**: 説明どおりなら `指摘した観点: 日本語ドキュメント` だけを持つ `contract/xxx` は writing だけへ渡る。実装では contract へも渡る。渡す量（DR が測った55%）もこの差で変わる。説明を信じて後から手を入れる人が、ありもしないフォールバック分岐を前提に壊す。
- **失敗シナリオ**: pr-61.md では、欄に挙がっていない観点が接頭辞由来で8件追加されている。
- **提案**: docstring と DR を実装に合わせる（「接頭辞は欄と併せて読み、両方の対応先へ渡す」）。あるいは実装を説明に合わせる。どちらを採るかは DR の決定として書く。

### `contract/recorded-value-not-what-was-passed` — 記録する「渡した量」を、渡した後に追記されたファイル自身から測る手順になっている

- **場所**: `.claude/skills/pr-review/references/review-file-format.md`:48-56, `.claude/skills/pr-review/SKILL.md` 手順6
- **指摘した観点**: 設計契約との整合 / 日本語ドキュメント（2観点から独立に挙がった）
- **累計**: 1回目
- **何が起きているか**: 新設した節は `--review=<このファイル> --report` の結果を書けと指示する。だが手順2 で選別したのは**前周までの内容**で、手順6 でこの節を書く時点のファイルには**その周の指摘が追記済み**になる（「既存の節を消さない」規定）。同じコマンドを走らせても、手順2 が実際に渡したものとは別の集合を測る。
- **なぜ問題か**: DR-0055 決定5 の検証材料が、実際に渡した量と別の値になる。渡していない（その周に新しく出た）指摘が「渡さなかった指摘」として並ぶと、次の周の判定材料が壊れる。
- **失敗シナリオ**: 2周目の記録で `--report` は1周目＋2周目の指摘を数える。2周目に出た指摘が「渡さなかった」として記録されるが、実際にはその周に存在すらしていなかった。
- **提案**: 「手順2 で実行した出力をそのまま残す。手順6 で測り直さない」と時点を書式側に明記する。あるいはスクリプトへ周を指定する引数を足し、追記後でも手順2 と同じ集合を再現できるようにする。

### `code/test-misses-core-path` — テストが実在のレビュー記録を1本も通しておらず、書式のずれを検出できない

- **場所**: `scripts/select-review-findings.test.mjs`:29-31（`review()` ヘルパ）, :85-127
- **指摘した観点**: コード品質 / 検査ルールの実効性（2観点から独立に挙がった）
- **累計**: 4回目
- **何が起きているか**: `parseFindings` のテストはすべて手書きの合成文字列で、そのすべてが新しい書式で書かれている。`docs/reviews/` の実ファイルを1本も読んでいない。`loadPrefixMap` / `loadPerspectiveMap` は正本を読むテストを持つのに、唯一の実入力である「レビュー記録」だけが合成データしか通らない。フィクスチャの観点名も、括弧を含む正本の名前を避けた3つになっている。CLI も未テスト。
- **なぜ問題か**: 上の blocker 2件は、いずれもテストが緑のまま成立する。実際 `pnpm test` は24件すべて通る一方、実データでは239件の指摘が消える。テストが固定しているのは「合成した1つの書き方なら分解できる」ことであって、名前が主張する「レビュー記録を指摘単位へ分解する」ではない。
- **失敗シナリオ**: 上の blocker がそのまま実例。
- **提案**: 実ファイル（複数周を含む pr-17 / pr-58、旧形式の pr-60、新形式の pr-61）を読み、「カテゴリID形の見出し数と `parseFindings` の件数が一致する」ことを不変条件として固定する。`loadPerspectiveMap()` の実値を使った照合テストを1本置く（括弧を含む名前が解決すること）。解析0件で黙って空を返さないことのテストも置く。

### `contract/value-outside-source-of-truth` — 重要度の語彙が正本から実装へ書き写されている

- **場所**: `scripts/select-review-findings.mjs`:30, :201, 正本は `.claude/skills/pr-review/references/finding-format.md`
- **指摘した観点**: 設計契約との整合
- **累計**: 6回目
- **何が起きているか**: 接頭辞と観点名の対応は正本から実行時に読んでいるのに、重要度の3値だけは実装へ直書きされている。この PR は同じ `finding-format.md` に「この対応は機械が読む唯一の場所になる」と書いている。
- **なぜ問題か**: 重要度の語が増減・改称されたとき、接頭辞側は表を書き換えれば追従するが重要度は片方だけが古くなる。失敗の出方も同じで、認識できない重要度節の下の指摘は静かに0件になり、`blocker` 全観点配布（決定3）も効かなくなる。
- **失敗シナリオ**: 重要度表に語を足して記録がそれを使うと、その節の指摘は拾われず全観点へ渡らない。表を正本と宣言しているのにエラーも出ない。
- **提案**: 重要度も正本から読む。読まない選択をするなら、その理由をスクリプト冒頭と正本側の両方へ残す。

### `inspection/rule-coverage-partial` — 接頭辞の対応先が観点表に無いファイルを指すと、その指摘はどこへも渡らず黙って消える

- **場所**: `scripts/select-review-findings.mjs`:212-217, :237
- **指摘した観点**: 検査ルールの実効性
- **累計**: 12回目
- **何が起きているか**: `targetsFor` は「解決先が0件」のときだけ全観点へ退避する。解決先が1件以上あっても、それが `loadPerspectiveMap()` の値集合に含まれないファイルなら、全観点で `dropped` に入る。例外も警告も出ない。`loadPrefixMap` / `loadPerspectiveMap` は `size === 0` のときしか落ちないので、部分的なズレは検出されない。
- **なぜ問題か**: 2つの表を別ファイルの正本として意図的に分けた以上、片方だけが変わる形（観点の統廃合、指示ファイルの改名）が起こりうる。そのとき壊れ方が「例外で落ちる」ではなく「その接頭辞の指摘が全レビュアーから消える」になっている。
- **失敗シナリオ**: `agents/code-quality.md` を改名して観点表だけ更新し `finding-format.md` を直し忘れると、以後 `code/*` の指摘は blocker でない限り誰にも渡らず、`--report` の削減率だけが良くなる。
- **提案**: 起動時に `loadPrefixMap` の値集合が `loadPerspectiveMap` の値集合に含まれることを検査して落とす。少なくとも「解決先が `allTargets` に一つも無い」場合を `unresolved` と同じ扱いにする。

### `writing/contradictory-instruction` — 「『なし』で省略しない」が、同じファイルのテンプレート「（無ければ「なし」）」と食い違う

- **場所**: `.claude/skills/pr-review/references/review-file-format.md`:91, :56
- **指摘した観点**: 日本語ドキュメント
- **累計**: 11回目
- **何が起きているか**: 注意書きは「渡さなかった指摘を「なし」で省略しない」と書き、テンプレートは「（無ければ「なし」）」と書く。既存の並行ルール（「落とした指摘の節を省略しない。ここが空でも「なし」と書く」）は後者の立場。
- **なぜ問題か**: 「『なし』で省略しない」は文字どおり読めば「『なし』と書いて済ませるな」になり、テンプレートと衝突する。この節を書くのは AI なので、どちらに従うかで記録の形が周ごとに揺れる。
- **失敗シナリオ**: dropped ゼロの周で、テンプレートに従えば「なし」、注意書きに従えば節ごと書かない。後の周で空欄が「渡した」なのか「記録し忘れ」なのか判別できない。
- **提案**: 落とした指摘の文と同じ形に揃える。「渡さなかった指摘の**行**を省略しない。無ければ「なし」と書く」。

### `writing/contradictory-instruction`（同一カテゴリのため計上は1回） — `## 渡した前周の指摘` が初回用テンプレートにだけ置かれ、周の節テンプレートに無い

- **場所**: `.claude/skills/pr-review/references/review-file-format.md`:49-56, :89, :78-84
- **指摘した観点**: 日本語ドキュメント
- **何が起きているか**: 新しい節は初回の骨格テンプレートの中に追加され、その中の注記は「再レビューのみ。初回は節ごと置かない」と言う。一方、置くべき場所である `## <N>周目` のテンプレートにはこの節が現れない。
- **なぜ問題か**: 「初回には置くな」と書かれた節が初回用テンプレートの中にだけ存在する。`##` は周の節と同じ深さなので、周の節の下に置くと周の節を閉じてしまう。見出しの形が揺れれば機械でも拾えない。
- **失敗シナリオ**: 3周目の統合役が初回テンプレートの位置へ追記し、2周目のぶんを書く場所が無く上書きになる。
- **提案**: `## <N>周目` のテンプレートの中へ移す（`###` にするか `## 渡した前周の指摘（<N>周目）` のように周を明示する）。冒頭テンプレートからは外す。

### `decisions/dr-missing-required-section` — DR-0055 に `## 理由` と `## 検討した他の選択肢` が無い

- **場所**: `docs/decisions/0055-prior-review-selected-per-perspective.md`, `docs/decisions/README.md`「新しい決定を追加するとき」手順2
- **指摘した観点**: 決定記録（DR）との整合
- **累計**: 1回目
- **何が起きているか**: README 手順2 は「文脈 / 決定 / 理由 / 検討した他の選択肢と却下理由 / 帰結」を書くと定める。DR-0055 の節は `## 文脈` / `## 決定` / `## 却下した案` / `## 帰結` で、`## 理由` が無く、`## 検討した他の選択肢` の代わりに `## 却下した案` を置いている。全55本を走査すると、**54本が `## 理由` と `## 検討した他の選択肢` を持ち、欠くのは DR-0055 だけ**。`decisions:check` は節名を見ないので素通りする。
- **なぜ問題か**: 節名が揃っていないと、DR を横断して理由を読むことも機械で拾うこともできない。理由は決定の本文へ太字で埋め込まれているが、決定の記述と混ざっており、置き換え運用に乗らない。
- **失敗シナリオ**: 該当なし（設計上の問題）。
- **提案**: `## 理由` / `## 検討した他の選択肢` へ揃える。意図的に形を変えるなら README 手順2 を先に変える。

### `decisions/undocumented-decision` — 欄と接頭辞をどう合成するかが DR に無い

- **場所**: `scripts/select-review-findings.mjs`:205-213, `docs/decisions/0055-prior-review-selected-per-perspective.md`
- **指摘した観点**: 決定記録（DR）との整合
- **累計**: 17回目
- **何が起きているか**: DR は「欄と接頭辞の2つを読む」とは書くが、両方が読めたときにどう合わせるかを書いていない。実装は和集合を取る。
- **なぜ問題か**: 「欄が読めたらそれだけを使う」という選択肢が実在し（却下案が「欄のほうが実データに近い」と書いている以上、こちらも自然な読み）、削減率が変わる。#65 のベースライン比較の土台も揺れる。
- **失敗シナリオ**: 該当なし（設計上の問題）。
- **提案**: 決定3 へ「欄と接頭辞は和集合を取る」を足し、その理由（迷ったら渡す側へ倒す）を明記する。

### `writing/notation-inconsistency` — DR-0055 の「83%まで落ちる」が削減率とも残存率とも読める

- **場所**: `docs/decisions/0055-prior-review-selected-per-perspective.md`
- **指摘した観点**: 日本語ドキュメント
- **累計**: 9回目
- **何が起きているか**: 「削減 55%」の直後に「接頭辞で各指摘を1観点へ割り当てるだけなら 83%まで落ちる」とある。「83%まで落ちる」は字面では残存率（削減17%）とも読め、「これにあたる」の指す先も 83% とも 55% とも読める。
- **なぜ問題か**: 削減55%はこの決定の唯一の定量的根拠で、#65 の増分評価の基準になる。率の意味が読み取れないと同じ土俵で比べられない。
- **失敗シナリオ**: #65 で「接頭辞だけなら残りが83%」と読み、選別の効きを過小に見積もって別の設計を選ぶ。
- **提案**: 率の意味と「これ」の指す先を明示する。

### `writing/incomplete-pr-description` — PR 本文が「作業中。」のまま

- **場所**: PR #66 本文
- **指摘した観点**: 日本語ドキュメント
- **累計**: 8回目
- **何が起きているか**: PR は Draft ではないが本文は「作業中。」で終わっている。本文の要約は接頭辞による割り当てしか述べておらず、実装の主軸である `指摘した観点` 欄・blocker の全観点配布・記録の義務化に触れていない。
- **なぜ問題か**: 完成度と残りが本文から判断できない。DR-0055 決定3 の存在も読み取れない。
- **失敗シナリオ**: 該当なし（設計上の問題）。
- **提案**: 手順6（PR 本文を整える）で書き直す。

## consider

### `decisions/cited-tally-not-reproducible` — `before` が「全文」を名乗るが、実体は指摘本文の合計

- **場所**: `scripts/select-review-findings.mjs`（`summarize` の `fullSize`, `--report` の表示）, `.claude/skills/pr-review/references/review-file-format.md`:53, `docs/decisions/0055-prior-review-selected-per-perspective.md`
- **指摘した観点**: コード品質 / 決定記録（DR）との整合（2観点から独立に挙がった）
- **累計**: 3回目
- **何が起きているか**: `before` は指摘の `text` の合計 × 観点数で、要約・落とした指摘・ルール化候補・周の見出しを含まない。pr-61.md ではファイル全体 45,224 文字に対し `fullSize` は 39,633 文字。全文基準なら削減は61%、表示は55%。選別前に渡していたのはファイル全文なので、「全文を配ると」と名乗る値がその全文ではない。誤差は削減を小さく見せる向きなので過大申告ではない。
- **なぜ問題か**: #65 がこの数値をベースラインに使う。分母が読めないと同じ土俵で測れない。
- **失敗シナリオ**: 該当なし（数値の意味づけの問題）。
- **提案**: `before` を全文基準にするか、表示と書式の文言を「指摘本文を全観点へ配ると」に改め、返り値名も意味の分かる名前へ変える。

### `writing/subjectless-predicate` — 手順2 の「どちらも読めない指摘」が何を指すか一意に読めない

- **場所**: `.claude/skills/pr-review/SKILL.md` 手順2
- **指摘した観点**: 日本語ドキュメント
- **累計**: 4回目
- **何が起きているか**: 「`blocker` は全観点へ渡り、複数観点から挙がった指摘はその全部へ渡り、どちらも読めない指摘も全観点へ渡る」。「どちらも」の直前の2項目はその2つで、そう読むと意味を成さない。意図している対象は1文前にある。DR-0055 決定3 は「欄も接頭辞も読めない指摘は」と明示しており、SKILL.md だけが省略形。
- **なぜ問題か**: 手順2 は渡し漏れの条件を定める契約文で、SKILL.md 単体で確認できない。
- **失敗シナリオ**: 該当なし（設計上の問題）。
- **提案**: DR-0055 決定3 と同じく書き下す。

### `writing/term-inconsistency` — 同一文中で「プレフィックス」と「接頭辞」が同じものを指す

- **場所**: `.claude/skills/pr-review/references/finding-format.md`
- **指摘した観点**: 日本語ドキュメント
- **累計**: 17回目
- **何が起きているか**: 「観点の**プレフィックス**は次を使う。右列は、その**接頭辞**を使う観点の指示ファイル」。表の見出しも DR もスクリプトも「接頭辞」で、この差分で書き換えた文にだけ「プレフィックス」が残っている。
- **なぜ問題か**: この段落は「機械が読む唯一の正本」を宣言する場所で、その正本の名前が1文の中で2通りになっている。
- **失敗シナリオ**: 該当なし（設計上の問題）。
- **提案**: 「接頭辞」に統一する。

### `contract/contract-structure-duplicated` — 観点名の一覧が SKILL.md の frontmatter にも残っている

- **場所**: `.claude/skills/pr-review/references/finding-format.md`, `.claude/skills/pr-review/SKILL.md`（`description` と観点表）
- **指摘した観点**: 設計契約との整合
- **累計**: 9回目
- **何が起きているか**: この PR が「観点名の正本は SKILL.md 手順4 の観点表」と明文化した。だが同じ SKILL.md の frontmatter が観点名の一覧と件数を独立に持っており、括弧の全角・半角も既にずれている。
- **なぜ問題か**: description 側は元からあるが、この PR が観点表を「機械が読む正本」に格上げしたことで二重化が実害を持つ側面へ移った。観点名を改称して観点表だけ直すと、description を見て書かれた欄が無効値になり、決定3 の2つ目が静かに外れる。
- **失敗シナリオ**: 上記。
- **提案**: description から観点名の列挙を落とすか、観点表の表記へ揃える縛りを明記する。

### `contract/source-of-truth-ambiguous` — `SKILL.md` の指し先にスキル名もリンクも無い

- **場所**: `.claude/skills/pr-review/references/finding-format.md`
- **指摘した観点**: 設計契約との整合
- **累計**: 10回目
- **何が起きているか**: 新しい記述は2箇所とも「`SKILL.md` 手順4 の観点表」とだけ書く。リポジトリには複数の `SKILL.md` があり、同じ事柄を指す `review-file-format.md` の既存記述は「`pr-review/SKILL.md` の観点表」とスキル名付き。リンクも無い。
- **なぜ問題か**: 「正本はここ」と宣言する文が一意に読ませないと、宣言そのものが機能しない。実装は決め打ちで読むので、実装と文書で指し先の確かさに差がある。
- **失敗シナリオ**: 該当なし（設計上の問題）。
- **提案**: 既存表記に合わせるか、相対リンクを張る。

### `decisions/alternatives-cover-only-one-decision` — 却下案が決定5 を覆っていない

- **場所**: `docs/decisions/0055-prior-review-selected-per-perspective.md`
- **指摘した観点**: 決定記録（DR）との整合
- **累計**: 1回目
- **何が起きているか**: 却下した案の3件は決定1〜3 に対応するが、決定5（渡さなかった指摘を記録する）には代案の記録が無い。
- **なぜ問題か**: 決定5 は手順6 と書式に新しい節を増やす決定で、記録しない選択肢も実在した。代案が無いと、記録のコストが問題になったときに再検討の起点が残らない。
- **失敗シナリオ**: 該当なし（設計上の問題）。
- **提案**: 「記録しない」を却下案として足す。

## 落とした指摘

- **「実在のレビュー記録はすべて『決定記録との整合』と書いており、正本の `決定記録（DR）との整合` は一致しない」**（決定記録、blocker の根拠として）— **根拠が事実と異なる。** 実データには `決定記録（DR）との整合` が28箇所ある（`決定記録との整合` 60箇所、`決定記録` 27箇所）。ただし**指摘本体は成立し、むしろ報告より重い**（表記が3通りに割れているうえ、正本の表記自体が注記剥がしで壊れる）。根拠を訂正のうえ `inspection/canonical-value-unmatchable` として採用した
- **`REPO_ROOT` の算出が `scripts/lib/bypass-fixtures.mjs` と重複**（コード品質、報告者自身が指摘に挙げなかったもの）— 1行の定数であり、既知の `code/duplicate-resolve-readjson-helpers` が指す「走査基盤の複製」とは規模が違う。統合側でも同じ判断を採る

## ルール化候補

- `inspection/rule-has-bypass` — 通算20回。既に [DR-0044](../decisions/0044-bypass-fixtures-required.md) / #43 で仕組み化済みだが、**その仕組みは `design/rules.json` のスライド検査ルールを対象にしており、`scripts/` の解析器には及んでいない**。書式を読む解析器に対しても、抜け道の事例（bypass フィクスチャ）を要求する形へ広げる候補
- `decisions/consequence-not-followed` — 通算13回。[DR-0054](../decisions/0054-decision-reference-checked-by-machine.md) で「意味の問題で機械判定に向かない」として見送り済み
- `writing/inaccurate-rationale` — 通算19回。同上
- `decisions/undocumented-decision` — 通算17回。同上
- `writing/term-inconsistency` — 通算17回。#44（用語集を正本として置く）が扱う
- `inspection/rule-coverage-partial` — 通算12回
- `writing/contradictory-instruction` — 通算11回
- `phase/completion-criterion-not-verified-e2e` — 通算10回
- `contract/source-of-truth-ambiguous` — 通算10回

## 2周目（2026-09-24）

- **立った観点**: 設計契約との整合 / 検査ルールの実効性 / 決定記録（DR）との整合 / フェーズスコープ / 日本語ドキュメント / コード品質

対象は `e8fa1dd..b250fd2`（1周目のレビュー時点からの増分）。

**1周目の blocker 3件はすべて塞がった。** 3観点（設計契約・検査・コード品質）が独立に、実データ27本へスクリプトを走らせて確認している。取りこぼしは 239件 → 1件（`pr-55.md` の1件は、記録側が重要度の節を持たないまま指摘を書いている実在の書式崩れで、ファイル単位の縮退が正しく発動する）。`決定記録（DR）との整合` の照合も、3通りの表記すべてで解決することを確認した。

blocker なし。should 5件、consider 1件。**うち2件は、1周目の修正そのものが作った穴**——決定番号の振り直しに引用が追従していないことと、blocker の回帰テストが blocker を再現しても緑のまま通ること。

### 渡した前周の指摘（2周目）

**この周から選別が有効になった。** 以下は手順2 で実行したときの出力。

- **選別前に渡していた量**: 97,704 文字 / **選別後**: 37,489 文字（削減 62%）
- 内訳: 日本語ドキュメント 12件 / 決定記録（DR）との整合 9件 / 設計契約との整合 7件 / 検査ルールの実効性 5件 / コード品質 5件 / フェーズスコープ 4件（指摘21件・観点6）
- **縮退**: 起きていない（21件すべてを指摘として読めた）
- **渡さなかった指摘**:
  - `設計契約との整合` に `decisions/consequence-not-followed`（should） / `writing/inaccurate-rationale`（should） / `code/test-misses-core-path`（should） / `inspection/rule-coverage-partial`（should） / `writing/contradictory-instruction`（should、2件） / `decisions/dr-missing-required-section`（should） / `decisions/undocumented-decision`（should） / `writing/notation-inconsistency`（should） / `writing/incomplete-pr-description`（should） / `decisions/cited-tally-not-reproducible`（consider） / `writing/subjectless-predicate`（consider） / `writing/term-inconsistency`（consider） / `decisions/alternatives-cover-only-one-decision`（consider）
  - `検査ルールの実効性` に `decisions/consequence-not-followed`（should） / `writing/inaccurate-rationale`（should） / `contract/recorded-value-not-what-was-passed`（should） / `contract/value-outside-source-of-truth`（should） / `writing/contradictory-instruction`（should、2件） / `decisions/dr-missing-required-section`（should） / `decisions/undocumented-decision`（should） / `writing/notation-inconsistency`（should） / `writing/incomplete-pr-description`（should） / `decisions/cited-tally-not-reproducible`（consider） / `writing/subjectless-predicate`（consider） / `writing/term-inconsistency`（consider） / `contract/contract-structure-duplicated`（consider） / `contract/source-of-truth-ambiguous`（consider） / `decisions/alternatives-cover-only-one-decision`（consider）
  - `決定記録（DR）との整合` に `contract/recorded-value-not-what-was-passed`（should） / `code/test-misses-core-path`（should） / `contract/value-outside-source-of-truth`（should） / `inspection/rule-coverage-partial`（should） / `writing/contradictory-instruction`（should、2件） / `writing/notation-inconsistency`（should） / `writing/incomplete-pr-description`（should） / `writing/subjectless-predicate`（consider） / `writing/term-inconsistency`（consider） / `contract/contract-structure-duplicated`（consider） / `contract/source-of-truth-ambiguous`（consider）
  - `フェーズスコープ` に `writing/inaccurate-rationale`（should） / `contract/recorded-value-not-what-was-passed`（should） / `code/test-misses-core-path`（should） / `contract/value-outside-source-of-truth`（should） / `inspection/rule-coverage-partial`（should） / `writing/contradictory-instruction`（should、2件） / `decisions/dr-missing-required-section`（should） / `decisions/undocumented-decision`（should） / `writing/notation-inconsistency`（should） / `writing/incomplete-pr-description`（should） / `decisions/cited-tally-not-reproducible`（consider） / `writing/subjectless-predicate`（consider） / `writing/term-inconsistency`（consider） / `contract/contract-structure-duplicated`（consider） / `contract/source-of-truth-ambiguous`（consider） / `decisions/alternatives-cover-only-one-decision`（consider）
  - `日本語ドキュメント` に `code/test-misses-core-path`（should） / `contract/value-outside-source-of-truth`（should） / `inspection/rule-coverage-partial`（should） / `decisions/dr-missing-required-section`（should） / `decisions/undocumented-decision`（should） / `decisions/cited-tally-not-reproducible`（consider） / `contract/contract-structure-duplicated`（consider） / `contract/source-of-truth-ambiguous`（consider） / `decisions/alternatives-cover-only-one-decision`（consider）
  - `コード品質` に `decisions/consequence-not-followed`（should） / `writing/inaccurate-rationale`（should） / `contract/recorded-value-not-what-was-passed`（should） / `contract/value-outside-source-of-truth`（should） / `inspection/rule-coverage-partial`（should） / `writing/contradictory-instruction`（should、2件） / `decisions/dr-missing-required-section`（should） / `decisions/undocumented-decision`（should） / `writing/notation-inconsistency`（should） / `writing/incomplete-pr-description`（should） / `writing/subjectless-predicate`（consider） / `writing/term-inconsistency`（consider） / `contract/contract-structure-duplicated`（consider） / `contract/source-of-truth-ambiguous`（consider） / `decisions/alternatives-cover-only-one-decision`（consider）

**渡さなかったことによる再生産は、この周では観測されなかった。** 2周目の指摘6件のうち、1周目と同じカテゴリで再び出たのは `decisions/wrong-dr-citation` と `writing/ambiguous-criterion` の2件だが、どちらも**1周目にその観点へ渡した指摘の中に含まれていた**（前者は決定記録へ9件、後者は日本語へ12件渡している）。渡し損ねが原因の再生産ではない。

## blocker（2周目）

なし。

## should（2周目）

### `decisions/wrong-dr-citation` — DR-0055 の決定番号を振り直したのに、引用側が追従していない

- **場所**: `.claude/skills/pr-review/SKILL.md`:170, `.claude/skills/pr-review/references/review-file-format.md`:89, `scripts/select-review-findings.mjs`:352, Issue #69 本文
- **指摘した観点**: 決定記録（DR）との整合
- **累計**: 6回目
- **何が起きているか**: 1周目の指摘を受けて DR-0055 を全面的に書き直した際、決定を5個から7個へ振り直した。旧「決定3 迷ったら渡す」は新「決定4」へ、旧「決定5 渡さなかった指摘を記録する」は新「決定7」へ移った。引用側は旧番号のまま。`review-file-format.md`:89 は**この増分でまさに改稿した行**で、旧番号を新しい行へ持ち越している。
- **なぜ問題か**: 番号を開くと、引用元が主張する内容と違う決定が出てくる。DR は決定の追跡可能性のために番号を振っており、ずれると「どの判断の根拠か」を人にも機械にも確認できない。既存の問題ではなく、**この増分（決定の並べ替え）が作った**。
- **失敗シナリオ**: `SKILL.md`:170 の「決定5」を確認しに行った人が、フォールバックの話（新決定5）を読み、記録の根拠になっていないことに気づかず誤解するか、DR 側の記述ミスと誤認して DR を直す。
- **提案**: `SKILL.md`:170 と `review-file-format.md`:89 を決定7 へ、`select-review-findings.mjs`:352 を決定4 へ直す。Issue #69 の「決定3」も決定4 へ訂正する。**`docs/reviews/` の過去の引用は書き換えない**（書き換えない履歴であり、その時点で何を根拠に指摘したかの記録だから）。報告は3箇所だったが、統合時に `select-review-findings.mjs`:352 を追加で見つけた。

### `code/test-cannot-detect-regression` — 1周目の blocker への回帰テストが、その blocker を再現しても緑のまま通る

- **場所**: `scripts/select-review-findings.test.mjs`:220-231（`括弧を含む観点名から挙がった指摘が、実記録でもその観点へ渡る`）
- **指摘した観点**: 検査ルールの実効性
- **累計**: 5回目
- **何が起きているか**: このテストは `pr-61.md` から観点名が `決定記録` で始まる指摘を抜き、`agents/decisions.md` へ届くことを検証する。`buildLookup` を正規化なしの素のマップ（＝1周目の壊れた実装）に差し替えて同じ17件を通すと、**17件すべてが変わらず届く**。実測した。

  ```
  正しい実装: 17件届く  {"field+prefix":16, "broadcast":1}
  壊れた実装: 17件届く  {"unresolved-perspective":16, "broadcast":1}
  ```

  届く経路が「欄の解決」から「欄が解決できないので全観点へ broadcast」（決定4）へ変わるだけで、到達という結果は同じになる。
- **なぜ問題か**: 1周目の blocker（`inspection/canonical-value-unmatchable`）の再発防止だと読める名前のテストが、別の安全弁で偶然通っているだけで、正規化ロジックを固定していない。**修正を守るはずのテストが守れていない。** 同じ `describe` の直前にある `正本の観点名が、そのままでも注記付きでも括弧無しでも同じ観点へ解決する` は `buildLookup` を直接検証しており、そちらは有効。
- **失敗シナリオ**: 誰かが `normalizePerspectiveName` を「余計な処理だ」と外しても、`pnpm test` は緑のまま通る。1周目の blocker がそのまま戻る。
- **提案**: 到達だけでなく `basis` も併せて固定する（`field` または `field+prefix` であり `unresolved-perspective` でないこと）。あるいは冗長なので削除し、直前の直接検証に一本化する。

### `inspection/perspective-separator-splits-annotation` — 区切りに読点を足したことで、注記に読点を含む欄が破片化する

- **場所**: `scripts/select-review-findings.mjs`:54（`PERSPECTIVE_SEPARATOR`）, `splitPerspectiveField`
- **指摘した観点**: 検査ルールの実効性
- **累計**: 1回目
- **何が起きているか**: 1周目の指摘を受けて区切りに `、` `,` を足した。ところが注記の中にも読点は現れる。`docs/reviews/pr-25.md` の実データで確認した。

  ```
  入力: 検査ルールの実効性、決定記録（DR-0011 の帰結が…実装されていない、という角度から。複数観点から指摘された）
  出力: ["検査ルールの実効性",
        "決定記録（DR-0011 の帰結が…実装されていない",
        "という角度から。複数観点から指摘された）"]
  ```

  後ろ2つはどちらも単独では意味をなさず、片方は閉じ括弧だけが残る。同種の破片化は `/` 由来でも `pr-52` / `pr-53` / `pr-55` で起きる（こちらは1周目からの既存挙動）。
- **なぜ問題か**: 決定4 の安全弁が吸収するので**指摘は消えない**（破片は必ず解決できず全観点へ渡る。実測で確認した）。ただし**選別が効かず常に全観点へ渡る**ので、機能の目的そのものが無効になる。しかも「なぜ選別されなかったか」が記録にも警告にも残らない。安全弁への依存が暗黙で、後から `targetsFor` の unresolved 処理を変える人に手掛かりが無い。
- **失敗シナリオ**: 該当なし（取りこぼしではなく、選別効果が出ないだけ）。
- **提案**: 注記（最初の `（` から行末まで）を先に丸ごと外してから本体を区切る。あるいは既知の観点名の集合に対する最長一致で分割する。いずれにせよ、解決できなかった断片があったことを記録に残す経路を足す。**恒久的な形の固定は [#67](https://github.com/nashiusagi/slide-design-system/issues/67) が扱う。**

### `code/sibling-field-test-gap` — `--report` の終了コードを決める `summarize().fallback` にテストが無い

- **場所**: `scripts/select-review-findings.mjs`（`summarize` / `main` の `--report` 分岐）, `scripts/select-review-findings.test.mjs`（`summarize` の describe）
- **指摘した観点**: コード品質
- **累計**: 4回目
- **何が起きているか**: `selectForPerspective()` の `fallback` / `unparsed` はテストされている。しかし `summarize()` は**別に自分で** `countUnparsedHeadings` を呼んで同名のフィールドを計算しており、`--report` の「NG」表示と `process.exitCode = 1` はそちらだけを見ている。その `summarize().fallback` / `.unparsed` を直接確かめるテストが無い。
- **なぜ問題か**: この縮退は1周目 blocker への直接の対策で、「検査が黙って通らない」ための安全弁だ。そのトリガー値が、兄弟にあたる値はテストされているのに素通りしている。
- **失敗シナリオ**: `summarize()` を「`selectForPerspective` を6回呼ぶのは無駄だ」とリファクタして `unparsed` の集計を誤っても、既存テストは1本も落ちない。`--report` は縮退しているファイルに対して終了コード0 のまま「NG」も出さなくなる。**1周目 blocker が指摘した「検査が黙って通ったことになる」形を、その対策自身が再演する。**
- **提案**: `summarize()` の describe へ、`unparsed` が非ゼロになる入力（重要度の節の外に指摘がある記録、または `docs/reviews/pr-55.md`）で `summarize(...).fallback` と `.unparsed` を直接確かめるテストを足す。

### `writing/ambiguous-criterion` — docstring の「渡し損ねを黙って起こさない」3項目目が、実装の分岐を言い表していない

- **場所**: `scripts/select-review-findings.mjs`:19-23（冒頭 docstring）
- **指摘した観点**: 日本語ドキュメント
- **累計**: 12回目
- **何が起きているか**: docstring は3項目目を「接頭辞も解決できなかった指摘」と書く。単独で読むと「接頭辞が未知なら、それだけで全観点へ渡る」と読めるが、実装は欄からの候補も空である場合に限る。実測で確認した。

  ```
  欄は解決 / 接頭辞は未知 → { targets: ['agents/writing.md'], basis: 'field' }   （全観点ではない）
  ```

  同じことを述べる `SKILL.md` 手順2 は「**欄も**接頭辞も解決できなかった指摘」と両方を明記しており、そちらは実装と一致する。docstring だけが「欄も」を落としている。
- **なぜ問題か**: 1周目に `writing/inaccurate-rationale` が指摘したのと**同じ場所・同じ構造**の食い違い。直すときに片方だけ直した。この docstring は選別ロジックの説明の正本にあたる場所で、誤った実装理解を招く。
- **失敗シナリオ**: 該当なし（設計上の問題）。
- **提案**: `SKILL.md` 手順2 と同じ表現へ揃える。

## consider（2周目）

### `inspection/fallback-false-positive-on-dropped-headings` — `落とした指摘` の節に見出し形式でカテゴリIDを書くと、ファイル全体が縮退する

- **場所**: `scripts/select-review-findings.mjs`（`countUnparsedHeadings`）, `.claude/skills/pr-review/references/review-file-format.md`（`## 落とした指摘` の規定）
- **指摘した観点**: 統合時の検証で確認（設計契約との整合が観点外として申し送ったもの）
- **累計**: 1回目
- **何が起きているか**: `落とした指摘` の節に `` ### `writing/b` — 成立しない `` のような見出しを書くと、パーサは正しく指摘として拾わない（拾うべきでない）が、`countUnparsedHeadings` は取りこぼしとして数える。結果、そのファイルは選別されず全文が渡る。実測で確認した。**実データ27本はすべて箇条書きで書いており、現に起きてはいない。**
- **なぜ問題か**: 書式は `落とした指摘` の書き方（箇条書きか見出しか）を定めていない。次に誰かが見出しで書くと、その PR の選別が丸ごと無効になる。壊れ方は安全側（全文を渡す）なので実害は小さいが、効かなくなったことに誰も気づかない。
- **失敗シナリオ**: 該当なし（安全側に倒れるため実行時の破損ではない）。
- **提案**: `review-file-format.md` で `落とした指摘` を箇条書きに固定する。あるいは `countUnparsedHeadings` が、重要度の節の外にある見出しのうち `落とした指摘` 配下のものを除く。**書式そのものの固定は [#67](https://github.com/nashiusagi/slide-design-system/issues/67) の守備範囲と重なる。**

## 落とした指摘（2周目）

なし。ただし1件、カテゴリを付け替えた。コード品質が `code/fix-lacks-regression-test` として挙げた「回帰テストが blocker を再現しても通る」は、`code/test-cannot-detect-regression`（テストが、その名前が示す回帰をフィクスチャの都合で検出できない）のほうが一致するため、そちらで計上した。前者は「修正に回帰テストが無い」、後者は「テストはあるが検出力が無い」で、今回は後者。

## ルール化候補（2周目）

- `code/test-cannot-detect-regression` — 通算5回。**レビューの指摘を直したときに足すテストが、その指摘を再現しても通る**という形が繰り返している。今回は「別の安全弁が結果を同じにするので、到達だけを見るテストでは区別が付かない」形。[#68](https://github.com/nashiusagi/slide-design-system/issues/68)（bypass フィクスチャを解析器へ広げる）が扱う範囲と重なる——指摘への修正にも「壊した状態で落ちること」を要求できないか
- `decisions/wrong-dr-citation` — 通算6回。今回は**決定番号の振り直しに引用が追従しない**形。`decisions:check` は DR 番号の実在とリンクの一致は見るが、`決定N` の N までは見ない。DR 内の決定の数と、引用側の `決定N` の N を突き合わせる検査は機械で書ける

## 3周目（2026-09-24）

- **立った観点**: 設計契約との整合 / 検査ルールの実効性 / 決定記録（DR）との整合 / フェーズスコープ / 日本語ドキュメント / コード品質

対象は `b250fd2..0549e87`（2周目のレビュー時点からの増分）。**上限の3周目。**

blocker なし。should 3件、consider 1件。**2周目の修正がいずれも「途中まで」だったという形で揃った**——DR の帰結を実装に追従させなかった、注記の剥がし方を末尾1つに限って見積もった、テストのタイトルが実際の検証対象とずれた。

2周目の指摘のうち、**回帰テストの件（`code/test-cannot-detect-regression`）と `summarize().fallback` の件（`code/sibling-field-test-gap`）は解消を確認した。** どちらも報告側が独立に、実装を壊してテストが落ちることを実測している。

### 渡した前周の指摘（3周目）

- **選別前に渡していた量**: 165,048 文字 / **選別後**: 48,663 文字（削減 71%）
- 内訳: 日本語ドキュメント 14件 / 決定記録（DR）との整合 11件 / 設計契約との整合 8件 / 検査ルールの実効性 8件 / コード品質 8件 / フェーズスコープ 5件（指摘27件・観点6）
- **縮退**: 起きていない
- **解決できなかった観点名**: `inspection/fallback-false-positive-on-dropped-headings` の「統合時の検証で確認」1件。安全弁で全観点へ渡っているので落ちてはいないが、この指摘については選別が効いていない
- **渡さなかった指摘**: 2周目と同じ顔ぶれに、2周目の6件が観点ごとに加わる（`--report` の出力による。個々の列挙は2周目の節と重複するため省く。省いてよいのは、この周に新しく渡さなくなった指摘が無いためで、`--report` の出力自体は手順2 の時点で控えてある）

**渡さなかったことによる再生産は、この周でも観測されなかった。** 3周目の指摘4件のうち、2周目と同じカテゴリで再び出たのは `inspection/perspective-separator-splits-annotation` の1件だが、これは**2周目にその観点（検査）へ渡した指摘**であり、渡し損ねではない。むしろ渡したからこそ「直り切っていない」と分かった。

## blocker（3周目）

なし。

## should（3周目）

### `writing/inaccurate-rationale`（この PR で計上済み） — DR-0055 の帰結が、2周目の実装変更に追従していない

- **場所**: `docs/decisions/0055-prior-review-selected-per-perspective.md`（帰結の「観点名の照合は…」の項）, `scripts/select-review-findings.mjs`（`TRAILING_ANNOTATION` / `splitPerspectiveField`）
- **指摘した観点**: 決定記録（DR）との整合
- **何が起きているか**: DR-0055 の帰結は「正本の観点名そのものが括弧を含みうるため、**注記だけを剥がす形にすると正本が照合できなくなる**」と書いている。これは「`splitPerspectiveField` では注記を剥がさず、照合側の正規化だけに頼る」という設計を前提にした文だ。ところが2周目の修正で、`splitPerspectiveField` は**行末で閉じる注記を先に剥がす**ようになった（docstring にも明記した）。「注記を剥がす／剥がさない」の二択ではない第三の選択肢を採ったのに、DR の帰結はそれを想定していない文言のまま残っている。
- **なぜ問題か**: DR だけを読んだ人は「注記は一切剥がさない設計」だと理解し、`splitPerspectiveField` を「DR に反する」として直そうとしかねない。1周目・2周目に指摘された「説明と実装の食い違い」を、**今度は DR 側でやった**。
- **失敗シナリオ**: 該当なし（設計上の食い違い）。
- **提案**: 帰結を現在の実装に合わせる。「行末の注記だけは分割前に外し、観点名の内部にある括弧は照合側の正規化に委ねる」。

### `inspection/perspective-separator-splits-annotation`（この PR で計上済み） — 末尾でない注記・複数の注記は、依然として破片化する

- **場所**: `scripts/select-review-findings.mjs`（`TRAILING_ANNOTATION`, `splitPerspectiveField`）
- **指摘した観点**: 検査ルールの実効性
- **何が起きているか**: 2周目の修正は「行末で閉じる注記1つ」だけを想定している。2つの形が残る。実データで確認した。

  ```
  docs/reviews/pr-55.md:43
  フェーズスコープ、検査ルールの実効性（あちらは `inspection/link-target-unchecked`
  として挙げた。同一の問題なので1件にまとめる）、設計契約

  → ["フェーズスコープ",
     "検査ルールの実効性（あちらは `inspection",     ← 観点名が壊れた
     "link-target-unchecked` として挙げた。…）",
     "設計契約"]
  ```

  注記の後にまだ観点名が続くため、末尾判定が効かない。注記の中の `/` が区切りと誤認される。もう一つは注記が複数並ぶ形（`…（Aの観点から、Bの観点から）（2周目に追加）`）で、最後の1つしか剥がさないため、残った注記の読点で割れる。
- **なぜ問題か**: 2周目の指摘は「注記の中の区切り文字で破片化する」という機序を挙げていた。今回の修正はその**具体例だけを直し、機序は塞ぎ切っていない**。該当は実データ28本中 pr-55 の1件だけで、しかもそのファイルは別要因で全文へ縮退するため実害が隠れているが、縮退しない記録で同じ形が書かれれば再発する。
- **失敗シナリオ**: 縮退しないレビュー記録に `検査ルールの実効性（他の指摘と同一のため1件にまとめる）、決定記録との整合` のような欄が書かれると、壊れた断片が「解決できなかった観点名」として `--report` に出て、その指摘の選別が効かなくなる。
- **提案**: 末尾から連続する注記をまとめて剥がせば複数注記の形は塞げる。**注記の後に観点名が続く形は正規表現では直らない**——既知の観点名への最長一致で分割する必要があり、それは [#67](https://github.com/nashiusagi/slide-design-system/issues/67) の守備範囲。**この PR で完全に直るとは言えない**ので、残課題として記録する。

### `writing/test-title-ambiguous` — 回帰テストのタイトルが、実際に与えている入力とずれている

- **場所**: `scripts/select-review-findings.test.mjs`（`括弧を含む観点名が、安全弁ではなく欄の解決として決定記録の観点へ渡る`）
- **指摘した観点**: 日本語ドキュメント
- **累計**: 10回目
- **何が起きているか**: タイトルは「括弧を含む観点名」が解決されることを固定すると読める。しかし実際にこのテストが `pr-61.md` から抜く16件の `指摘した観点` 欄は、**全件が `決定記録との整合`（括弧なし）**だった。括弧付きの `決定記録（DR）との整合` は同ファイルに4箇所あるが、いずれも `立った観点` 欄でパーサの読む対象ではない。実測で確認した。
- **なぜ問題か**: タイトルを読んだ人が「括弧付き表記の回帰はこれで守られている」と判断する。実際に守っているのは「括弧を含まない短縮表記が、安全弁ではなく欄の解決で決定記録へ渡ること」だ。**括弧を含むのは正本側の観点名**で、それを正規化するからこの短縮形が一致する——つまりテスト自体は正規化ロジックを固定しており（2周目に検査観点が、正規化を壊すとこのテストが落ちることを実測している）、**検出力は有る**。ずれているのはタイトルの指す対象だけ。
- **失敗シナリオ**: 該当なし（設計上の問題）。
- **提案**: タイトルとコメントを、実際に固定している不変条件に合わせる。括弧付き表記そのものを入力として与えたいなら、合成データを併用する。

## consider（3周目）

### `code/diagnostic-misattributes-cause` — `unresolvedPerspectives` が blocker まで拾い、実際とは違う理由を報告する

- **場所**: `scripts/select-review-findings.mjs`（`summarize` の `unresolvedPerspectives` 算出, `--report` の注意文）
- **指摘した観点**: 検査ルールの実効性 / コード品質（2観点から独立に挙がった）
- **累計**: 1回目
- **何が起きているか**: `unresolvedPerspectives` は `severity` を見ずに、欄の観点名が解決できない指摘をすべて集める。ところが `blocker` は `targetsFor` の最初の分岐で、**欄の解決を試す前に**全観点へ渡る（`basis: 'broadcast'`）。実測で確認した。

  ```
  severity='blocker', 指摘した観点='知らない観点'
  → targetsFor の basis: broadcast
  → unresolvedPerspectives: [{ categoryId:'writing/a', names:['知らない観点'] }]
  ```

  `--report` は「安全弁で全観点へ渡るので落ちてはいないが、選別は効いていない」と表示するが、blocker は欄が読めても読めなくても常に全観点へ渡る。この指摘について欄を直しても選別は変わらない。
- **なぜ問題か**: 過剰報告。これを見た人が「欄を直せば選別が効く」と誤解して効果の無い修正をする一方、**本当に選別が効いていないケース**（`should` / `consider` で欄が読めない）と見分けが付かず、ノイズに埋もれる。
- **失敗シナリオ**: 実データ28本には blocker × 欄未解決の組み合わせが無く、現時点では表示上の実害も出ていない。blocker の欄に短縮形や誤字が書かれた時点で起きる。
- **提案**: `unresolvedPerspectives` の集計から、全観点へ渡す重要度の指摘を除く。

## 落とした指摘（3周目）

なし。ただし2件、報告の根拠を訂正して採用した。

- **コード品質が「blocker の全観点配布は決定3」と書いた点** — 誤り。blocker の全観点配布は**決定4（迷ったら渡す）の1項目目**で、決定3 は「欄と接頭辞は和集合を取る」。`--report` の注意文が指す決定番号自体は正しく、ずれているのは「決定4 のどの項目か」だ。指摘の本体（過剰報告）は成立するので、根拠を訂正して採用した
- **日本語が「このテストは括弧を含む観点名を一度も入力として与えていない＝回帰を守っていない」と読める形で書いた点** — 前半は事実だが、後半は行き過ぎ。括弧を含むのは**正本側の観点名**であり、その正規化を壊すとこのテストは落ちる（2周目に検査観点が実測で確認済み）。**検出力は有る。** ずれているのはタイトルの指す対象だけなので、`writing/test-title-ambiguous` として採用した

## ルール化候補（3周目）

- `writing/inaccurate-rationale` — 通算19回。この PR だけで1周目・3周目の2回、**説明と実装の食い違い**が別の場所（docstring → DR の帰結）で出ている。修正のたびに、その修正を説明する文を追従させ損ねる形。[#68](https://github.com/nashiusagi/slide-design-system/issues/68)（bypass フィクスチャを解析器へ広げる）と同じ発想で、「変更した実装を説明している文書」を機械で突き合わせられないかが次の問い
- `writing/test-title-ambiguous` — 通算10回
