---
name: issue-workflow
description: GitHub Issue 番号を受け取り、ブランチ作成 → Draft PR → 実装 → Ready 化 → pr-review スキルによるレビュー → 指摘反映 のループ（最大3周）→ PR 本文整備 → 完了コメント までを一貫して進める。「#12 やって」「issue 34 を実装して」「このイシュー着手して」「チケット片付けて」のように Issue 番号や Issue の URL を伴う着手依頼には必ず使う。番号が明示されていなくても、既存の Issue を指して実装を頼まれたらまず Issue を特定してこのスキルに乗せる。単発の修正依頼（Issue が無い、その場のバグ直し）には使わない。
---

# Issue から PR 完了までの開発フロー

## このスキルが解こうとしている問題

実装そのものより、その前後が毎回抜ける。ブランチ名がばらつく、PR が実装の後追いで作られて途中経過が見えない、レビューを受けたのに指摘が半分だけ反映される、PR 本文が着手時のまま実態と食い違う、終わったのかどうかが Issue からも PR からも分からない。

どれも小さいが、積み重なると「何がどこまで終わったか」を人が読み取れなくなる。このスキルは**流れの順番と、各段階で残す痕跡**を固定する。実装の中身は Issue と設計正本が決めることで、ここでは扱わない。

レビューは `pr-review` スキルが持っている。ここでその仕事を再実装しない。**このスキルはレビューを呼び出し、その結果をコードへ反映し、いつ打ち切るかを決める役**に徹する。

## 手順

### 0. Issue を読む

```bash
gh issue view <N> --json number,title,body,labels,url
```

タイトル・本文・完了条件を読む。**完了条件（チェックリスト）があれば、それが実装のスコープになる。** 書かれていないことを勝手に足さない。

読んで曖昧な点があれば、この段階で人に聞く。ブランチを切った後に前提が崩れるのが一番高くつく。

### 1. ブランチ → 空コミット → push → Draft PR

ブランチ名は `feature/<issue番号>_<内容>` にする。`<内容>` は Issue タイトルから起こした短い英小文字スラグ（ハイフン区切り）。

```bash
git checkout main && git pull
git checkout -b feature/<N>_<slug>
git commit --allow-empty -m "chore: <何を始めるか>

Refs #<N>"
git push -u origin feature/<N>_<slug>
gh pr create --draft --title "<Issue タイトル>" --body "Closes #<N>

<何をやるかの一行>

作業中。"
```

**空コミットで先に PR を立てるのは、着手を可視化するため。** 実装が終わってから PR を作ると、走っている最中は誰にも見えない。本文の `Closes #<N>` はマージ時の Issue 自動クローズに必要なので必ず入れる。

Draft なのは、この時点ではレビュー対象になっていないから。人の目を無駄に呼ばない。

### 2. 実装 → Ready 化

Issue のスコープに沿って実装し、意味のある単位でコミットして push する。

このリポジトリで実装するなら、設計正本（`DESIGN.md` / `design/`）に従う。設計データを Skill やドキュメントへ複製しない。実装しながら判断したことがあれば、決定記録（`docs/decisions/`）に残すか、少なくとも PR 本文へ書く。

実装が一通り動く状態になったら Draft を外す。

```bash
gh pr ready <PR番号>
```

**Ready 化がレビューの開始線になる。** ここを越えるまでレビューを呼ばない。半端な差分をレビューさせると、指摘のほとんどが「まだ書いてないだけ」で埋まる。

### 3. レビュー

`pr-review` スキルを PR 番号付きで呼ぶ。レビューの観点も記録先も、あちらが持っている。ここでは結果だけ受け取る。

結果は `docs/reviews/pr-<N>.md` に残り、`blocker` / `should` / `consider` の重要度が付いている。

### 4. 指摘を反映する

- **`blocker`** — 必ず直す
- **`should`** — 原則直す
- **`consider`** — 判断する。直さないなら理由を残す

直したらコミットして push する。**直さないと決めた指摘は、黙って飛ばさない。** 何を見送ったかが残っていないと、次の周で同じ指摘が出たときに判断をやり直すことになる。見送りの理由は PR 本文かコメントに書く。

指摘が根本的な作り直しを示している場合は、パッチを重ねずに一度戻って作り直す方が早いことがある。周回数を消化することが目的ではない。

### 5. 3 と 4 を繰り返す

次のどちらかで抜ける。

- **指摘が無くなった**（`blocker` と `should` がゼロ）
- **3 周を終えた**

3 周で収束しないなら、それは実装の問題ではなく前提の問題であることが多い。粘るより人に渡す方が速い。

**打ち切った場合は、残った指摘を PR にコメントする。**

```bash
gh pr comment <PR番号> --body "<残指摘のまとめ>"
```

書式は `references/pending-comment.md`。何が残っていて、なぜ今回直さないのかを書く。レビューファイルへのリンクを添えると読み手が追える。

### 6. PR 本文を整える

着手時の本文は「これからやること」で書かれている。最終的な差分に合わせて書き直す。

```bash
gh pr diff <PR番号> --stat
gh pr edit <PR番号> --body-file <path>
```

書式は `references/pr-body.md`。`Closes #<N>` は消さない。

**ここを飛ばすと、マージ後に残るのは実態と違う説明だけになる。** PR 本文はコミットログより読まれる。

### 7. 完了を報告する

```bash
gh pr comment <PR番号> --body "<完了報告>"
```

書式は `references/completion-comment.md`。実装が終わったこと、レビューを何周したこと、残っている判断があればそれを書く。

最後に、人が判断すべきこと（マージの可否、残指摘の扱い）を会話へ短く出す。**マージはしない。**

## やらないこと

- **マージしない。** PR を出して報告するところまで。マージは人が決める
- **レビューの中身を自前で作らない。** `pr-review` に委譲する
- **Issue に書かれていないことを実装しない。** 気づいたことは Issue かコメントで提案する
- **打ち切りを黙って行わない。** 残指摘は必ず PR に残す

## 参照ファイル

- `references/pr-body.md` — 仕上げ時の PR 本文の書式
- `references/completion-comment.md` — 完了コメントの書式
- `references/pending-comment.md` — 打ち切り時の残指摘コメントの書式
